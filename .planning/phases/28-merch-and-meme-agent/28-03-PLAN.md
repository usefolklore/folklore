---
phase: 28-merch-and-meme-agent
plan: 03
type: execute
wave: 2
depends_on: ["28-01", "28-02"]
files_modified: [site/index.html]
autonomous: true
requirements: [AGENT-02]
must_haves:
  truths:
    - "The #memes section renders memes from site/assets/memes.json when it is fetchable (e.g. served over http)"
    - "When memes.json cannot be fetched (e.g. opened at file://), the existing static meme cards still display — graceful fallback, no blank section"
    - "Agent-posted memes appear on the site without any manual HTML edit (re-running the agent + reload shows the new entry)"
    - "The render uses the MemeEntry schema (image, alt, caption) defined in Plan 28-02 — no schema drift"
    - "The site stays self-contained: no new external host, no new dependency, no build step required"
  artifacts:
    - path: "site/index.html"
      provides: "a memegrid that fetches site/assets/memes.json and renders MemeEntry cards, with the static cards as graceful fallback"
      contains: "memes.json"
  key_links:
    - from: "the #memes grid script"
      to: "site/assets/memes.json"
      via: "fetch() with .catch fallback to the static cards already in the DOM"
      pattern: "fetch\\(.*memes\\.json"
    - from: "each fetched MemeEntry"
      to: "a folk-pop meme card (img src=entry.image alt=entry.alt)"
      via: "DOM render mirroring the existing .memegrid card markup"
      pattern: "\\.image|\\.alt|\\.caption"
---

<objective>
Make the site render agent-posted memes by fetching `site/assets/memes.json`
(the schema seeded in Plan 28-02) into the existing `#memes` grid, with the
current static meme cards kept as a graceful fallback so the page still works at
file:// where fetch() of a local JSON is blocked.

Purpose: AGENT-02 — close the loop "memes by the network": the agent appends to
memes.json (Plan 28-02), and the site surfaces those entries automatically, with
no manual HTML edit per meme. The fallback guarantees the single-file site stays
self-contained and never shows a blank memes section.

Output: an edited `site/index.html` whose memes grid is data-driven from
memes.json with a static-card fallback.

WAVE NOTE: this plan edits site/index.html, the SAME file Plan 28-01 edits, so it
runs in Wave 2 AFTER 28-01 (serialized to avoid concurrent-edit collision). It also
depends on 28-02 for the MemeEntry schema + seeded memes.json.
</objective>

<execution_context>
@/Users/saharbarak/.claude/get-shit-done/workflows/execute-plan.md
@/Users/saharbarak/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-merch-and-meme-agent/28-CONTEXT.md

# The memes section + the trailing <script> this plan edits:
@site/index.html

# The schema this render must match (from Plan 28-02):
@src/agents/meme-agent/types.ts
@site/assets/memes.json

<interfaces>
<!-- MemeEntry schema (from Plan 28-02 src/agents/meme-agent/types.ts) — render against this: -->
MemeEntry = { id, caption, image (relative "assets/gen/..."), alt, createdAt, source, postedUrl? }

<!-- The existing #memes grid in site/index.html (lines 532-548) — match this card markup exactly: -->
.memegrid = <div class="memegrid rv" data-d="1" style="display:grid;grid-template-columns:repeat(auto-fit,minmax(240px,1fr));gap:24px;margin-top:44px"> ... </div>
Each static card today:
  <div style="border:3px solid var(--ink);background:var(--paper);box-shadow:8px 8px 0 var(--<color>);overflow:hidden">
    <img src="assets/gen/<art>.png" alt="<alt>" style="width:100%;display:block"/>
  </div>
Shadow colors cycle: pink / blue / teal / yellow (see lines 539-544).

<!-- The trailing <script> (lines 561-584) — vanilla JS, no framework, no storage APIs.
     IntersectionObserver `io` reveals .rv elements. Add memes-fetch logic here. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Data-drive the #memes grid from memes.json with a graceful static fallback</name>
  <read_first>
    - site/index.html lines 532-548 (the #memes section + .memegrid static cards)
    - site/index.html lines 561-584 (the trailing vanilla-JS <script>: io observer, .copy, parallax)
    - src/agents/meme-agent/types.ts (MemeEntry fields) and site/assets/memes.json (seed)
  </read_first>
  <action>
    Edit `site/index.html` ONLY. Make the memes grid data-driven while keeping the
    existing static cards as the fallback:

    1. Keep the six existing static `.memegrid` cards in the DOM as-is — they ARE the
       fallback. Give the grid a stable hook if needed (it already has class
       `memegrid`; a `data-static` marker on the wrapper is fine), so JS can detect
       and replace them only on a successful fetch.

    2. In the trailing `<script>`, add a small async block that:
         - `fetch('assets/gen/...'-relative) → fetch('assets/memes.json')`
         - on success with a non-empty array: clear the static cards and render one
           card per MemeEntry, mirroring the existing card markup exactly (ink border,
           cycling pink/blue/teal/yellow `box-shadow`, `overflow:hidden`,
           `<img src="<entry.image>" alt="<entry.alt>" style="width:100%;display:block"/>`).
           Optionally add the caption as a small mono line or the img title.
         - on ANY failure (network error, file://, bad JSON, empty array): do NOTHING —
           leave the static cards in place. Wrap in try/catch and `.catch()` so a
           file:// `fetch` rejection is swallowed silently (no console error spam, no
           blank section).
       Use only vanilla DOM APIs (no framework, NO localStorage/sessionStorage — banned).
       Respect the existing `.rv` reveal: render before/without breaking the
       IntersectionObserver, or re-observe the new cards if needed.

    3. Keep image refs RELATIVE (entry.image is already `assets/gen/...`). Do not add
       any new external host. Do not touch the Store section (owned by 28-01) or the
       hero/commons/graph videos.
  </action>
  <acceptance_criteria>
    - `grep -nE "fetch\\(.*memes\\.json" site/index.html` returns the fetch call.
    - `grep -nE "\\.catch|try\\s*\\{" site/index.html` shows the fetch is guarded (fallback path exists).
    - `grep -cE "memegrid" site/index.html` >= 1 and the six static `<img src="assets/gen/` cards are still present in the static markup (grep the static block: `grep -c "assets/gen/.*folk meme\\|assets/gen/meme-" site/index.html` >= 4).
    - `grep -nE "localStorage|sessionStorage" site/index.html` returns nothing (banned storage not introduced).
    - Fallback proof (file://): `chrome --headless=new --screenshot` of `file://.../site/index.html` shows the #memes section populated with cards (static fallback renders, section not blank). Attach PNG path in SUMMARY.
    - Data-driven proof (http): serve site/ with `python3 -m http.server`, load it headless, and confirm the grid renders entries from memes.json (e.g. card count == memes.json length, or a known seed alt text present in the rendered DOM). Attach evidence in SUMMARY.
  </acceptance_criteria>
  <done>The #memes grid renders MemeEntry cards from site/assets/memes.json when served over http and falls back to the existing static cards at file:// (never blank); no banned storage, no new external host, only site/index.html touched.</done>
</task>

<task type="auto">
  <name>Task 2: Verify the agent → site round-trip (no manual edit) and mobile no-overflow</name>
  <read_first>
    - site/index.html (memes grid after Task 1)
    - src/agents/meme-agent/run.ts (the dry-run entrypoint from 28-02)
  </read_first>
  <action>
    Prove the loop end-to-end and the responsive invariant, then restore the seed:

    1. Round-trip: run the agent in dry-run (`node --import tsx src/agents/meme-agent/run.ts
       --text "round-trip check"`) so it appends one MemeEntry to site/assets/memes.json.
       Serve site/ over http, load headless, and confirm the new meme appears in the
       rendered #memes grid WITHOUT any HTML edit (the card count grew by one, or the
       new caption/alt is present in the DOM). This demonstrates "agent-posted memes
       appear on the site without manual edits".
    2. After verifying, RESTORE site/assets/memes.json to its committed seed state
       (the test append must not pollute the committed data file) — note this in SUMMARY.
    3. Mobile sweep: headless screenshot at 390px of the #memes section; confirm
       full-page mobile PNG width == 390 (no horizontal overflow), cards stack
       single-column. The grid uses the existing auto-fit minmax so this should hold;
       confirm the data-driven cards don't break it.
  </action>
  <acceptance_criteria>
    - Round-trip evidence: a before/after card-count or DOM-content diff showing the dry-run-appended meme rendered by the unchanged site/index.html (attach in SUMMARY).
    - `git -C /Users/saharbarak/personal/akashik status --porcelain site/assets/memes.json` shows it clean / restored to the seed after the round-trip test (no test-append committed).
    - 390px screenshot: full-page mobile PNG of #memes; PNG width == 390 (no horizontal overflow). Attach path in SUMMARY.
    - `npx tsc --noEmit` exits 0 (no source regression) and `git diff --name-only` for this plan lists ONLY site/index.html (memes.json restored to seed).
  </acceptance_criteria>
  <done>Re-running the dry-run agent + reloading the served site shows the newly-appended meme with zero HTML edits; memes.json is restored to its clean seed; #memes has no horizontal overflow at 390px.</done>
</task>

</tasks>

<verification>
- Only `site/index.html` is modified by this plan (memes.json stays the 28-02 seed).
- `grep -nE "fetch\\(.*memes\\.json" site/index.html` present; fetch guarded by try/catch + .catch.
- file:// load → static fallback cards render (section not blank); http load → cards render from memes.json.
- Re-running the dry-run agent appends an entry that the unchanged site surfaces (round-trip proven).
- No localStorage/sessionStorage, no new external host, no new dep, no build step.
- #memes has zero horizontal overflow at 390px (full-page PNG width == 390).
</verification>

<success_criteria>
The site's #memes grid is data-driven from site/assets/memes.json (the Plan 28-02
schema) with the existing static cards as a graceful file:// fallback, so
agent-posted memes surface automatically with no manual HTML edit, the section is
never blank, no banned storage / external host / dependency is introduced, and the
section has no horizontal overflow at 390px. AGENT-02 satisfied.
</success_criteria>

<output>
After completion, create `.planning/phases/28-merch-and-meme-agent/28-03-SUMMARY.md`.
Conventional commit, no AI co-authors. Then `roadmap update-plan-progress 28` and,
once 28-01/28-02/28-03 are all done, `requirements mark-complete MERCH-01 AGENT-01 AGENT-02`.
</output>
