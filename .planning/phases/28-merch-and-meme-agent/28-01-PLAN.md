---
phase: 28-merch-and-meme-agent
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [site/index.html]
autonomous: true
requirements: [MERCH-01]
must_haves:
  truths:
    - "Each of the three Store product cards (tee, sticker pack, pin) shows a real merch mockup, not a raw catalogue render of the source art"
    - "Each card carries a real, concrete print spec (material, technique, size) instead of placeholder marketing copy only"
    - "The mockups are crafted from existing assets / CSS / SVG composites — zero new image files require higgsfield credits"
    - "The folk-pop skin (ink borders, per-card pink/blue/teal hard shadows, clamped type) and the inert-but-wired buy CTAs are preserved by construction"
    - "The site stays self-contained and renders identically at file:// (no new network dependency introduced by the merch change)"
  artifacts:
    - path: "site/index.html"
      provides: "Store <section id=\"store\"> .prod cards upgraded to merch mockup composites + real spec text"
      contains: "data-buy=\"tee\""
  key_links:
    - from: "each .prod .shot"
      to: "a mockup composite (CSS-framed product shot, not the bare gen art)"
      via: "added .shot framing class + spec markup"
      pattern: "class=\"shot"
    - from: "each .prod card"
      to: "a real print spec line"
      via: "a .spec element listing material/technique/size"
      pattern: "class=\"spec"
---

<objective>
Replace the Phase-27 Store product-card placeholders with real merch product
mockups derived from the existing folk art, plus concrete print specs — so the
three cards (Never-Research-Twice Tee, Folk Sticker Pack, The Teller Enamel Pin)
read like real products a peer could buy, not catalogue renders of raw art.

Purpose: MERCH-01 — the Store is wired and structured (Phase 27) but its product
shots are the bare `assets/gen/*.png` art and the copy has no print spec. This
plan makes the merch real: mockup composites built from existing assets + CSS/SVG
framing (a tee mock, a sticker-sheet mock, an enamel-pin mock) and a real spec
line per card. No higgsfield credits (blocked), no new image files that need
generation, no fabricated shop URLs (the inert-but-wired CTAs stay inert).

Output: an edited `site/index.html` Store section with mockup-framed product
shots + `.spec` lines; folk-pop brand lock and inert buy CTAs untouched.
</objective>

<execution_context>
@/Users/saharbarak/.claude/get-shit-done/workflows/execute-plan.md
@/Users/saharbarak/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/28-merch-and-meme-agent/28-CONTEXT.md

# The exact Store markup + CSS this plan edits (built in 27-03):
@site/index.html

<interfaces>
<!-- The Store markup/CSS already on disk (site/index.html). Edit in place; do NOT
     rebuild the card system. Executor uses these selectors directly. -->

CSS (lines ~181-199):
  .shop   = grid repeat(auto-fit,minmax(240px,1fr)) of .prod cards
  .prod   = ink-bordered card; :nth-child(1/2/3) get pink/blue/teal 9px hard shadow
  .prod .shot      = ink-bordered 1:1 framed image box (paper-2 bg); nth-child hard shadow 6px
  .prod .shot img  = width:100% height:100% object-fit:cover
  .prod .shot.pair = flex, two half-width bordered imgs (used by the sticker card)
  .prod .soon      = mono uppercase "Drops to peers first" chip
  .prod h4 / .desc / .foot / .price / .buy = name / description / footer / price / inert CTA

Available folk art in site/assets/gen/ (all ~1-2.5MB PNGs, RELATIVE refs only):
  tee.png, char1.png, sun.png, bird.png, fishflower.png, coin.png, border.png,
  campfire.png, commons.png, graph.png, meme-amnesia.png, meme-coinrain.png

Store markup today (lines 483-517): three .prod cards —
  card1 data-buy="tee"          .shot img=tee.png
  card2 data-buy="sticker-pack" .shot.pair img=fishflower.png + sun.png
  card3 data-buy="enamel-pin"   .shot img=char1.png
Each card: .soon chip, .shot, h4, .desc, .foot(.price data-price="" $—, <!-- TODO --> , a.buy aria-disabled).

Brand tokens: --ink #1d1813 / --paper(-2) cream / --pink #ff4f6d / --blue #2b3a8c
  / --teal #1fae8b / --yellow #f5b921 ; fonts Fraunces + Geist Mono (--mono).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Upgrade the three product shots into merch mockup composites (CSS/SVG framed, from existing art)</name>
  <read_first>
    - site/index.html lines 181-199 (.shop/.prod/.shot CSS) and 483-517 (the three .prod cards)
    - site/assets/gen/ listing (tee.png, char1.png, sun.png, fishflower.png, bird.png)
  </read_first>
  <action>
    Edit `site/index.html` ONLY. For each of the three .prod cards, turn the bare
    `.shot` image into a *mockup composite* using existing assets + scoped CSS/SVG —
    NO new generated image files, NO higgsfield, NO new external host. Concretely:

    1. Add a small scoped CSS block next to the existing .shop/.prod rules (after
       line ~199) introducing mockup framing classes, e.g.:
         .shot.tee   — a garment-mock: dark cotton field (background:var(--ink) or a
                       CSS-drawn tee silhouette via radial/linear-gradient or an inline
                       SVG tee outline) with tee.png/char1.png art centered + scaled
                       (object-fit:contain, max-width ~62%) so it reads as a PRINT on a
                       shirt, not a full-bleed catalogue render.
         .shot.sheet — a sticker-SHEET mock: paper-2 field with a dashed die-cut look
                       (CSS dashed border / repeating-linear-gradient) holding the two
                       sticker arts (fishflower.png + sun.png) as individual die-cut
                       chips (each with a thin ink border + slight rotation via transform).
         .shot.pin   — an enamel-PIN mock: a circular metal bezel (border-radius:50%,
                       ink border + inset highlight via box-shadow inset) framing char1.png
                       so it reads as a round hard-enamel pin, optionally with a tiny
                       "pin post" hint.
       Keep all framing CSS scoped to these classes; reuse existing tokens; do not
       touch the generic .prod/.shot rules other peers' cards rely on.

    2. Apply the classes in the markup: card1 .shot → "shot tee", card2 .shot.pair →
       "shot sheet" (replace/augment the inline display:flex with the sheet treatment),
       card3 .shot → "shot pin". Keep the existing img src refs RELATIVE
       (assets/gen/...). Keep alt text accurate to the mockup ("…tee print mockup",
       "…sticker sheet mockup", "…enamel pin mockup").

    3. Preserve the per-card pink/blue/teal hard shadows and the 1:1 aspect framing —
       the mockup must still sit inside the ink-bordered .shot box with its colored
       shadow. Do NOT change .soon/.foot/.price/.buy or the <!-- TODO: real product URL --> comments.

    The goal: a viewer sees a tee with a print, a die-cut sticker sheet, and a round
    enamel pin — three distinct *product* mockups — all assembled from art already in
    the repo with CSS, costing zero credits and adding zero files.
  </action>
  <acceptance_criteria>
    - `grep -nE "shot (tee|sheet|pin)|class=\"shot tee|class=\"shot sheet|class=\"shot pin" site/index.html` returns the three new mockup classes applied in markup.
    - `grep -cE "data-buy=\"(tee|sticker-pack|enamel-pin)\"" site/index.html` == 3 (the three inert CTAs are still present, unchanged).
    - `grep -nE "<!-- TODO: real product URL" site/index.html | wc -l` == 3 (the launch TODO comments are preserved).
    - `git -C /Users/saharbarak/personal/akashik diff --name-only` lists ONLY site/index.html (no new image files added under site/assets/gen/).
    - `grep -cE "higgsfield|http://|https?://(?!bags\\.fm|x\\.com|github\\.com)" site/index.html` shows no NEW external image host introduced by this change (diff-scoped check; bags.fm/x.com/github.com pre-existing links allowed).
    - Headless screenshot proof at 1512px: clone site to /tmp, force `.rv` visible (`min-height:100svh→auto`), capture `#store` with `chrome --headless=new --screenshot`; the three shots visibly read as tee-print / sticker-sheet / round-pin (attach PNG path in SUMMARY).
  </acceptance_criteria>
  <done>The three Store product shots are merch mockups (tee print, sticker sheet, enamel pin) built from existing art + CSS, with folk-pop shadows and inert CTAs intact, zero new files, zero credits.</done>
</task>

<task type="auto">
  <name>Task 2: Add a real print spec line to each product card</name>
  <read_first>
    - site/index.html .prod card markup (now mockup-framed from Task 1)
    - site/index.html .prod h4/.desc/.foot CSS (~195-199) to match type scale
  </read_first>
  <action>
    Edit `site/index.html` ONLY. Add one concrete print-spec line per .prod card,
    between `.desc` and `.foot`, as a `<p class="spec">…</p>` (or `<ul class="spec">`)
    with real, plausible merch specs — NOT marketing fluff. Add a scoped `.spec` CSS
    rule (mono, small, ~11-12px, letter-spacing, muted ink) next to the .prod rules.

    Specs (concrete, print-shop-real):
      - Tee:          "Heavyweight 100% cotton · screen-printed front · S–3XL · ink on dark"
      - Sticker Pack: "Die-cut vinyl · matte laminate · weatherproof · 6-sticker sheet ~3in"
      - Enamel Pin:   "Hard enamel · 1.25in · gold plating · double rubber clutch backing"

    Keep the existing `.desc` copy. The `.spec` line is the print/material truth that
    makes each card a real product spec; phrase it in the folk-pop voice but keep the
    material facts concrete and grep-checkable. Do not alter .price/.buy/.soon.
  </action>
  <acceptance_criteria>
    - `grep -cE "class=\"spec" site/index.html` >= 3 (one spec per card).
    - `grep -nE "cotton|screen-print|Die-cut|vinyl|enamel|plating|clutch" site/index.html` returns the concrete material terms for all three products.
    - `grep -nE "\\.spec\\{" site/index.html` returns the scoped CSS rule for the spec line.
    - `npx tsc --noEmit` exits 0 (site is static; this confirms no source regression — should be unaffected).
    - Headless screenshot at 390px: full-page mobile PNG of `#store`; PNG width == 390 (no horizontal overflow); the .spec line renders under each .desc (attach path in SUMMARY).
  </acceptance_criteria>
  <done>Each of the three product cards carries a concrete, grep-checkable print spec (material/technique/size) in folk-pop voice, scoped CSS added, no overflow at 390px.</done>
</task>

</tasks>

<verification>
- Only `site/index.html` is modified (git diff --name-only).
- `grep -cE "data-buy=\"(tee|sticker-pack|enamel-pin)\"" site/index.html` == 3; the three `<!-- TODO: real product URL -->` comments survive.
- Three mockup classes (shot tee / shot sheet / shot pin) and three `.spec` lines present.
- No new files under site/assets/gen/; no new external image host; no higgsfield invocation.
- Headless Chrome at 1512px and 390px: Store renders mockups + specs, doc horizontal overflow == 0 at 390 (full-page PNG width == 390).
</verification>

<success_criteria>
The Store's three product cards show real merch mockups (tee print, die-cut sticker
sheet, round enamel pin) crafted from existing art + CSS at zero credit cost, each
with a concrete print spec, with the folk-pop skin and inert-but-wired buy CTAs
preserved by construction and no horizontal overflow at 390px. MERCH-01 satisfied.
</success_criteria>

<output>
After completion, create `.planning/phases/28-merch-and-meme-agent/28-01-SUMMARY.md`.
Conventional commit, no AI co-authors. Then `roadmap update-plan-progress 28`.
</output>
