---
phase: 25-cleanup-repo-restructure
plan: 04
type: execute
wave: 2
depends_on: ["25-03"]
files_modified:
  - docs/architecture/REPO-LAYOUT.md
  - docs/REPO-SPLIT.md
  - spec/README.md
  - examples/README.md
  - README.md
autonomous: true
requirements: [REPO-01, REPO-03]
must_haves:
  truths:
    - "The repository follows a documented akashikprotocol-clean layout covering src domains, docs, tests, spec, site, examples, and bench"
    - "A spec surface exists (the protocol/RFC docs are reachable as the spec) and an examples surface exists"
    - "A written org-split plan defines the folklore core+cli / spec / site / .github boundaries without physically splitting the repo this phase"
    - "The README top-level map matches the actual on-disk layout"
  artifacts:
    - path: "docs/architecture/REPO-LAYOUT.md"
      provides: "Authoritative description of the akashikprotocol-clean directory layout"
      min_lines: 60
      contains: "examples"
    - path: "docs/REPO-SPLIT.md"
      provides: "Written org-boundary split plan (core+cli / spec / site / .github)"
      min_lines: 50
      contains: "usefolklore"
    - path: "examples/README.md"
      provides: "Examples surface entry point"
    - path: "spec/README.md"
      provides: "Spec surface index pointing at RFCs + protocol docs"
  key_links:
    - from: "docs/architecture/REPO-LAYOUT.md"
      to: "bench/"
      via: "layout references the consolidated bench dir from 25-03"
      pattern: "bench/"
    - from: "README.md"
      to: "docs/architecture/REPO-LAYOUT.md"
      via: "repo map link"
      pattern: "REPO-LAYOUT"
---

<objective>
Reorganize the repository into a documented akashikprotocol-clean layout (src domains, docs, tests, spec, site, examples, bench) and author a written org-split plan defining the folklore core+cli / spec / site / .github boundaries — physical multi-repo split deferred (blocked on org creation per CONTEXT.md).

Purpose: REPO-01 wants a well-thought, org-ready layout; REPO-03 wants documented org boundaries with a split plan. The repo already has src/ (functional-DDD domains), docs/, tests/, site/, and (after 25-03) bench/. The two gaps versus akashikprotocol-clean are a clear spec surface and an examples surface. This plan establishes both with minimal, low-risk additions, documents the whole layout authoritatively, and writes the split plan — without a destabilizing mass file move.
Output: docs/architecture/REPO-LAYOUT.md (the layout map), docs/REPO-SPLIT.md (the org split plan), a spec/ surface, an examples/ surface, and an updated README top-level repo map.

Scope guard: Do NOT physically split repos. Do NOT move src/ files (that risks the 942-test bar — see 25-02 rationale). The "spec" surface is satisfied by a thin spec/README.md index that links to docs/protocol + docs/rfc. Prefer documentation + a thin examples surface over churny moves. Do NOT touch any file under site/.
</objective>

<execution_context>
@/Users/saharbarak/.claude/get-shit-done/workflows/execute-plan.md
@/Users/saharbarak/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/25-cleanup-repo-restructure/25-CONTEXT.md
@.planning/phases/25-cleanup-repo-restructure/25-03-SUMMARY.md

<facts>
<!-- Discovered during planning. -->

Current top-level layout (relevant dirs):
  src/        — functional-DDD: domain/, application/, infrastructure/, cli/, daemon/, mcp/
  tests/      — 85 *.test.ts files, ~938 test cases (the suite npm test runs)
  docs/       — architecture/, rfc/ (0001-folklore-core.md + README.md index), protocol/, research/,
                product/ (BENCHMARKS.md), p2p/, brand/, marketing/, whitepaper, ADRs.
  site/       — the folk-pop site (KEEP byte-intact; do not touch site files).
  bench/      — (created in 25-03) the consolidated benchmark runners + README.
  folklore-rs/— Rust retrieval crate (Cargo.toml).
  demo/       — recorded demo tapes/gifs + research-corpus.
  .github/    — workflows/ (ci.yml, release.yml), pull_request_template.md. NO .github/profile yet
                (org profile README is DOCS-03, Phase 26 — do NOT create it here).
  bin/        — folklore.js entrypoint.  config/ — config.example.yaml.  vendor/ — graphify submodule.

NO examples/ dir exists yet — create it (examples/README.md).
NO spec/ dir exists — create a thin spec/README.md that links to docs/rfc + docs/protocol. The protocol
  spec actually lives in docs/rfc/ (README.md index + 0001-folklore-core.md), docs/protocol/
  (PROTOCOL-QUALITY-QUESTIONS.md), and docs/architecture/ (V3/V4/V5-PROTOCOL.md; V5 is current).

Org-split target (CONTEXT.md "Mirror akashikprotocol's clean separation spec / core / site / .github";
  ROADMAP Wave C "core+cli / spec / site / .github"):
  - folklore (core + cli): src/, tests/, bench/, folklore-rs/, bin/, config/, package.json, engine docs.
  - folklore-spec (or the spec/ surface): docs/rfc/, docs/protocol/, the versioned protocol docs.
  - folklore-site: site/ + wrangler.toml + _headers.
  - .github (org profile): the usefolklore org profile README (authored later in DOCS-03).
  Physical split is BLOCKED ON USER (GitHub org creation). This phase writes the PLAN only — a doc.

README.md currently exists (13KB). It should carry an accurate top-level repo map after this plan.
</facts>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Establish the spec + examples surfaces</name>
  <read_first>
    - run `ls docs/rfc/ docs/protocol/ docs/architecture/` (confirm what the spec surface points at)
    - docs/rfc/README.md (the existing RFC index — spec/README links to it)
    - run `node bin/folklore.js help` OR read src/cli/commands/help.ts (to derive REAL example commands)
    - run `ls bench/` (confirm 25-03 landed bench/ before referencing it)
  </read_first>
  <files>spec/README.md, examples/README.md</files>
  <action>
    Create the spec surface: write `spec/README.md` (~20-40 lines) stating the Folklore protocol
    specification lives in docs/rfc/ (RFCs, starting RFC-0001) and docs/protocol/ + the versioned protocol
    docs in docs/architecture/ (V5-PROTOCOL.md is current). Link to docs/rfc/README.md as the canonical RFC
    index. Documentation only — no build impact.
    Create the examples surface: write `examples/README.md` with at least two concrete, copy-paste runnable
    examples derived from real CLI behavior (verify every flag/subcommand against `node bin/folklore.js help`
    or src/cli/commands/help.ts before writing — do NOT invent commands), e.g.:
      - Index + query a codebase: `folklore codebase index <path>` then `folklore codebase search <term>`.
      - Ask the graph (network-before-web): `folklore ask "<query>"`.
      - Optionally federated: `folklore ask "<query>" --peers`.
  </action>
  <verify>
    <automated>test -f spec/README.md && test -f examples/README.md && grep -q 'rfc' spec/README.md && grep -q 'folklore' examples/README.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - spec/README.md exists and references docs/rfc (the RFC index) and the current protocol doc (V5).
    - examples/README.md exists and contains at least two distinct `folklore ...` commands.
    - Every `folklore <subcommand>` shown in examples/README.md appears in `node bin/folklore.js help` output (no invented commands).
  </acceptance_criteria>
  <done>The repo has an explicit spec surface (spec/README.md → docs/rfc + protocol docs) and an examples surface (examples/README.md with real runnable commands).</done>
</task>

<task type="auto">
  <name>Task 2: Author the akashikprotocol-clean layout map (REPO-01)</name>
  <read_first>
    - run `ls` at repo root + `ls src/` (enumerate the real top-level + src layout)
    - .planning/phases/25-cleanup-repo-restructure/25-03-SUMMARY.md (the exact bench/ contents)
    - spec/README.md + examples/README.md (the surfaces created in Task 1 — reference them accurately)
    - docs/architecture/RETRIEVAL-MODULES.md (created in 25-02; link it from the layout map if present)
  </read_first>
  <files>docs/architecture/REPO-LAYOUT.md</files>
  <action>
    Write docs/architecture/REPO-LAYOUT.md describing the akashikprotocol-clean layout — prose plus a
    directory tree and a role table:
    - A directory tree of the top level mapping each dir to its role: src/ (functional-DDD engine:
      domain/application/infrastructure/cli/daemon/mcp), tests/ (the suite), bench/ (standalone benchmark
      runners — reference 25-03's contents), spec/ (protocol + RFCs), docs/ (architecture, product,
      research, brand), site/ (folk-pop site), examples/ (runnable usage), folklore-rs/ (Rust retrieval
      crate), bin/ (CLI entry), config/, vendor/ (graphify submodule), .github/ (CI + org).
    - State explicitly where the spec surface is and where examples live (closing the two akashikprotocol gaps).
    - A short "design principles" paragraph: functional DDD, IO at the edges, docs/spec separated from code,
      benchmarks isolated from the test suite, site kept independent for separate deploy.
    - Link to docs/architecture/RETRIEVAL-MODULES.md and spec/README.md.
    Every directory cited MUST exist on disk. Do NOT cite a directory that does not exist (e.g. do not claim
    a .github/profile — that is Phase 26).
  </action>
  <verify>
    <automated>test -f docs/architecture/REPO-LAYOUT.md && for d in src tests bench spec docs site examples folklore-rs bin config; do test -d "$d" || { echo "MISSING DIR: $d"; exit 1; }; done && grep -q 'examples' docs/architecture/REPO-LAYOUT.md && grep -q 'spec' docs/architecture/REPO-LAYOUT.md && grep -q 'bench/' docs/architecture/REPO-LAYOUT.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - docs/architecture/REPO-LAYOUT.md exists and is at least 60 lines.
    - Every top-level dir it names (src, tests, bench, spec, docs, site, examples, folklore-rs, bin, config) exists on disk (verify loop exits 0 — no MISSING DIR).
    - It contains the strings `examples`, `spec`, and `bench/`.
    - It does NOT claim a `.github/profile` directory exists.
  </acceptance_criteria>
  <done>An authoritative layout doc maps every top-level directory to its role, identifies the spec + examples surfaces, and references the consolidated bench/ dir — citing only directories that exist.</done>
</task>

<task type="auto">
  <name>Task 3: Write the org-split plan (REPO-03) and update the README repo map</name>
  <read_first>
    - docs/architecture/REPO-LAYOUT.md (from Task 2 — the split plan partitions this layout)
    - .planning/ROADMAP.md Wave C + .planning/phases/25-cleanup-repo-restructure/25-CONTEXT.md <specifics> (the split intent)
    - README.md (full — locate where a top-level repo map belongs; preserve all existing content)
  </read_first>
  <files>docs/REPO-SPLIT.md, README.md</files>
  <action>
    Write docs/REPO-SPLIT.md as the written org-boundary split plan (this is a DOC, not a physical split —
    physical split is blocked on usefolklore org creation). Include:
    - Statement of intent: mirror akashikprotocol's clean separation under the usefolklore org; split happens
      at push time once the org exists (out of scope this milestone).
    - A table of target repos and their contents:
        usefolklore/folklore        — core + cli: src/, tests/, bench/, folklore-rs/, bin/, config/, package.json
        usefolklore/folklore-spec   — spec/, docs/rfc/, docs/protocol/, versioned protocol docs
        usefolklore/folklore-site   — site/, wrangler.toml, _headers
        usefolklore/.github         — org profile README (authored in DOCS-03 / Phase 26)
    - Migration mechanics per repo: what `git mv` / `git filter-repo` boundary applies, which submodule
      (vendor/graphify) goes where, and what cross-repo references (e.g. site links to docs) need rewriting.
    - A "blocked-on-user" note: org creation, Cloudflare domain — do not attempt.
    Then update README.md: add (or refresh) a concise "Repository layout" section near the top that mirrors
    docs/architecture/REPO-LAYOUT.md's top-level tree and links to it (`docs/architecture/REPO-LAYOUT.md`)
    and to docs/REPO-SPLIT.md. Preserve all existing README content — only ADD the map/links section; do not
    delete prose.
  </action>
  <verify>
    <automated>test -f docs/REPO-SPLIT.md && grep -q 'usefolklore' docs/REPO-SPLIT.md && grep -q 'folklore-site' docs/REPO-SPLIT.md && grep -q 'REPO-LAYOUT' README.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - docs/REPO-SPLIT.md exists, is at least 50 lines, and names all four target repos (folklore, folklore-spec, folklore-site, .github) under the usefolklore org.
    - docs/REPO-SPLIT.md states the physical split is blocked on org creation (deferred this milestone).
    - README.md links to both `docs/architecture/REPO-LAYOUT.md` and `docs/REPO-SPLIT.md` and contains a top-level layout section.
    - `git diff README.md` shows only additions (no existing content deleted) — verify the line count did not decrease.
  </acceptance_criteria>
  <done>A written org-split plan defines the core+cli / spec / site / .github boundaries under usefolklore (physical split deferred), and the README carries an accurate top-level repo map linking to the layout + split docs.</done>
</task>

</tasks>

<verification>
- spec/, examples/ surfaces exist with real content; docs/architecture/REPO-LAYOUT.md + docs/REPO-SPLIT.md exist.
- Every directory named in REPO-LAYOUT.md exists on disk (no fabricated dirs).
- README links to the layout + split docs and only gained content.
- No src/ file moved, no site/ file touched: `git status --porcelain src/ site/` is empty.
- `npx tsc --noEmit` still exits 0 (docs-only changes did not affect the build).
</verification>

<success_criteria>
The repository follows a documented akashikprotocol-clean layout (src domains, docs, tests, spec, site, examples, bench) with explicit spec + examples surfaces, and a written org-split plan defines the folklore core+cli / spec / site / .github boundaries under usefolklore (physical split deferred, blocked on org). REPO-01 and REPO-03 satisfied.
</success_criteria>

<output>
After completion, create `.planning/phases/25-cleanup-repo-restructure/25-04-SUMMARY.md`.
</output>
