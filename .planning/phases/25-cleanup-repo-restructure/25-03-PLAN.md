---
phase: 25-cleanup-repo-restructure
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - bench/
  - scripts/
  - docs/product/BENCHMARKS.md
  - bench/README.md
autonomous: true
requirements: [CLEAN-05]
must_haves:
  truths:
    - "All standalone benchmark runner scripts live under a single bench/ directory, moved with git mv to preserve history"
    - "Each benchmark in bench/ runs from a documented copy-paste command and still resolves its imports (dist build) correctly"
    - "docs/product/BENCHMARKS.md repro commands point at the new bench/ paths, not the old scripts/ paths"
    - "npm run build and the full test suite still pass (no test imported a moved script)"
  artifacts:
    - path: "bench/README.md"
      provides: "Index of every benchmark with a copy-paste reproduction command and what it measures"
      min_lines: 40
      contains: "node bench/"
    - path: "bench/bench-beir.mjs"
      provides: "BEIR runner relocated under bench/"
  key_links:
    - from: "bench/bench-beir.mjs"
      to: "dist/infrastructure/embedders.js"
      via: "relative import after the move (../dist → must become correct depth)"
      pattern: "dist/infrastructure/embedders"
    - from: "docs/product/BENCHMARKS.md"
      to: "bench/"
      via: "repro command paths"
      pattern: "node bench/"
---

<objective>
Consolidate every standalone benchmark runner under a single documented bench/ directory with copy-paste reproduction commands, using git mv to preserve history and updating all references (relative imports inside the scripts, the docs repro commands, and the bench-v2 entrypoint).

Purpose: CLEAN-05 requires benchmark code consolidated under a documented bench/ structure with reproduction commands. Today the runners are scattered across scripts/ (bench-*.mjs, sweep-*.mjs, qrel-*.mjs, debug-reranker.mjs, contextualize-corpus.mjs, jacobi-preconditioning.mjs, bench-v2.sh). The .test.ts bench files in tests/ are part of the test suite (run by `npm test`) and must NOT move — moving them would break the 942-test run. Only the standalone scripts move.
Output: A bench/ directory containing the moved runners + a bench/README.md index, with docs/product/BENCHMARKS.md repro commands updated, build green, and tests still passing.

CRITICAL coupling fact: the bench-*.mjs scripts import from `../dist/infrastructure/embedders.js` (relative to scripts/, which is one level under repo root). After `git mv scripts/bench-foo.mjs bench/bench-foo.mjs` the file is STILL one level under repo root, so `../dist/...` STILL resolves correctly. The relative depth does NOT change (scripts/ → bench/ are siblings). Therefore the import paths do NOT need editing — but you MUST verify this by running one benchmark after the move.
</objective>

<execution_context>
@/Users/saharbarak/.claude/get-shit-done/workflows/execute-plan.md
@/Users/saharbarak/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/25-cleanup-repo-restructure/25-CONTEXT.md
@docs/product/BENCHMARKS.md

<facts>
<!-- Discovered during planning. -->

Standalone benchmark scripts currently in scripts/ (MOVE these to bench/):
  bench-arguana-dense.mjs, bench-beir-rust.mjs, bench-beir-sota.mjs, bench-beir.mjs, bench-bridge.mjs,
  bench-compare.mjs, bench-compounding.mjs, bench-consolidation.mjs, bench-e2e.mjs,
  bench-embed-throughput.mjs, bench-index-health.mjs, bench-lab.mjs, bench-matryoshka.mjs,
  bench-ppr-multihop.mjs, bench-ppr.mjs, bench-room-routing.mjs, bench-subgraph-transfer.mjs,
  bench-user-value.mjs, bench-v2.sh, bench-value-model.mjs, bench-warm.mjs,
  sweep-rocchio.mjs, sweep-rrf.mjs, qrel-rejudge.mjs, qrel-rejudge-v2.mjs, qrel-rejudge-v3.mjs,
  contextualize-corpus.mjs, debug-reranker.mjs, jacobi-preconditioning.mjs
  (Confirm the exact set with `ls scripts/ | grep -E '^(bench-|sweep-|qrel-|debug-reranker|contextualize-corpus|jacobi-)'` before moving — move exactly what matches.)

Scripts that must STAY in scripts/ (NOT benchmarks): bootstrap.sh (npm + CI use it), render-og-cards.sh,
  and the gitignored fellows-eval/ directory. Do NOT move these.

Import coupling: bench-*.mjs use `from '../dist/infrastructure/embedders.js'` (16×),
  `from '../dist/infrastructure/vector-index.js'` (2×), `from '../dist/domain/pagerank.js'` (2×).
  scripts/ and bench/ are both direct children of the repo root → `../dist/...` resolves identically
  after the move. NO import edit needed; VERIFY by running one.

bench-v2.sh writes its report to `OUT=.planning/BENCH-v2.md` and calls `npm test`. After moving it to
  bench/bench-v2.sh, the OUT path (repo-root-relative via mktemp/`dirname`) still works because the script
  cd's nowhere — but update its self-reference footer line "Generated by: scripts/bench-v2.sh" → "bench/bench-v2.sh".

Docs references to update (docs/product/BENCHMARKS.md):
  line 32: `node scripts/bench-compounding.mjs`        → `node bench/bench-compounding.mjs`
  line 33: `node scripts/bench-subgraph-transfer.mjs`  → `node bench/bench-subgraph-transfer.mjs`
  line 34: `node scripts/bench-value-model.mjs`        → `node bench/bench-value-model.mjs`
  line 111:`node scripts/bench-beir-rust.mjs scifact ...`→ `node bench/bench-beir-rust.mjs scifact ...`
  line 114:`node scripts/bench-beir-sota.mjs scifact --hybrid` → `node bench/bench-beir-sota.mjs ...`
  line 117:`node scripts/bench-beir-sota.mjs scifact --hybrid --rerank` → `node bench/...`
  line 120:`node scripts/bench-room-routing.mjs ...`    → `node bench/bench-room-routing.mjs ...`
  (Grep `scripts/bench` across docs/ for the full set; replace every `scripts/<benchfile>` → `bench/<benchfile>`.)

package.json scripts block does NOT reference any of the moved files (only build/dev/bootstrap/doctor/start/lint/test).
  The "bootstrap" script → scripts/bootstrap.sh, which stays. So package.json needs NO edit. Verify with
  `grep -E 'scripts/bench|scripts/sweep|scripts/qrel' package.json` returning nothing.

tests/bench-*.test.ts import from `../src/...` and are run by `npm test`. They are NOT standalone runners.
  Leave them in tests/ untouched.
</facts>
</context>

<tasks>

<task type="auto">
  <name>Task 1: git mv the standalone benchmark runners into bench/</name>
  <read_first>
    - run `ls scripts/` (full listing — confirm exactly which files match the benchmark set in <facts>)
    - one sample runner, e.g. scripts/bench-beir.mjs lines 1-25 (confirm the `../dist/...` import depth)
    - scripts/bench-v2.sh (find the "Generated by:" footer line)
  </read_first>
  <files>bench/, scripts/</files>
  <action>
    Create the bench/ directory, then move the standalone runners with history-preserving `git mv`:
    `mkdir -p bench`
    For each file matching `scripts/(bench-*|sweep-*|qrel-*|debug-reranker|contextualize-corpus|jacobi-preconditioning).{mjs,sh}`
    that is in the benchmark set (NOT bootstrap.sh, NOT render-og-cards.sh, NOT scripts/fellows-eval/):
      `git mv scripts/<file> bench/<file>`
    Confirm the exact set first with the read_first ls. Move bench-v2.sh too: `git mv scripts/bench-v2.sh bench/bench-v2.sh`.
    Do NOT edit the `../dist/...` imports — scripts/ and bench/ are siblings under the repo root, so the
    relative depth is unchanged and the imports still resolve.
    After moving, edit bench/bench-v2.sh: change the footer self-reference from `scripts/bench-v2.sh`
    to `bench/bench-v2.sh`.
  </action>
  <verify>
    <automated>test -d bench && test -f bench/bench-beir.mjs && test -f scripts/bootstrap.sh && test -z "$(ls scripts/ | grep -E '^(bench-|sweep-|qrel-|debug-reranker|contextualize-corpus|jacobi-)')" && grep -q 'bench/bench-v2.sh' bench/bench-v2.sh && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - bench/ exists and contains the moved runners (bench/bench-beir.mjs present).
    - scripts/ no longer contains any file matching `^(bench-|sweep-|qrel-|debug-reranker|contextualize-corpus|jacobi-)`.
    - scripts/bootstrap.sh and scripts/render-og-cards.sh still exist (non-bench scripts preserved).
    - `git status --porcelain bench/ scripts/` shows the moves as renames (git mv preserved history).
    - bench/bench-v2.sh footer references `bench/bench-v2.sh`.
  </acceptance_criteria>
  <done>Every standalone benchmark runner lives under bench/ via git mv; non-bench scripts and the test suite's tests/bench-*.test.ts are untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Verify imports resolve post-move and write bench/README.md repro index</name>
  <read_first>
    - bench/bench-index-health.mjs OR bench/bench-compounding.mjs (a cheap, dependency-light runner to smoke-test the move — pick one whose top imports are just node:* + ../dist/* and that does not need network/datasets)
    - docs/product/BENCHMARKS.md (to mirror the measured numbers + methodology into the bench index)
    - bench/bench-v2.sh (to document it in the index)
  </read_first>
  <files>bench/README.md</files>
  <action>
    First, prove the move did not break imports: run `npm run build` (produces dist/), then run ONE cheap
    moved benchmark that imports `../dist/...`, e.g. `node bench/bench-index-health.mjs` (or another that
    needs no external dataset). It must start and load the dist module without a MODULE_NOT_FOUND error.
    If it errors with `Cannot find module '../dist/...'`, the depth assumption was wrong — fix the import
    to the correct relative depth and re-run.
    Then write bench/README.md as the documented index. Include:
    - A one-paragraph intro: this directory holds Folklore's standalone benchmark runners; they import the
      COMPILED build, so run `npm run build` first. The in-suite benchmark TESTS live in tests/bench-*.test.ts
      and run via `npm test` (point readers there too).
    - A "Quick start" block: `npm run build` then a representative `node bench/bench-beir.mjs scifact`.
    - A table: benchmark file | what it measures | copy-paste command. Cover the major runners: bench-beir.mjs
      (BEIR NDCG@10), bench-beir-sota.mjs (hybrid + rerank), bench-beir-rust.mjs (rust path),
      bench-compounding.mjs, bench-subgraph-transfer.mjs, bench-value-model.mjs, bench-ppr.mjs / bench-ppr-multihop.mjs,
      bench-matryoshka.mjs, bench-embed-throughput.mjs, sweep-rrf.mjs / sweep-rocchio.mjs, bench-v2.sh
      (full v2 system report). Only list files that actually exist in bench/ after the move (confirm via `ls bench/`).
    - Cite the honest measured headline: Wave-2 hybrid 72.30% NDCG@10 on BEIR SciFact (per STATE.md / BENCHMARKS.md).
    Every command in the README must use the `bench/<file>` path (not `scripts/`).
  </action>
  <verify>
    <automated>npm run build >/dev/null 2>&1 && test -f bench/README.md && grep -q 'node bench/' bench/README.md && test -z "$(grep -E 'node scripts/bench' bench/README.md)" && for f in $(grep -oE 'bench/[a-zA-Z0-9._-]+\.(mjs|sh)' bench/README.md | sort -u); do test -f "$f" || { echo "STALE: $f"; exit 1; }; done && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0 after the move.
    - A moved runner that imports `../dist/...` starts without MODULE_NOT_FOUND (smoke-tested manually during the task).
    - bench/README.md exists, is at least 40 lines, and every `bench/<file>` it references exists on disk (verify loop exits 0).
    - bench/README.md contains zero `node scripts/bench` references.
    - bench/README.md cites the `72.30` SciFact headline number.
  </acceptance_criteria>
  <done>Imports resolve post-move (build green + one runner smoke-tested), and bench/README.md is a complete copy-paste reproduction index pointing only at bench/ paths.</done>
</task>

<task type="auto">
  <name>Task 3: Update docs/product/BENCHMARKS.md repro commands to bench/ paths</name>
  <read_first>
    - docs/product/BENCHMARKS.md (full — locate every `scripts/bench...` / `scripts/sweep...` reference)
    - run `grep -rn 'scripts/bench\|scripts/sweep\|scripts/qrel' docs/` to find ALL doc references, not just BENCHMARKS.md
  </read_first>
  <files>docs/product/BENCHMARKS.md</files>
  <action>
    In docs/product/BENCHMARKS.md (and any other docs/ file the grep surfaces), replace every reproduction
    command path `scripts/<benchfile>` with `bench/<benchfile>` for the runners moved in Task 1. Specifically
    the known lines: bench-compounding.mjs, bench-subgraph-transfer.mjs, bench-value-model.mjs,
    bench-beir-rust.mjs, bench-beir-sota.mjs (×2), bench-room-routing.mjs. Do a final grep to ensure no
    `scripts/bench`, `scripts/sweep`, or `scripts/qrel` reference remains in docs/ pointing at a moved file.
    Leave references to scripts/bootstrap.sh and scripts/render-og-cards.sh (not moved) intact.
    Do NOT alter the benchmark NUMBERS or methodology text — only the command paths.
  </action>
  <verify>
    <automated>test -z "$(grep -rhE 'scripts/(bench-|sweep-|qrel-)' docs/ 2>/dev/null)" && grep -q 'bench/bench-beir' docs/product/BENCHMARKS.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rhE 'scripts/(bench-|sweep-|qrel-)' docs/` returns nothing (no stale repro paths anywhere in docs/).
    - docs/product/BENCHMARKS.md contains `bench/bench-beir` (a moved path is now referenced).
    - The numeric content (NDCG values, 72.30, 17%→1% etc.) is unchanged — `git diff docs/product/BENCHMARKS.md` shows only path edits, no number edits.
  </acceptance_criteria>
  <done>Every benchmark reproduction command in docs/ points at the new bench/ location; numbers and methodology are untouched.</done>
</task>

</tasks>

<verification>
- bench/ holds the moved runners; scripts/ holds no benchmark runners but keeps bootstrap.sh + render-og-cards.sh.
- `npm run build` exits 0; a moved runner smoke-tested without MODULE_NOT_FOUND.
- `npm test` still passes (the moved files were never imported by tests/ — confirm in the validation gate plan 25-05; locally `npm test` should show the same pass count as baseline).
- bench/README.md is a complete repro index using only bench/ paths.
- No `scripts/(bench-|sweep-|qrel-)` references remain in docs/.
- Moves are git renames (history preserved).
</verification>

<success_criteria>
All standalone benchmark runners are consolidated under a documented bench/ directory (git-mv'd, history preserved), with a copy-paste reproduction README, imports verified resolving against dist/, and every docs repro command updated to the new paths — build green and tests unaffected. CLEAN-05 satisfied.
</success_criteria>

<output>
After completion, create `.planning/phases/25-cleanup-repo-restructure/25-03-SUMMARY.md`. Record the exact list of files moved (so plan 25-04's repo-layout doc and 25-05's validation gate reference the real bench/ contents).
</output>
