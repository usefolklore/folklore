---
phase: 25-cleanup-repo-restructure
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/architecture/RETRIEVAL-MODULES.md
  - src/infrastructure/README.md
autonomous: true
requirements: [CLEAN-04]
must_haves:
  truths:
    - "A reader can find one authoritative document that maps every ML/embedding/retrieval module to its role and its place in the functional-DDD layering"
    - "The embedder modules and the hybrid retrieval pipeline (dense + lexical + RRF + rerank) are described as a coherent, documented module layout"
    - "The doc names real, current files that exist on disk (no stale paths)"
  artifacts:
    - path: "docs/architecture/RETRIEVAL-MODULES.md"
      provides: "Authoritative ML/embedding + hybrid-retrieval module map with the pipeline flow"
      min_lines: 60
      contains: "embedders.ts"
    - path: "src/infrastructure/README.md"
      provides: "In-tree index pointing at the retrieval module map"
      contains: "RETRIEVAL-MODULES"
  key_links:
    - from: "docs/architecture/RETRIEVAL-MODULES.md"
      to: "src/infrastructure/embedders.ts"
      via: "documented module reference"
      pattern: "embedders\\.ts"
    - from: "src/infrastructure/README.md"
      to: "docs/architecture/RETRIEVAL-MODULES.md"
      via: "doc cross-link"
      pattern: "RETRIEVAL-MODULES"
---

<objective>
Organize the ML / embedding / retrieval code into a documented module layout by producing one authoritative module-map document plus an in-tree index, WITHOUT physically moving source files.

Purpose: CLEAN-04 asks for "a clean, documented module layout (embedders, hybrid retrieval pipeline)". The embedder and retrieval modules already live in src/infrastructure/ under functional-DDD layering, and tsconfig pins `rootDir: ./src` with relative `.js` imports across ~80 test files and dozens of source files. Physically reshuffling these into subdirectories would force a large, risky import rewrite that endangers the 942-test zero-regression bar for no behavioral gain. The architect-correct way to satisfy "documented module layout" here is an authoritative layout doc that defines the module boundary and pipeline flow, plus a short in-tree README that indexes it. This is verifiable, low-risk, and makes the layout legible to anyone arriving at the org repo.
Output: docs/architecture/RETRIEVAL-MODULES.md (the map) and src/infrastructure/README.md (the index).

Scope guard: Do NOT move, rename, or delete any source file. Do NOT change any import. `npm run build` and the test suite must remain untouched by this plan.
</objective>

<execution_context>
@/Users/saharbarak/.claude/get-shit-done/workflows/execute-plan.md
@/Users/saharbarak/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/25-cleanup-repo-restructure/25-CONTEXT.md
@docs/architecture/ADR-001-v3-memory-protocol.md

<facts>
<!-- Discovered during planning. The ML/embedding/retrieval modules currently on disk: -->

Embedding / retrieval modules in src/infrastructure/ (all real, confirmed present):
  - embedders.ts          — ONNX / @xenova/transformers embedder(s) (xenovaEmbedder export). Dense vectors.
  - vector-index.ts       — sqlite-vec KNN index (dense ANN store + query).
  - cross-encoder.ts      — cross-encoder reranker.
  - llm-listwise-rerank.ts— LLM listwise reranker.
  - rust-retrieval.ts     — native Rust retrieval client (folklore-rs bridge).
  - search-sync.ts        — local hybrid search assembly.
  - search-gossip.ts      — federated/gossip search (also hosts swarm-sim responder — Folklore's own).
  - hw-detect.ts          — rerank hardware-tier picker (gpu>accelerated>cpu>minimal) that gates which reranker tier runs.
Related domain-layer logic (pure, in src/domain/): cross-rerank.ts, multi-rrf (RRF fusion), pagerank.ts,
  vectors.ts, eval-metrics.ts, llm-listwise-rerank.ts. (Confirm exact filenames with `ls src/domain | grep -iE 'rerank|rrf|vector|pagerank|eval'` before citing.)
Rust crate: folklore-rs/ (Cargo.toml + src/) — the accelerated retrieval backend rust-retrieval.ts talks to.

Measured retrieval facts to cite (from STATE.md "Retrieval / benchmarks"):
  - Wave-2 hybrid = nomic-embed-text-v1.5 dense + BM25 lexical fused via RRF = 72.30% NDCG@10 on BEIR SciFact.
  - Wave-3 reranker (bge-reranker-base) REGRESSED on scientific text (-1.92, MS-MARCO domain mismatch) — documented as avoid.
  - Wave-4 room-aware routing gate failed (+0.34 only) — rooms are not a retrieval signal.

Functional-DDD layering (from CLAUDE.md folklore section + STATE.md invariants):
  src/domain (pure types + helpers, neverthrow Results, no classes) → src/application (use-cases) →
  src/infrastructure (IO: embedders, sqlite-vec, rust bridge, libp2p) → src/cli / src/daemon / src/mcp (entry points).

tsconfig.json: rootDir ./src, include src/**/*, NodeNext module resolution, imports use relative `.js` extensions.
</facts>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the authoritative retrieval module map</name>
  <read_first>
    - src/infrastructure/embedders.ts (top of file — confirm exports/role)
    - src/infrastructure/vector-index.ts (top — confirm sqlite-vec role)
    - src/infrastructure/cross-encoder.ts (top)
    - src/infrastructure/search-sync.ts (top — how dense + lexical are assembled)
    - src/infrastructure/rust-retrieval.ts (top — the folklore-rs bridge)
    - src/infrastructure/hw-detect.ts (lines 1-130 — rerank tier picker)
    - run `ls src/domain | grep -iE 'rerank|rrf|vector|pagerank|eval'` to confirm domain-layer filenames before citing them
    - docs/architecture/ADR-001-v3-memory-protocol.md (for tone / format consistency with existing architecture docs)
  </read_first>
  <files>docs/architecture/RETRIEVAL-MODULES.md</files>
  <action>
    Write docs/architecture/RETRIEVAL-MODULES.md as the single authoritative map of Folklore's
    ML/embedding/retrieval code. Structure it as prose (with a module table and an ASCII pipeline
    diagram where they aid clarity):
    1. Layering overview: where retrieval sits in the domain → application → infrastructure layering.
    2. Embedding layer: embedders.ts (ONNX dense vectors via @xenova/transformers), vectors.ts (domain
       vector helpers), vector-index.ts (sqlite-vec ANN store + KNN query). State the 384-dim ONNX default
       and the Wave-2 nomic-embed dense option.
    3. Hybrid retrieval pipeline: describe the flow dense (vector-index) + lexical (BM25) → RRF fusion
       (cite the domain RRF/multi-rrf module by its confirmed filename) → optional rerank
       (cross-encoder.ts / llm-listwise-rerank.ts) → optional graph re-rank (pagerank/PPR). Show the
       pipeline as an arrow diagram. Note the rust-retrieval.ts accelerated path (folklore-rs).
    4. Rerank tiering: hw-detect.ts picks gpu>accelerated>cpu>minimal and downgrades the reranker
       when no GPU/Ollama is present.
    5. Measured behavior: cite the Wave-2 72.30% NDCG@10 SciFact result, the Wave-3 reranker regression
       (-1.92, avoid on scientific text), and the Wave-4 routing-gate null result, each as a one-liner
       so future contributors do not re-add the regressing pieces.
    6. A module table: file path | layer | role — covering every module listed in <facts> (only cite
       files you confirmed exist via read_first).
    Every file path cited MUST exist on disk. Do NOT cite a path you did not confirm.
  </action>
  <verify>
    <automated>test -f docs/architecture/RETRIEVAL-MODULES.md && for f in $(grep -oE 'src/[a-zA-Z0-9/_-]+\.ts' docs/architecture/RETRIEVAL-MODULES.md | sort -u); do test -f "$f" || { echo "STALE PATH: $f"; exit 1; }; done && grep -q 'embedders.ts' docs/architecture/RETRIEVAL-MODULES.md && grep -q 'RRF' docs/architecture/RETRIEVAL-MODULES.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - docs/architecture/RETRIEVAL-MODULES.md exists and is at least 60 lines.
    - Every `src/...ts` path it cites resolves to a real file on disk (the verify loop exits 0 — no STALE PATH).
    - It names embedders.ts, vector-index.ts, cross-encoder.ts, and rust-retrieval.ts (`grep -c` each >= 1).
    - It contains the strings `RRF` and `72.30` (the hybrid fusion + measured SciFact number).
    - It contains a section describing the rerank tiering via `hw-detect`.
  </acceptance_criteria>
  <done>A single authoritative document maps the embedding + hybrid-retrieval modules to their layer and role, shows the pipeline flow, and records the measured ceilings/regressions — all citing only files that exist.</done>
</task>

<task type="auto">
  <name>Task 2: Add an in-tree index pointing at the module map</name>
  <read_first>
    - docs/architecture/RETRIEVAL-MODULES.md (the doc created in Task 1 — link to it accurately)
    - run `ls src/infrastructure/` to enumerate what the README should briefly group
  </read_first>
  <files>src/infrastructure/README.md</files>
  <action>
    Create src/infrastructure/README.md as a short in-tree index for the infrastructure layer. It should:
    - State that this directory is the IO/adapter layer of the functional-DDD architecture.
    - Group the modules into clusters: retrieval/embedding (embedders, vector-index, cross-encoder,
      llm-listwise-rerank, rust-retrieval, search-sync, search-gossip, hw-detect), P2P/federation
      (peer-transport, peer-store, share-sync, ydoc-store, bandwidth-limiter, connection-health, oracle-gossip,
      touch-protocol, recall-sync), code-graph (code-graph, tree-sitter-parser), and storage/misc
      (graph-repository, atomic-write, config-loader, log-store, sessions-state). Confirm names via `ls`.
    - For the retrieval/embedding cluster, link to `../../docs/architecture/RETRIEVAL-MODULES.md` as the
      authoritative deep-dive.
    Keep it concise (roughly 30-70 lines). This README is documentation only — it is excluded from tsc
    (tsconfig include is src/**/* but README.md is not a .ts file, so it has zero build impact).
  </action>
  <verify>
    <automated>test -f src/infrastructure/README.md && grep -q 'RETRIEVAL-MODULES' src/infrastructure/README.md && npx tsc --noEmit && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - src/infrastructure/README.md exists.
    - It links to `RETRIEVAL-MODULES.md` (`grep -q RETRIEVAL-MODULES` succeeds).
    - It names the four clusters (retrieval/embedding, P2P/federation, code-graph, storage).
    - `npx tsc --noEmit` still exits 0 (the README did not break the build).
  </acceptance_criteria>
  <done>src/infrastructure/README.md indexes the infrastructure modules by cluster and points the retrieval cluster at the authoritative module map; the build is unaffected.</done>
</task>

</tasks>

<verification>
- Both docs exist: `test -f docs/architecture/RETRIEVAL-MODULES.md && test -f src/infrastructure/README.md`.
- No stale paths: every `src/*.ts` cited in RETRIEVAL-MODULES.md resolves to a real file.
- No source/import changes: `git status --porcelain` shows only the two new .md files added (plus this plan's docs) — no .ts file modified.
- Build still green: `npx tsc --noEmit` exits 0.
</verification>

<success_criteria>
The ML/embedding/retrieval code has a documented module layout: an authoritative map (docs/architecture/RETRIEVAL-MODULES.md) describing embedders + the hybrid dense/lexical/RRF/rerank pipeline with measured ceilings, indexed in-tree by src/infrastructure/README.md — with zero source moves and a green build. CLEAN-04 satisfied.
</success_criteria>

<output>
After completion, create `.planning/phases/25-cleanup-repo-restructure/25-02-SUMMARY.md`.
</output>
