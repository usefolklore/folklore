---
phase: 25-cleanup-repo-restructure
plan: 05
type: execute
wave: 3
depends_on: ["25-01", "25-02", "25-03", "25-04"]
files_modified:
  - .planning/phases/25-cleanup-repo-restructure/25-VALIDATION.md
autonomous: true
requirements: [REPO-02]
must_haves:
  truths:
    - "npm run build (tsc) succeeds with zero errors after the full phase 25 cleanup + restructure"
    - "npm run lint succeeds (eslint over src + tests)"
    - "The full test suite passes with the same pass count as the pre-phase baseline — zero regressions"
    - "A grep over the config surface (CLAUDE.md + .claude config + .mcp.json) confirms no live ruflo/claude-flow/hive-mind references remain"
    - "The folk-pop site under site/ is byte-identical to before the phase (untouched)"
  artifacts:
    - path: ".planning/phases/25-cleanup-repo-restructure/25-VALIDATION.md"
      provides: "Recorded build + lint + test results proving zero regressions, with the cruft-grep evidence"
      contains: "pass"
  key_links:
    - from: "25-VALIDATION.md"
      to: "npm test"
      via: "recorded full-suite pass count"
      pattern: "pass"
---

<objective>
Final validation gate for Phase 25: prove the build is green, lint is clean, the full test suite passes with zero regressions versus baseline, the config surface is free of inherited cruft, and the site is untouched — then record the evidence.

Purpose: REPO-02 is the zero-regression contract for the whole phase. Plans 25-01..25-04 each kept changes scoped and low-risk, but only an end-to-end build + lint + full-test run proves the cleanup and restructure introduced no regressions. This gate runs LAST, after all other plans land, and is the phase's go/no-go.
Output: .planning/phases/25-cleanup-repo-restructure/25-VALIDATION.md with build/lint/test results + the cruft-grep evidence.

This plan is verification-only. It makes NO source changes. If any check fails, it documents the failure and the executor must surface it (do not paper over a regression).
</objective>

<execution_context>
@/Users/saharbarak/.claude/get-shit-done/workflows/execute-plan.md
@/Users/saharbarak/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/25-cleanup-repo-restructure/25-CONTEXT.md
@.planning/phases/25-cleanup-repo-restructure/25-01-SUMMARY.md
@.planning/phases/25-cleanup-repo-restructure/25-03-SUMMARY.md
@.planning/phases/25-cleanup-repo-restructure/25-04-SUMMARY.md

<facts>
<!-- Baseline + commands. -->

Baseline (measured at planning time, 2026-06-15): `npx tsc --noEmit` exits 0 (build green).
The test suite is run by `npm test` = `node --import tsx --test tests/*.test.ts`. tests/ has 85 *.test.ts
  files, ~938 test cases. STATE.md records 313/313 at v2.0 close; the planning brief cites ~942 tests now.
  The authoritative baseline is whatever `npm test` reports BEFORE any phase-25 change — capture it first.
Build command: `npm run build` (= `tsc`). Lint: `npm run lint` (= `eslint src tests`).
Config-surface cruft grep (must return nothing live): scope to CLAUDE.md + .claude/settings.json + .mcp.json
  + .claude/README.md. Do NOT grep src/ (Folklore's own swarm-sim legitimately uses the word "swarm").
Site integrity: site/ must be byte-identical. `git status --porcelain site/` must be empty (no site file changed
  by any phase-25 plan).

How to read npm test pass/fail: node --test prints summary lines `# pass N`, `# fail N`, `# tests N`.
  Capture them from stdout.
</facts>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run build + lint + full test suite and capture results</name>
  <read_first>
    - package.json (confirm the build/lint/test script bodies)
    - .planning/STATE.md (the recorded prior test baseline for comparison)
  </read_first>
  <files>.planning/phases/25-cleanup-repo-restructure/25-VALIDATION.md</files>
  <action>
    Run, in order, capturing each command's exit code and output tail:
    1. `npm run build` — must exit 0. Capture any tsc error.
    2. `npm run lint` — must exit 0. Capture any eslint error.
    3. `npm test` — capture the node:test summary lines (`# pass`, `# fail`, `# tests`). The run must show
       `# fail 0`.
    Write .planning/phases/25-cleanup-repo-restructure/25-VALIDATION.md recording:
    - The three commands, their exit codes, and the test pass/fail/total counts.
    - A PASS/FAIL verdict line. The phase passes ONLY if build=0, lint=0, and `# fail 0`.
    If any command fails, record the exact failure (file + error) in the doc and mark the verdict FAIL — do
    not attempt to fix source here (that is a gap-closure follow-up); surface it clearly.
  </action>
  <verify>
    <automated>npm run build && npm run lint && npm test 2>&1 | tee /tmp/p25-test.out | tail -20; grep -qE '# fail 0|tests .* 0 failures|fail 0' /tmp/p25-test.out && test -f .planning/phases/25-cleanup-repo-restructure/25-VALIDATION.md && echo GATE_OK</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0 (recorded in 25-VALIDATION.md).
    - `npm run lint` exits 0 (recorded).
    - `npm test` reports `# fail 0` (zero failures) — recorded with the actual `# pass` / `# tests` counts.
    - 25-VALIDATION.md contains a single explicit verdict line (PASS or FAIL) consistent with the captured results.
  </acceptance_criteria>
  <done>Build, lint, and the full test suite all pass with zero failures, and the results are recorded with concrete counts in 25-VALIDATION.md.</done>
</task>

<task type="auto">
  <name>Task 2: Verify config-surface cruft is gone and the site is untouched</name>
  <read_first>
    - CLAUDE.md, .claude/settings.json, .mcp.json, .claude/README.md (the cleaned config surface from 25-01)
    - .planning/phases/25-cleanup-repo-restructure/25-VALIDATION.md (append to it)
  </read_first>
  <files>.planning/phases/25-cleanup-repo-restructure/25-VALIDATION.md</files>
  <action>
    Run the cruft grep scoped to the config surface (NOT src/):
      `grep -niE 'ruflo|claude-flow|claude_flow|RuFlo|hive-mind|hive_mind|hierarchical-mesh|agent.?booster|3-tier model|agent teams|ruv@ruv' CLAUDE.md .claude/settings.json .mcp.json .claude/README.md`
    It must return NOTHING (the single allowed exception: .claude/README.md may contain one sentence stating
    these were removed — if grep flags only that one line in README.md, that is acceptable; note it).
    Verify both config JSON files still parse: `node -e "JSON.parse(...)"` for settings.json and .mcp.json.
    Verify the site is byte-intact: `git status --porcelain site/` must be empty.
    Verify bench/ exists with runners (25-03) and the new docs exist (25-02, 25-04):
      `test -d bench && test -f docs/architecture/RETRIEVAL-MODULES.md && test -f docs/architecture/REPO-LAYOUT.md && test -f docs/REPO-SPLIT.md`.
    Append a "Cruft + integrity checks" section to 25-VALIDATION.md recording each check and its result.
  </action>
  <verify>
    <automated>node -e "JSON.parse(require('fs').readFileSync('.claude/settings.json','utf8')); JSON.parse(require('fs').readFileSync('.mcp.json','utf8'))" && test -z "$(git status --porcelain site/)" && test -d bench && test -f docs/architecture/RETRIEVAL-MODULES.md && test -f docs/architecture/REPO-LAYOUT.md && test -f docs/REPO-SPLIT.md && test -z "$(grep -niE 'ruflo|claude-flow|hive-mind|hierarchical-mesh' CLAUDE.md .claude/settings.json .mcp.json)" && echo INTEGRITY_OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -niE 'ruflo|claude-flow|hive-mind|hierarchical-mesh' CLAUDE.md .claude/settings.json .mcp.json` returns nothing.
    - `.claude/settings.json` and `.mcp.json` both parse as valid JSON.
    - `git status --porcelain site/` is empty (site untouched).
    - bench/ exists and the three new architecture/split docs exist.
    - 25-VALIDATION.md has a "Cruft + integrity checks" section recording all of the above.
  </acceptance_criteria>
  <done>The config surface is verified cruft-free, both config JSON files are valid, the site is byte-intact, and the phase's new structure (bench/ + docs) is present — all recorded in 25-VALIDATION.md.</done>
</task>

</tasks>

<verification>
- `npm run build` exits 0; `npm run lint` exits 0; `npm test` reports `# fail 0`.
- Config-surface cruft grep returns nothing live.
- site/ is byte-identical (git porcelain empty).
- 25-VALIDATION.md exists with build/lint/test counts + cruft/integrity evidence + a PASS verdict.
</verification>

<success_criteria>
After the full Phase 25 cleanup + restructure, `npm run build` succeeds, lint passes, and the full test suite passes with zero regressions versus baseline; the config surface carries no inherited ruflo/claude-flow/hive-mind references; and the folk-pop site is untouched — all proven and recorded in 25-VALIDATION.md. REPO-02 satisfied.
</success_criteria>

<output>
After completion, create `.planning/phases/25-cleanup-repo-restructure/25-05-SUMMARY.md` linking to 25-VALIDATION.md and stating the final PASS/FAIL verdict for the phase.
</output>
