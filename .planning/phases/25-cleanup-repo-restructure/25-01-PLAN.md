---
phase: 25-cleanup-repo-restructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - CLAUDE.md
  - .claude/settings.json
  - .mcp.json
  - .claude/README.md
autonomous: true
requirements: [CLEAN-01, CLEAN-02, CLEAN-03, CLEAN-06]
must_haves:
  truths:
    - "A grep for ruflo/claude-flow/RuFlo/hive-mind over CLAUDE.md, .claude/settings.json and .mcp.json returns no live config references"
    - "CLAUDE.md contains only Folklore's own config (the folklore:start block) plus a short Folklore project header"
    - "The Claude Code hooks that Folklore actually ships (smart-hook, mcp-pre, post-fetch, prompt-submit, session-start/capture) are documented and still wired in settings.json"
    - "The claude-flow MCP server is removed from .mcp.json"
    - "The claude-flow 3-tier model-routing block is removed from CLAUDE.md; Folklore's real rerank-tier mechanism (hw-detect) is documented in its place"
  artifacts:
    - path: "CLAUDE.md"
      provides: "Folklore-only project instructions"
      contains: "<!-- folklore:start -->"
    - path: ".claude/settings.json"
      provides: "Folklore hook wiring with no claude-flow config"
    - path: ".mcp.json"
      provides: "mcpServers block free of claude-flow"
    - path: ".claude/README.md"
      provides: "Documentation of Folklore's own hooks"
      contains: "folklore-smart-hook"
  key_links:
    - from: ".claude/settings.json"
      to: ".claude/hooks/folklore-smart-hook.sh"
      via: "PreToolUse command path"
      pattern: "folklore-smart-hook"
    - from: ".claude/settings.json"
      to: ".claude/helpers/ak-statusline.cjs"
      via: "statusLine command path"
      pattern: "ak-statusline"
---

<objective>
Strip all inherited ruflo / claude-flow / RuFlo-V3 / swarm-orchestration / hive-mind / 3-tier-model-routing cruft out of the Claude Code configuration surface (CLAUDE.md, .claude/settings.json, .mcp.json), keep ONLY Folklore's own hooks and config, and document those hooks.

Purpose: The repo is going public under the usefolklore org. The CLAUDE.md and .claude config currently advertise a foreign agent framework (claude-flow V3) that Folklore does not use, including a leaked attribution co-author and a broken statusLine reference. This is the single most visible piece of inherited cruft.
Output: A Folklore-only CLAUDE.md, a settings.json wired only to Folklore's hooks with a working statusLine, a claude-flow-free .mcp.json, and a new .claude/README.md documenting the shipped hooks.

NOTE on scope: The string "swarm" appears legitimately in `src/` — it is Folklore's OWN P2P swarm-sim feature (virtual peer responders), NOT claude-flow. Do NOT touch any file under `src/`. This plan only edits the three config files plus a new doc. The grep verification below is deliberately scoped to config files so it does not flag Folklore's legitimate swarm-sim code.
</objective>

<execution_context>
@/Users/saharbarak/.claude/get-shit-done/workflows/execute-plan.md
@/Users/saharbarak/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-cleanup-repo-restructure/25-CONTEXT.md

<facts>
<!-- Discovered during planning. Use these directly — no exploration needed. -->

CLAUDE.md structure (358 lines total):
- Lines 1-247: claude-flow / RuFlo V3 cruft. Header "# Claude Code Configuration - RuFlo V3",
  sections: Behavioral Rules, File Organization, Project Architecture, Project Config,
  Build & Test, Concurrency, Swarm Orchestration, 3-Tier Model Routing (ADR-026),
  Swarm Configuration & Anti-Drift, Swarm Execution Rules, V3 CLI Commands, Available Agents,
  Memory & Vector Search, Key MCP Tools (314 available), Quick Setup, Claude Code vs MCP Tools, Support.
  ALL of this is claude-flow cruft and must be deleted.
- Line 248: `<!-- folklore:start -->` — Folklore's OWN content begins here.
- Lines 248-358: the real Folklore project doc (network-before-web, system rooms, freshness rule,
  "When to invoke folklore"). KEEP this block verbatim.

Some Behavioral Rules in the cruft block (lines 3-12) are generic-good (DDD, no secrets in commits,
read-before-edit). Do NOT preserve them here — the authoritative Folklore conventions live in
.planning/PROJECT.md + STATE.md "Architecture invariants". A short pointer is enough.

.claude/settings.json cruft to remove:
- `permissions.allow`: the four claude-flow entries `Bash(npx @claude-flow*)`, `Bash(npx claude-flow*)`,
  `Bash(node .claude/*)`, `mcp__claude-flow__:*`. Keep the `deny` block (.env reads).
- `attribution`: the whole block (claude-flow co-author + PR footer) — DELETE.
- `env`: `CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS`, `CLAUDE_FLOW_V3_ENABLED`, `CLAUDE_FLOW_HOOKS_ENABLED` — DELETE the env block.
- `claudeFlow`: the entire top-level block (lines 91-194: version, agentTeams, swarm, memory, neural,
  daemon, learning, adr, ddd, security) — DELETE.
- The PreToolUse matcher `mcp__folklore__.*` → folklore-mcp-pre.cjs is FOLKLORE'S OWN. KEEP.
- `statusLine.command` references `statusline.cjs` and `wi-statusline.cjs` which DO NOT EXIST on disk.
  The only helper present is `.claude/helpers/ak-statusline.cjs`. FIX statusLine to call ak-statusline.cjs.

.mcp.json: contains a single mcpServers entry "claude-flow" → DELETE it, leaving `{ "mcpServers": {} }`.
(Folklore's own MCP server is registered separately via the plugin, not via this file.)

Folklore's own hooks present in .claude/hooks/ (KEEP and document):
  folklore-hook.sh, folklore-mcp-pre.cjs, folklore-post-edit.sh, folklore-post-fetch.cjs,
  folklore-post-fetch.sh, folklore-prompt-submit.cjs, folklore-session-capture.sh,
  folklore-session-start.sh, folklore-smart-hook.cjs, folklore-smart-hook.sh.
Helper present: .claude/helpers/ak-statusline.cjs.
Skill present: .claude/skills/folklore/SKILL.md.

The claude-flow "3-Tier Model Routing (ADR-026)" in CLAUDE.md is config cruft (Agent Booster / Haiku /
Sonnet routing). Folklore's REAL tiering is the rerank hardware-tier picker in
src/infrastructure/hw-detect.ts (gpu > accelerated > cpu > minimal). CLEAN-06 is satisfied by removing
the claude-flow routing text and documenting the hw-detect rerank-tier mechanism in the new .claude/README.md.

NOTE (out of scope, surface to user only): .claude/settings.local.json is NOT git-tracked and contains
a leaked HCLOUD_TOKEN plus enabledMcpjsonServers:["claude-flow"]. Do NOT edit it in this plan (untracked).
Mention it in the SUMMARY as a user follow-up (rotate the token; the file is gitignored so it will not ship).
</facts>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace CLAUDE.md with a Folklore-only document</name>
  <read_first>
    - CLAUDE.md (read the whole file; confirm `<!-- folklore:start -->` is at line 248 and the cruft is 1-247)
    - .planning/PROJECT.md (for the one-line project framing + conventions pointer)
  </read_first>
  <files>CLAUDE.md</files>
  <action>
    Rewrite CLAUDE.md so it contains, in order:
    1. A short header replacing "# Claude Code Configuration - RuFlo V3" with:
       `# Folklore — Claude Code Project Guide`
       followed by 2-4 prose sentences: Folklore is a knowledge-graph-first, network-before-web
       research layer with P2P federation; this file tells Claude Code how the project's own hooks
       behave. For coding conventions (functional DDD, neverthrow Results, ESM, max 3 deps/phase,
       no claude/anthropic git co-authors) point to `.planning/PROJECT.md` and `.planning/STATE.md`
       "Architecture invariants". Do NOT reproduce the claude-flow swarm/agent/MCP-tool sections.
    2. The ENTIRE existing Folklore block from `<!-- folklore:start -->` (line 248) to end of file,
       preserved byte-for-byte (the network-before-web description, System rooms, Freshness rule,
       "When to invoke folklore"). Keep the `<!-- folklore:start -->` marker.
    Delete everything that was between the old line 1 and line 247.
    Do NOT introduce any new mention of ruflo, claude-flow, RuFlo, swarm orchestration, hive-mind,
    agent teams, "3-Tier Model Routing", "Agent Booster", or "314 MCP tools".
  </action>
  <verify>
    <automated>test -z "$(grep -niE 'ruflo|claude-flow|claude_flow|RuFlo|hive-mind|hive_mind|agent.?booster|3-tier model|agent teams|314 available|swarm orchestration' CLAUDE.md)" && grep -q '<!-- folklore:start -->' CLAUDE.md && echo CLEAN</automated>
  </verify>
  <acceptance_criteria>
    - `grep -niE 'ruflo|claude-flow|RuFlo|hive-mind|agent.?booster|3-tier model|agent teams|314 available|swarm orchestration' CLAUDE.md` returns nothing.
    - `grep -q '<!-- folklore:start -->' CLAUDE.md` succeeds (the Folklore block is intact).
    - `grep -c 'network-before-web' CLAUDE.md` returns at least 1.
    - First line of CLAUDE.md is `# Folklore — Claude Code Project Guide`.
  </acceptance_criteria>
  <done>CLAUDE.md is a Folklore-only document: a short Folklore header + a conventions pointer, then the preserved folklore:start block. No claude-flow/ruflo/swarm/hive-mind/3-tier text remains.</done>
</task>

<task type="auto">
  <name>Task 2: Strip claude-flow from settings.json + .mcp.json and fix the statusLine</name>
  <read_first>
    - .claude/settings.json (the full file — note the `claudeFlow` block lines 91-194, `attribution`, `env`, and the four claude-flow `permissions.allow` entries)
    - .mcp.json (single claude-flow mcpServers entry)
    - .claude/helpers/ak-statusline.cjs (confirm this is the only statusline helper present)
  </read_first>
  <files>.claude/settings.json, .mcp.json</files>
  <action>
    In `.claude/settings.json`:
    - Remove from `permissions.allow` the four claude-flow entries: `Bash(npx @claude-flow*)`,
      `Bash(npx claude-flow*)`, `Bash(node .claude/*)`, `mcp__claude-flow__:*`. Leave `permissions.allow`
      as an empty array `[]` if nothing else remains; keep the `permissions.deny` block (.env reads) unchanged.
    - Delete the entire `attribution` block (claude-flow co-author + PR footer).
    - Delete the entire `env` block (CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS, CLAUDE_FLOW_V3_ENABLED,
      CLAUDE_FLOW_HOOKS_ENABLED).
    - Delete the entire top-level `claudeFlow` block.
    - Keep the full `hooks` block intact (all entries point to folklore-* hooks — these are Folklore's own).
    - Fix `statusLine.command` so it calls ONLY the helper that exists. Replace the body with:
      `sh -c 'node "${CLAUDE_PROJECT_DIR:-.}/.claude/helpers/ak-statusline.cjs" 2>/dev/null'`
      (drop the non-existent statusline.cjs and wi-statusline.cjs references).
    - Ensure the file remains valid JSON.
    In `.mcp.json`:
    - Remove the `claude-flow` server, leaving `{ "mcpServers": {} }` as valid JSON.
  </action>
  <verify>
    <automated>node -e "JSON.parse(require('fs').readFileSync('.claude/settings.json','utf8')); JSON.parse(require('fs').readFileSync('.mcp.json','utf8'))" && test -z "$(grep -niE 'claude-flow|claude_flow|claudeFlow|agent_teams|hierarchical-mesh|ruv@ruv|AGENT_TEAMS' .claude/settings.json .mcp.json)" && grep -q 'ak-statusline.cjs' .claude/settings.json && echo CLEAN</automated>
  </verify>
  <acceptance_criteria>
    - Both files parse as valid JSON (`node -e "JSON.parse(...)"` exits 0).
    - `grep -niE 'claude-flow|claudeFlow|agent_teams|hierarchical-mesh|ruv@ruv|CLAUDE_FLOW' .claude/settings.json .mcp.json` returns nothing.
    - `.mcp.json` `mcpServers` is an empty object.
    - `settings.json` `hooks.PreToolUse` still contains both folklore-smart-hook and folklore-mcp-pre entries (`grep -c folklore-smart-hook .claude/settings.json` >= 1 and `grep -c folklore-mcp-pre .claude/settings.json` >= 1).
    - `settings.json` `statusLine.command` references `ak-statusline.cjs` and does NOT reference `wi-statusline` or a bare `statusline.cjs`.
  </acceptance_criteria>
  <done>settings.json and .mcp.json carry zero claude-flow config; Folklore's hooks remain wired; the statusLine points to the helper that actually exists.</done>
</task>

<task type="auto">
  <name>Task 3: Document Folklore's own hooks in .claude/README.md (CLEAN-02 + CLEAN-06 doc)</name>
  <read_first>
    - .claude/settings.json (post-edit — to describe the live wiring accurately)
    - .claude/hooks/folklore-smart-hook.sh and .claude/hooks/folklore-smart-hook.cjs (the deny-on-confidence PreToolUse gate — read the top doc comment)
    - .claude/hooks/folklore-post-fetch.cjs (the PostToolUse auto-save)
    - .claude/hooks/folklore-prompt-submit.cjs (top comment only)
    - src/infrastructure/hw-detect.ts (lines 1-130 — the rerank-tier picker, for the CLEAN-06 note)
  </read_first>
  <files>.claude/README.md</files>
  <action>
    Create `.claude/README.md` documenting the Folklore Claude Code integration. Include, as prose with
    a small table where it aids clarity:
    - A one-paragraph intro: these are Folklore's OWN hooks; nothing in this directory is claude-flow.
    - A table mapping each shipped hook file to (event, what it does): folklore-smart-hook.{sh,cjs}
      = PreToolUse deny-on-confidence graph gate; folklore-mcp-pre.cjs = PreToolUse for mcp__folklore__*;
      folklore-post-fetch.{sh,cjs} = PostToolUse auto-save of WebSearch/WebFetch into the graph;
      folklore-prompt-submit.cjs = UserPromptSubmit context injection; folklore-session-start.sh +
      folklore-hook.sh = SessionStart; folklore-session-capture.sh = session capture;
      folklore-post-edit.sh = post-edit helper. Note the statusLine helper .claude/helpers/ak-statusline.cjs
      and the skill .claude/skills/folklore/SKILL.md.
    - A short "Model tiering (CLEAN-06)" section: Folklore does NOT use a claude-flow 3-tier model router.
      The only tiering in Folklore is the rerank HARDWARE-tier picker in src/infrastructure/hw-detect.ts
      (gpu > accelerated > cpu > minimal), which downgrades the reranker when no GPU/Ollama is available.
      State that the removed claude-flow "3-Tier Model Routing (ADR-026)" config was unused and has been deleted.
    Keep it concise (roughly 40-90 lines). Do NOT mention claude-flow/ruflo except in the single sentence
    that says they were removed.
  </action>
  <verify>
    <automated>test -f .claude/README.md && grep -q 'folklore-smart-hook' .claude/README.md && grep -q 'hw-detect' .claude/README.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `.claude/README.md` exists.
    - It names at least these hooks: folklore-smart-hook, folklore-mcp-pre, folklore-post-fetch, folklore-prompt-submit, folklore-session-start (`grep -c` each >= 1).
    - It contains a model-tiering section referencing `hw-detect` (CLEAN-06).
    - `grep -ciE 'claude-flow|ruflo' .claude/README.md` is at most 1 (only the single "these were removed" sentence).
  </acceptance_criteria>
  <done>.claude/README.md documents every Folklore-owned hook + the statusLine helper + the skill, and records that the claude-flow 3-tier router was removed in favor of the hw-detect rerank tiering.</done>
</task>

</tasks>

<verification>
- `grep -riE 'ruflo|claude-flow|RuFlo|hive-mind|agent.?booster|3-tier model|hierarchical-mesh' CLAUDE.md .claude/settings.json .mcp.json` returns no matches (config surface is clean).
- `node -e "JSON.parse(require('fs').readFileSync('.claude/settings.json','utf8'))"` and the same for `.mcp.json` both exit 0 (valid JSON).
- Folklore's hooks are still wired: `grep -q folklore-smart-hook .claude/settings.json` and `grep -q folklore-mcp-pre .claude/settings.json` both succeed.
- `.claude/README.md` exists and documents the hooks.
- No file under `src/` was modified (Folklore's swarm-sim untouched): `git status --porcelain src/` is empty.
</verification>

<success_criteria>
CLAUDE.md, .claude/settings.json, and .mcp.json contain only Folklore's own configuration; the claude-flow MCP server, attribution, env vars, claudeFlow block, swarm/hive-mind sections, and 3-tier model router are all gone; the statusLine points to the helper that exists; and Folklore's hooks are documented in .claude/README.md. CLEAN-01, CLEAN-02, CLEAN-03, CLEAN-06 satisfied.
</success_criteria>

<output>
After completion, create `.planning/phases/25-cleanup-repo-restructure/25-01-SUMMARY.md`. In it, surface the untracked `.claude/settings.local.json` finding (leaked HCLOUD_TOKEN + claude-flow enabledMcpjsonServers) as a user follow-up — the file is gitignored so it will not ship, but the token should be rotated.
</output>
