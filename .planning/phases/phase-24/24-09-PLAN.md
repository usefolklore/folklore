---
phase: phase-24
plan: 09
type: execute
wave: 3
depends_on: [24-02, 24-03, 24-04, 24-06, 24-07, 24-08]
files_modified:
  - src/cli/commands/ask.ts
  - src/cli/commands/recall.ts
  - src/cli/commands/discover.ts
  - src/cli/commands/report.ts
  - src/cli/commands/viz.ts
  - src/cli/commands/save.ts
  - src/cli/commands/touch.ts
  - src/cli/commands/trigger.ts
  - src/cli/commands/index-project.ts
  - src/cli/commands/this.ts
  - src/cli/commands/init.ts
  - src/cli/commands/onboard.ts
  - src/cli/commands/lint.ts
  - src/cli/commands/export-obsidian.ts
  - src/cli/commands/discover-loop.ts
  - src/cli/commands/dashboard.ts
  - src/cli/commands/consolidate.ts
  - src/cli/commands/recent-sessions.ts
  - src/cli/commands/sessions.ts
  - src/application/ask.ts
  - src/application/recall.ts
  - src/application/discover.ts
  - src/application/discovery-loop.ts
  - src/application/ingest.ts
  - src/application/session-ingest.ts
  - src/application/federated-search.ts
  - src/application/use-cases.ts
  - src/domain/subject-key.ts
  - src/domain/recency-rerank.ts
  - src/domain/sources.ts
  - src/domain/sharing.ts
  - src/domain/save-note.ts
  - src/domain/share-envelope.ts
autonomous: true
requirements:
  - ROOMS-DEL-01
  - ROOMS-DEL-02
  - ROOMS-DEL-04
  - ROOMS-DEL-07
must_haves:
  truths:
    - "All CLI write-side commands (save, touch, trigger, etc.) accept --private and reject --room"
    - "All CLI read-side commands (ask, recall, discover, report, viz) accept --workspace and auto-detect from cwd"
    - "subject-key.ts has no subjectFromRoom; subjectFromEntity only"
    - "recency-rerank.ts has no HALF_LIFE_BY_ROOM; uniform global half-life"
    - "application/* layers contain no Room / RoomId / defaultRoom / findRoom references"
    - "Session retrieval (recent-sessions.ts) filters by source_uri scheme, not room"
  artifacts:
    - path: "src/cli/commands/save.ts"
      provides: "save with --private flag, no --room"
    - path: "src/cli/commands/ask.ts"
      provides: "ask with --workspace flag, cwd-auto-filter"
    - path: "src/domain/subject-key.ts"
      provides: "Entity-only subject keys (no room: prefix)"
  key_links:
    - from: "ask.ts / recall.ts / discover.ts / report.ts"
      to: "detectWorkspace() helper in runtime.ts"
      via: "auto-filter at command entry"
      pattern: "detectWorkspace\\("
    - from: "save.ts"
      to: "GraphNode.private + GraphNode.workspace"
      via: "set at write time from --private flag + detectWorkspace"
      pattern: "private:\\s*(true|false)|workspace:"
---

<objective>
Wave 3a: surgical edits to ~32 files in the CLI + application + domain layers. Resolves the majority of tsc --noEmit errors from Wave 0.

**Open Question 4 RESOLVED:** Drop HALF_LIFE_BY_ROOM entirely; use uniform global half-life. Per-source-uri-scheme tuning is deferred to Phase 25+.
**Open Question 2 RESOLVED:** Oracle substrate vanishes with system rooms. Oracle Q&A becomes a source_uri filter (`oracle-question:`, `oracle-answer:` schemes) applied at query time.
**Open Question 3 RESOLVED:** federation-sim.ts gets a surgical edit here (file lives in domain/infrastructure and must compile against new types). Benchmark scoring logic deferred to Phase 25; sim compiles, just doesn't model rooms.

Purpose: most of these edits are mechanical — drop room imports, replace `nodesInRoom(graph, room)` with `graph.nodes.filter(...)`, drop --room flags, add --workspace/--private flags where appropriate. The compiler's error list from Wave 0 is the work-tracker.
Output: ~32 files updated. tsc --noEmit error count drops substantially (target: zero compile errors in this batch's files).
</objective>

<execution_context>
@/Users/saharbarak/.claude/get-shit-done/workflows/execute-plan.md
@/Users/saharbarak/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/phase-24/24-CONTEXT.md
@.planning/phases/phase-24/24-RESEARCH.md
@src/cli/runtime.ts
@src/domain/graph.ts
@/tmp/phase24-wave0-blastradius.txt
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Update read-side CLI commands (ask, recall, discover, report, viz) — add --workspace pre-filter</name>
  <files>src/cli/commands/ask.ts, src/cli/commands/recall.ts, src/cli/commands/discover.ts, src/cli/commands/report.ts, src/cli/commands/viz.ts</files>
  <read_first>
    - Each of the 5 files (entire content)
    - src/cli/runtime.ts (the detectWorkspace export from 24-04)
    - .planning/phases/phase-24/24-RESEARCH.md (lines 307-308 — read-side group G1 pattern)
    - .planning/phases/phase-24/24-RESEARCH.md (lines 459-470 — recall.ts HALF_LIFE_BY_ROOM removal)
  </read_first>
  <action>
    For each of these 5 read-side CLI commands:

    1. Drop all `defaultRoom`, `findRoom`, `RoomId`, `Room` imports — they're from deleted modules.
    2. Drop `--room` flag parsing.
    3. Add `--workspace <slug | all>` flag parsing. Semantics:
       - omitted → call detectWorkspace(process.cwd()); if returns a slug, pre-filter graph.nodes to those with `n.workspace === slug`. If returns undefined, no pre-filter.
       - `--workspace all` → no pre-filter (cross-workspace query).
       - `--workspace <slug>` → pre-filter to that slug, overriding cwd detection.
    4. The pre-filter is a `.filter(n => n.workspace === slug)` applied to the result-set after the underlying query (search/recall/etc.) returns. NOT inside the vector index — keep that simple per Wave 2 vector-index simplification (Plan 10 G5).
    5. Drop any `nodesInRoom(graph, room)` calls — replace with `graph.nodes.filter(n => n.workspace === target)` or `graph.nodes` (no filter when --workspace all).
    6. Help text updates: drop `--room`; document `--workspace`. Mention auto-detect from git toplevel.
    7. For recall.ts specifically: drop the `HALF_LIFE_BY_ROOM` map (line ~69). Replace with a single global constant `RECENCY_HALF_LIFE_DAYS = 14` (or whatever the median was — pick a middle value between 7 and 30). Open Question 4 resolved: per-source-uri tuning deferred to Phase 25+.
  </action>
  <verify>
    <automated>npx tsc --noEmit src/cli/commands/ask.ts src/cli/commands/recall.ts src/cli/commands/discover.ts src/cli/commands/report.ts src/cli/commands/viz.ts 2>&1 | grep -cE "src/cli/commands/(ask|recall|discover|report|viz)\.ts"</automated>
  </verify>
  <acceptance_criteria>
    - For each of the 5 files: grep -cE "defaultRoom|findRoom|RoomId\\b|nodesInRoom\\b|HALF_LIFE_BY_ROOM" returns 0
    - For each: grep -cE "--workspace|detectWorkspace" returns greater than 0
    - For each: grep -cE -- "--room\\b" returns 0
    - tsc --noEmit error count IN these 5 files === 0
    - recall.ts has exactly one global half-life constant: grep -cE "HALF_LIFE|RECENCY_HALF" src/cli/commands/recall.ts returns 1
  </acceptance_criteria>
  <done>Read-side CLI: workspace auto-filter active in git repos, --workspace all opts out, no room references.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Update write-side CLI commands (save, touch, trigger, index-project, this, init, onboard, lint, export-obsidian, discover-loop, dashboard, consolidate, recent-sessions, sessions)</name>
  <files>src/cli/commands/save.ts, src/cli/commands/touch.ts, src/cli/commands/trigger.ts, src/cli/commands/index-project.ts, src/cli/commands/this.ts, src/cli/commands/init.ts, src/cli/commands/onboard.ts, src/cli/commands/lint.ts, src/cli/commands/export-obsidian.ts, src/cli/commands/discover-loop.ts, src/cli/commands/dashboard.ts, src/cli/commands/consolidate.ts, src/cli/commands/recent-sessions.ts, src/cli/commands/sessions.ts</files>
  <read_first>
    - Each file (entire content)
    - src/cli/runtime.ts (detectWorkspace + new deps shape)
    - .planning/phases/phase-24/24-RESEARCH.md (lines 308 — write-side group G2 + line 621 — recent-sessions notes)
    - .planning/phases/phase-24/24-RESEARCH.md (lines 458-465 — save.ts new flag spec)
  </read_first>
  <action>
    For each write-side command:

    1. Drop `defaultRoom`, `findRoom`, `RoomId`, `Room`, `RoomsConfig` imports.
    2. Drop --room flag parsing.

    save.ts specifically (per research lines 458-465):
    3. Drop the REQUIRED --room flag at lines ~32-61.
    4. Add `--private` boolean flag (default false). When set: stamp `private: true` on the new node.
    5. Add `--workspace <slug>` optional override (skip cwd detection if set). When absent: call detectWorkspace(cwd) and stamp `workspace: <slug>` if returns a value.
    6. At node-construction time: ALWAYS set `private` (boolean), CONDITIONALLY set `workspace` (omit field if undefined).

    For touch.ts / trigger.ts:
    7. Drop the `--room` arg. The new command shape: `wellinformed touch <peer>` (or just `wellinformed touch` to touch all peers) — request the peer's freshest non-private nodes per 24-03 touch-protocol rewrite.
    8. trigger.ts: drop the per-room trigger; trigger now runs all sources flat.

    For recent-sessions.ts (per research line 621):
    9. The `nodesInRoom(graph, 'sessions')` call becomes `graph.nodes.filter(n => n.source_uri?.startsWith('claude_sessions:'))`. The "sessions" pseudo-room is gone; source_uri scheme is the filter.

    For sessions.ts (if it exists — verify):
    10. Same treatment as recent-sessions — source_uri filter.

    For lint.ts (per pitfall 11):
    11. Drop the loadSharedRooms-based consistency check. Pivot to: check private-flag consistency (e.g., no `private: undefined` on any node), workspace-tag sanity (slugs only — no whitespace/uppercase/special chars).
    12. The exact lint rules are planner discretion; keep it useful, drop it from the dispatch if utterly empty.

    For index-project.ts, this.ts, init.ts, onboard.ts, export-obsidian.ts, discover-loop.ts, dashboard.ts, consolidate.ts:
    13. Mechanical: drop room imports, drop --room flag, drop any `node.room =` assignments. Where the command's domain made sense per-room (e.g., index-project's "which room to dump this codebase into") — drop that arg. The codebase is attached to the global graph; workspace tag (if cwd is a git repo) auto-applies.

    14. init.ts / onboard.ts: drop any "create default room" boot logic. The default room concept is gone.

    Each file's help text updates accordingly.
  </action>
  <verify>
    <automated>npx tsc --noEmit src/cli/commands/save.ts src/cli/commands/touch.ts src/cli/commands/trigger.ts src/cli/commands/index-project.ts src/cli/commands/this.ts src/cli/commands/init.ts src/cli/commands/onboard.ts src/cli/commands/lint.ts src/cli/commands/export-obsidian.ts src/cli/commands/discover-loop.ts src/cli/commands/dashboard.ts src/cli/commands/consolidate.ts src/cli/commands/recent-sessions.ts src/cli/commands/sessions.ts 2>&1 | grep -cE "src/cli/commands/.*\.ts"</automated>
  </verify>
  <acceptance_criteria>
    - For each of the 14 files: grep -cE "defaultRoom|findRoom|RoomId\\b|nodesInRoom\\b" returns 0
    - save.ts: grep -c "private: " returns greater than 0 AND grep -cE -- "--private\\b" returns greater than 0
    - save.ts: grep -cE -- "--room\\b" returns 0
    - recent-sessions.ts: grep -cE "source_uri.*claude_sessions" returns greater than 0 AND grep -cE "nodesInRoom" returns 0
    - tsc --noEmit error count IN these 14 files === 0
  </acceptance_criteria>
  <done>Write-side CLI commands work on V5: --private flag wired, --workspace auto-detected, room flags removed.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Update application/* layer (8 files)</name>
  <files>src/application/ask.ts, src/application/recall.ts, src/application/discover.ts, src/application/discovery-loop.ts, src/application/ingest.ts, src/application/session-ingest.ts, src/application/federated-search.ts, src/application/use-cases.ts</files>
  <read_first>
    - Each file (entire content)
    - .planning/phases/phase-24/24-RESEARCH.md (lines 309 — application group G3)
    - .planning/phases/phase-24/24-RESEARCH.md (line 621 — session-ingest's ensureSessionsRoom becomes no-op)
  </read_first>
  <action>
    For each application-layer file:

    1. Drop all room imports (RoomId, Room, defaultRoom, findRoom, slugifyRoomName, system-rooms.*).
    2. Drop the `room` parameter from public function signatures. Where the function previously took `(deps, room, ...)`, simplify to `(deps, ...)`.
    3. Replace `nodesInRoom(graph, room)` with `graph.nodes` (or with a workspace filter passed FROM the caller, not derived here — application layer is workspace-agnostic; CLI layer applies the workspace filter).
    4. Inside ask.ts / recall.ts / discover.ts: results are now flat across the whole graph. The workspace filter (when present) is applied UPSTREAM at the CLI command boundary in Task 1, then the application layer operates on the pre-filtered subset.

    session-ingest.ts specifically:
    5. Drop `ensureSessionsRoom`. The session adapter still emits nodes with source_uri prefix `claude_sessions:` — that's the existing contract from Phase 20. The "sessions room" pinning was orthogonal and is gone.

    federated-search.ts:
    6. Already touched in 24-03 (PeerMatch.room dropped). Verify no residual room references. The fan-out routing no longer groups by room.

    ingest.ts:
    7. Drop any "tag every ingested node with room X" logic. The new ingest tags `workspace` only if the source adapter provides one (most don't — workspace is a write-time-from-cwd concept, not a source-derived one).

    use-cases.ts:
    8. If this file aggregates use-case helpers (verify by reading), it likely re-exports a set of functions whose signatures changed. Mechanical fix: drop room params from re-exports.
  </action>
  <verify>
    <automated>npx tsc --noEmit src/application/ask.ts src/application/recall.ts src/application/discover.ts src/application/discovery-loop.ts src/application/ingest.ts src/application/session-ingest.ts src/application/federated-search.ts src/application/use-cases.ts 2>&1 | grep -cE "src/application/.*\.ts"</automated>
  </verify>
  <acceptance_criteria>
    - For each of the 8 files: grep -cE "defaultRoom|findRoom|RoomId\\b|nodesInRoom\\b|ensureSessionsRoom" returns 0
    - For each: grep -cE "from.*rooms\.js|from.*system-rooms|from.*rooms-config" returns 0
    - tsc --noEmit error count IN these 8 files === 0
  </acceptance_criteria>
  <done>Application layer is workspace-agnostic; room signatures gone; CLI is the only layer that applies workspace filter.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Update domain/* layer (subject-key, recency-rerank, sources, sharing, save-note, share-envelope)</name>
  <files>src/domain/subject-key.ts, src/domain/recency-rerank.ts, src/domain/sources.ts, src/domain/sharing.ts, src/domain/save-note.ts, src/domain/share-envelope.ts</files>
  <read_first>
    - Each file (entire content)
    - .planning/phases/phase-24/24-RESEARCH.md (lines 310 — domain group G4)
    - .planning/phases/phase-24/24-RESEARCH.md (lines 399-405 — subject-key.ts spec)
    - .planning/phases/phase-24/24-RESEARCH.md (lines 467-471 — recency-rerank decision)
    - docs/p2p/peer-reputation-design.md (read the entity:* vs room:* section to inform subject-key.ts changes)
  </read_first>
  <action>
    subject-key.ts (per research lines 399-405):
    1. Drop `subjectFromRoom` function and its callers within this file.
    2. Drop `kind: 'room'` from the SubjectKey union — leaves only `kind: 'entity'`.
    3. Simplify `subjectsForMatch` to return only entity-derived subjects. If a chunk has zero entity mentions: return an empty list (no fallback). This is the "lossy but correct" flatten — peer reputation accrues on entities, not on a partition primitive.
    4. Update any prefix string `'room:'` to no longer exist as a recognized scheme.

    recency-rerank.ts (per research lines 467-471):
    5. Drop `HALF_LIFE_BY_ROOM` map.
    6. Drop `halfLifeForRoom` function.
    7. Add a single `DEFAULT_HALF_LIFE_DAYS = 14` constant (or median value; planner discretion within 7-30 range).
    8. The rerank function signature loses its `room` param.

    sources.ts:
    9. Drop the `forRoom` helper.
    10. Drop the `room` field from source descriptor type (if present).
    11. Source adapters register against the global graph, no room.

    sharing.ts:
    12. Drop `auditRoom` and any related room-scoped audit helpers.
    13. The remaining audit functions operate on the global graph.

    save-note.ts:
    14. Drop the `room` field from SaveNoteRequest / SaveNoteResult types.
    15. Add `private: boolean` (default false) and `workspace?: string` (optional) to SaveNoteRequest.
    16. The factory function that builds a GraphNode from a SaveNoteRequest: set `private` (boolean, default false), CONDITIONALLY set `workspace`.

    share-envelope.ts:
    17. Already touched in 24-03 for the wire field removal. This task verifies no residual references.
  </action>
  <verify>
    <automated>npx tsc --noEmit src/domain/subject-key.ts src/domain/recency-rerank.ts src/domain/sources.ts src/domain/sharing.ts src/domain/save-note.ts src/domain/share-envelope.ts 2>&1 | grep -cE "src/domain/.*\.ts"</automated>
  </verify>
  <acceptance_criteria>
    - subject-key.ts: grep -cE "subjectFromRoom|kind:\\s*['\"]room['\"]" returns 0
    - subject-key.ts: grep -cE "subjectFromEntity|kind:\\s*['\"]entity['\"]" returns greater than 0
    - recency-rerank.ts: grep -cE "HALF_LIFE_BY_ROOM|halfLifeForRoom" returns 0
    - recency-rerank.ts: grep -cE "DEFAULT_HALF_LIFE|HALF_LIFE_DAYS" returns 1
    - save-note.ts: grep -c "private:" returns greater than 0
    - For each of the 6 files: grep -cE "defaultRoom|findRoom|RoomId\\b|Room\\b" returns 0 (no Room/RoomId in type refs)
    - tsc --noEmit error count IN these 6 files === 0
  </acceptance_criteria>
  <done>Domain layer: entity-only subjects, uniform half-life, no room in source/sharing/save-note signatures.</done>
</task>

</tasks>

<verification>
- For all 32 files: tsc --noEmit produces 0 errors IN them. Cross-file errors with files in Plan 10 may remain — that's fine.
- grep -rnE "(defaultRoom|findRoom|nodesInRoom|RoomId|HALF_LIFE_BY_ROOM|subjectFromRoom)" src/cli/commands src/application src/domain | wc -l returns 0
- Open Questions 2, 3, 4 resolved per spec above (decisions documented in 24-09-SUMMARY).
</verification>

<success_criteria>
1. Read-side CLI auto-applies workspace pre-filter from cwd (ROOMS-DEL-07)
2. save accepts --private (ROOMS-DEL-04)
3. Application + domain layers are workspace-agnostic / entity-keyed
4. ~32 files in this batch compile cleanly
</success_criteria>

<output>
After completion, create .planning/phases/phase-24/24-09-SUMMARY.md with:
- File-by-file change list (one-liner each)
- Open Question resolutions: 2 (oracle deleted), 3 (federation-sim surgical-edited per Plan 10 G5 — this plan delegates that to Wave 3b), 4 (HALF_LIFE_BY_ROOM dropped, uniform 14d)
- Sample `wellinformed save --private "..."` test (verifies the new flag works)
- Sample `wellinformed ask --workspace all` test (verifies cross-workspace opt-out)
</output>
