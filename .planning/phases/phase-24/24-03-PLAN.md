---
phase: phase-24
plan: 03
type: execute
wave: 1
depends_on: [24-01]
files_modified:
  - src/infrastructure/search-sync.ts
  - src/application/peer-pull-telemetry.ts
  - src/infrastructure/touch-protocol.ts
  - src/domain/share-envelope.ts
  - src/application/federated-search.ts
autonomous: true
requirements:
  - ROOMS-DEL-05
must_haves:
  truths:
    - "SearchRequest type has no `room` field"
    - "SearchResponse / PeerMatch has no `room` field"
    - "Touch protocol envelope has no `room` parameter"
    - "share-envelope.ts has no `room` field"
    - "peer-pull-telemetry is rewritten to peer-only (no room dimension)"
    - "Pre-V5 (V4) SearchRequest with `room` field produces a clear ProtocolMismatchError"
  artifacts:
    - path: "src/infrastructure/search-sync.ts"
      provides: "V5 SearchRequest / SearchResponse without room"
    - path: "src/application/peer-pull-telemetry.ts"
      provides: "Peer-only telemetry (room dimension removed)"
    - path: "src/infrastructure/touch-protocol.ts"
      provides: "Touch protocol without room param"
    - path: "src/domain/share-envelope.ts"
      provides: "ShareEnvelope without room field"
  key_links:
    - from: "search-sync.ts"
      to: "V5 protocol enforcement"
      via: "ProtocolMismatchError when room field present in inbound envelope"
      pattern: "ProtocolMismatchError"
---

<objective>
Wave 1b: rewrite the 4 wire-protocol envelope files. The `room` field is excised from `SearchRequest`, `SearchResponse`, `PeerMatch`, `TouchRequest`, `ShareEnvelope`, and peer-pull telemetry. This is the on-wire V5 contract.

**Open Question 5 RESOLVED:** Rewrite `peer-pull-telemetry.ts` (don't delete) — the per-peer signal still matters for reputation. The room dimension was extra; the peer dimension stays.

Purpose: lock the wire protocol at V5 so the share-sync rewrite (Wave 2a) has stable envelope types to consume. Pre-V5 peers receive an explicit ProtocolMismatchError.
Output: 5 wire-layer files updated; peer-pull-telemetry rewritten; explicit V5 protocol check added to search-sync inbound handler.
</objective>

<execution_context>
@/Users/saharbarak/.claude/get-shit-done/workflows/execute-plan.md
@/Users/saharbarak/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/phase-24/24-CONTEXT.md
@.planning/phases/phase-24/24-RESEARCH.md
@src/infrastructure/search-sync.ts
@src/application/peer-pull-telemetry.ts
@src/infrastructure/touch-protocol.ts
@src/domain/share-envelope.ts
@src/application/federated-search.ts
@src/domain/errors.ts
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Excise room from search-sync.ts (SearchRequest / SearchResponse / PeerMatch) + add V5 protocol guard</name>
  <files>src/infrastructure/search-sync.ts, src/domain/errors.ts</files>
  <read_first>
    - src/infrastructure/search-sync.ts (entire file — note the SearchRequest/SearchResponse types around line 178; loadSharedRooms import at line 49)
    - src/domain/errors.ts (entire file — to see SearchError pattern and add ProtocolMismatch variant in the same style)
    - .planning/phases/phase-24/24-RESEARCH.md (lines 593-595 — ROOMS-DEL-05 mapping)
  </read_first>
  <action>
    In `src/infrastructure/search-sync.ts`:

    1. Remove `import { loadSharedRooms } from './share-store.js'` (file is deleted in 24-02).

    2. Find the `SearchRequest` type/interface (around line 178). Drop the `room?: string` field. Add an explicit `protocol_version: 5` literal field (e.g., `readonly protocol_version: 5`) so an inbound V4 envelope (no version, has room) is detectable.

    3. Find `SearchResponse` (search the file). Drop the `room?: string` field from response shape.

    4. Find `PeerMatch` type (likely in same file or imported from federated-search.ts). Drop `room?: string`.

    5. In the inbound request decoder (look for `parseSearchRequest` or the framed-stream handler that decodes the JSON envelope), add a V5 guard at the top:
       ```ts
       if ((decoded as Record<string, unknown>).room !== undefined) {
         return errAsync(SE.protocolMismatch(`peer at ${peerLabel} sent V4 SearchRequest with \`room\` field. This peer is on V5; the \`room\` field is removed. See docs/architecture/V5-PROTOCOL.md.`));
       }
       if ((decoded as Record<string, unknown>).protocol_version !== 5) {
         return errAsync(SE.protocolMismatch(`unknown protocol_version: ${(decoded as Record<string, unknown>).protocol_version}`));
       }
       ```
       Use the existing SearchError style (neverthrow Results).

    6. Outbound request constructor: always set `protocol_version: 5` on the envelope.

    7. Remove any `searchByRoom` / `searchByRoomHybrid` / `searchByRoomHybridBinary` calls in this file — replace with `searchGlobal` (the workspace pre-filter is applied at the read CALLER site in Wave 3, NOT inside search-sync).

    8. Token bucket / rate limiter keyed on `peerId` only (was `${peerId}:${room}` per research). If you see compound keys, simplify to peer-only.

    In `src/domain/errors.ts`:

    1. Add a new SearchError variant:
       ```ts
       export type SearchError =
         | { kind: 'protocolMismatch'; message: string }
         | // ... existing variants
       ```

    2. Add a constructor `SE.protocolMismatch = (message: string): SearchError => ({ kind: 'protocolMismatch', message })` next to existing SE.* constructors.

    3. Add the `protocolMismatch` case to `formatError`'s SearchError switch (return the `message` field).
  </action>
  <verify>
    <automated>! grep -E "room\\??:\\s*(string|Room)" src/infrastructure/search-sync.ts && grep -c "protocol_version" src/infrastructure/search-sync.ts && grep -c "protocolMismatch" src/domain/errors.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "room\\??:\\s*(string|Room)" src/infrastructure/search-sync.ts` returns 0
    - `grep -c "loadSharedRooms\|share-store" src/infrastructure/search-sync.ts` returns 0
    - `grep -c "protocol_version: 5\|protocol_version === 5" src/infrastructure/search-sync.ts` returns ≥ 2 (outbound + inbound check)
    - `grep -c "protocolMismatch" src/domain/errors.ts` returns ≥ 3 (type variant + constructor + formatError)
    - `grep -cE "searchByRoom(Hybrid|HybridBinary)?" src/infrastructure/search-sync.ts` returns 0
  </acceptance_criteria>
  <done>search-sync.ts speaks V5 only. V4 envelopes (with `room`) return ProtocolMismatchError. Outbound envelopes always carry `protocol_version: 5`.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Rewrite peer-pull-telemetry.ts to peer-only (drop room dimension)</name>
  <files>src/application/peer-pull-telemetry.ts</files>
  <read_first>
    - src/application/peer-pull-telemetry.ts (entire file — small, ~4KB per research; understand current state shape)
    - .planning/phases/phase-24/24-RESEARCH.md (line 633 — peer-pull-telemetry decision: rewrite per-peer)
    - .planning/phases/phase-24/24-RESEARCH.md (line 891-892 — Open Question 5)
  </read_first>
  <action>
    Open Question 5 RESOLVED: rewrite to peer-only (don't delete).

    In `src/application/peer-pull-telemetry.ts`:

    1. Remove `import { ... } from '../domain/system-rooms.js'` (file is deleted in 24-02).

    2. Restructure the telemetry shape — currently it's `{ peer, room, ... }` per record; drop `room`. New record shape: `{ peerId: PeerId, lastPullAt: Date, pullCount: number, ...whatever other peer-scoped signals existed }`.

    3. If there is a `Map<string, ...>` keyed on `${peerId}:${room}`, change key to `peerId` only.

    4. Remove any `roomId` parameter from public functions. The export surface narrows to: `recordPull(peerId, ...)`, `getTelemetry(peerId)`, `listAllTelemetry()`.

    5. If the file has a persistence path involving `room`, drop the room from the path (e.g., `peer-pull-telemetry/<peer>/<room>.json` → `peer-pull-telemetry/<peer>.json`).

    6. Keep neverthrow Result return types — match the file's existing style.

    7. Update JSDoc / file header to note: "V5: per-peer telemetry only; room dimension dropped 2026-05-27."

    Target file size: ~80-150 lines (down from ~120 lines pre-deletion — peer-only is smaller than peer×room).
  </action>
  <verify>
    <automated>! grep -E "room\\??:\\s*(string|Room|RoomId)" src/application/peer-pull-telemetry.ts && ! grep "system-rooms" src/application/peer-pull-telemetry.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "room\\??:\\s*(string|Room|RoomId)" src/application/peer-pull-telemetry.ts` returns 0
    - `grep -c "from.*system-rooms" src/application/peer-pull-telemetry.ts` returns 0
    - `grep -c "peerId" src/application/peer-pull-telemetry.ts` returns ≥ 3 (functions still take peer)
    - `wc -l < src/application/peer-pull-telemetry.ts` returns 50-200
  </acceptance_criteria>
  <done>peer-pull-telemetry tracks peer signal only; no room references remain.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Excise room from touch-protocol.ts + share-envelope.ts + federated-search.ts</name>
  <files>src/infrastructure/touch-protocol.ts, src/domain/share-envelope.ts, src/application/federated-search.ts</files>
  <read_first>
    - src/infrastructure/touch-protocol.ts (entire file — note line 42 share-store import, line 50 system-rooms import per research)
    - src/domain/share-envelope.ts (entire file — share envelope shape)
    - src/application/federated-search.ts (entire file — note lines 50/94 PeerMatch.room per research)
    - .planning/phases/phase-24/24-RESEARCH.md (lines 264-266 — Wave 1b file list)
  </read_first>
  <action>
    **In `src/infrastructure/touch-protocol.ts`:**

    1. Drop `import { loadSharedRooms } from '../infrastructure/share-store.js'` and `import { ... } from '../domain/system-rooms.js'`.
    2. Find the `TouchRequest` / `TouchResponse` types. Drop the `room?: string` parameter from the request.
    3. New semantics: a touch request says "give me your N freshest non-private nodes." Update the handler to filter `graph.nodes.filter(n => !n.private)` and sort by `fetched_at` desc.
    4. Keep the bandwidth + secrets-scanner gates.
    5. The handler signature changes from `handleTouch(req: { room, limit, ... })` to `handleTouch(req: { limit, ... })`.

    **In `src/domain/share-envelope.ts`:**

    1. Find the `ShareEnvelope` type/interface. Drop `room?: string` field.
    2. If there's a `buildEnvelope` / `parseEnvelope` helper, drop the room read/write.
    3. If the envelope explicitly versions (e.g., `version: 1`), bump to `version: 5` to be consistent with the V5 cutover.

    **In `src/application/federated-search.ts`:**

    1. Find `PeerMatch` type (lines 50/94 per research). Drop `room?: string` field.
    2. In the fan-out routing logic, drop any `byRoom` grouping. Results are flat; the `--workspace` filter (when present) is applied at the local read site BEFORE the fan-out (caller's responsibility, Wave 3).
    3. The result re-ranker no longer groups by `(peer, room)` — only by peer.

    Hard cutover: do NOT preserve backward-compat aliases (`room || workspace`). No shims.
  </action>
  <verify>
    <automated>! grep -E "room\\??:\\s*(string|Room|RoomId)" src/infrastructure/touch-protocol.ts src/domain/share-envelope.ts src/application/federated-search.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "room\\??:\\s*(string|Room|RoomId)" src/infrastructure/touch-protocol.ts` returns 0
    - `grep -cE "room\\??:\\s*(string|Room|RoomId)" src/domain/share-envelope.ts` returns 0
    - `grep -cE "room\\??:\\s*(string|Room|RoomId)" src/application/federated-search.ts` returns 0
    - `grep -c "from.*share-store\|from.*system-rooms" src/infrastructure/touch-protocol.ts` returns 0
    - `grep -c "n\\.private\|!n\\.private\|node\\.private" src/infrastructure/touch-protocol.ts` returns ≥ 1 (new private gate)
  </acceptance_criteria>
  <done>All wire-protocol envelopes are V5-clean. No `room` field on any outgoing or incoming message.</done>
</task>

</tasks>

<verification>
- `grep -rn "room\\??:\\s*\\(string\\|Room\\|RoomId\\)" src/infrastructure/search-sync.ts src/infrastructure/touch-protocol.ts src/domain/share-envelope.ts src/application/federated-search.ts src/application/peer-pull-telemetry.ts` returns 0 lines
- `grep -c "protocol_version" src/infrastructure/search-sync.ts` ≥ 2
- `grep -c "protocolMismatch" src/domain/errors.ts` ≥ 3
- `tsc --noEmit` STILL has errors (Wave 2/3 fix), but the V5 wire types are now stable
</verification>

<success_criteria>
1. Wire envelopes (SearchRequest/Response, PeerMatch, ShareEnvelope, TouchRequest) have NO `room` field
2. Pre-V5 peers receive `SearchError.protocolMismatch` with clear message
3. peer-pull-telemetry tracks peer-only (no room dimension)
4. SE.protocolMismatch added to errors.ts following existing SE.* style
</success_criteria>

<output>
After completion, create `.planning/phases/phase-24/24-03-SUMMARY.md` with:
- Final shape of SearchRequest / SearchResponse / PeerMatch / TouchRequest / ShareEnvelope (paste types)
- Decision: peer-pull-telemetry rewritten (Open Question 5)
- LOC delta for peer-pull-telemetry.ts (before → after)
</output>
