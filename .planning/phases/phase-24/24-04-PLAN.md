---
phase: phase-24
plan: 04
type: execute
wave: 1
depends_on: [24-01]
files_modified:
  - src/cli/runtime.ts
  - src/daemon/loop.ts
autonomous: true
requirements:
  - ROOMS-DEL-02
  - ROOMS-DEL-07
must_haves:
  truths:
    - "runtime.ts no longer imports fileRoomsConfig or ensureSystemRoomsShared"
    - "runtime.ts no longer carries `rooms: string` paths field or `rooms: RoomsConfig` deps field"
    - "runtime.ts exports a `detectWorkspace(cwd?: string): string | undefined` helper backed by git rev-parse"
    - "daemon/loop.ts no longer imports RoomsConfig or calls defaultRoom() / triggerRoom"
    - "Boot path does not touch ~/.wellinformed/rooms.json or shared-rooms.json"
  artifacts:
    - path: "src/cli/runtime.ts"
      provides: "Composition root with detectWorkspace helper; no rooms wiring"
      exports: ["detectWorkspace"]
    - path: "src/daemon/loop.ts"
      provides: "Daemon loop without RoomsConfig or per-room triggers"
  key_links:
    - from: "runtime.ts.detectWorkspace"
      to: "CLI commands (ask, save, recall, report — surgical edits in Wave 3)"
      via: "import + invocation at command entry"
      pattern: "detectWorkspace\\("
---

<objective>
Wave 1c: rewire the composition root (runtime.ts) and daemon loop to drop all rooms infrastructure and add the new `detectWorkspace` helper.

Purpose: severs the boot-time dependency on rooms.json + shared-rooms.json. After this lands, no first-boot path attempts to read either file. The new `detectWorkspace(cwd)` helper becomes the workspace pre-filter source for all read-side commands (Wave 3 consumes it).
Output: runtime.ts and daemon/loop.ts compile cleanly against the V5 schema (assuming Wave 1a/1b deletions land first via the depends_on gate on 24-01).
</objective>

<execution_context>
@/Users/saharbarak/.claude/get-shit-done/workflows/execute-plan.md
@/Users/saharbarak/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/phase-24/24-CONTEXT.md
@.planning/phases/phase-24/24-RESEARCH.md
@src/cli/runtime.ts
@src/daemon/loop.ts
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Strip rooms wiring from runtime.ts; add detectWorkspace helper</name>
  <files>src/cli/runtime.ts</files>
  <read_first>
    - src/cli/runtime.ts (entire file — composition root, ~250-300 lines per research)
    - .planning/phases/phase-24/24-RESEARCH.md (lines 428-456 — exact line numbers to drop + helper code)
    - .planning/phases/phase-24/24-RESEARCH.md (lines 175-186 — detectWorkspace code from CONTEXT.md specifics block)
  </read_first>
  <action>
    In `src/cli/runtime.ts`:

    **Removals (per research lines 450-456):**
    1. Drop line ~23: `import { fileRoomsConfig } from '../infrastructure/rooms-config.js'` (file deleted in 24-02).
    2. Drop line ~30: `import { ensureSystemRoomsShared } from '../domain/system-rooms.js'` (file deleted in 24-02).
    3. Drop the `rooms: string` path field (around line 123, 135 — the path joined to `home/'rooms.json'`).
    4. Drop the `rooms: RoomsConfig` deps field (around line 148).
    5. Drop the boot-time `ensureSystemRoomsShared(...)` invocation (lines 189-193).
    6. Drop the `fileRoomsConfig(paths.rooms)` wiring (lines 208, 249).

    **Additions:**
    7. Add the `detectWorkspace` helper EXACTLY as specified in 24-RESEARCH.md lines 430-447:

       ```ts
       import { execFileSync } from 'node:child_process';
       import { basename } from 'node:path';

       const slugify = (s: string): string =>
         s.toLowerCase().replace(/[^a-z0-9]+/g, '-').replace(/^-+|-+$/g, '').slice(0, 63);

       export const detectWorkspace = (cwd: string = process.cwd()): string | undefined => {
         try {
           const top = execFileSync('git', ['rev-parse', '--show-toplevel'], {
             cwd, encoding: 'utf8', stdio: ['ignore', 'pipe', 'ignore'],
           }).trim();
           if (!top) return undefined;
           return slugify(basename(top));
         } catch {
           return undefined;
         }
       };
       ```

       Place near the top of the file (after imports, before other exports).

    8. Verify `execFileSync` and `basename` imports do not duplicate existing imports — merge if needed.

    9. The exported deps/paths object shape changes. Update any consumer types in the same file (e.g., `AppDeps`, `AppPaths`) to remove the `rooms` keys. The downstream surgical edits (Wave 3) will adjust callers as `tsc --noEmit` errors appear.

    10. Hard cutover. No shim functions. No `// @deprecated` markers. Just removed.
  </action>
  <verify>
    <automated>! grep -E "fileRoomsConfig|ensureSystemRoomsShared|RoomsConfig|rooms\\.json" src/cli/runtime.ts && grep -c "export const detectWorkspace" src/cli/runtime.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "fileRoomsConfig|ensureSystemRoomsShared|RoomsConfig" src/cli/runtime.ts` returns 0
    - `grep -c "rooms\\.json" src/cli/runtime.ts` returns 0
    - `grep -c "export const detectWorkspace" src/cli/runtime.ts` returns 1
    - `grep -c "git.*rev-parse.*--show-toplevel" src/cli/runtime.ts` returns 1
    - `grep -c "slugify" src/cli/runtime.ts` returns ≥ 2 (definition + invocation)
    - File still compiles its own type definitions cleanly (the consumer-side errors are Wave 3's job): `npx tsc --noEmit src/cli/runtime.ts 2>&1 | grep -c "in src/cli/runtime.ts"` returns 0 (only errors in CALLERS, not in this file itself)
  </acceptance_criteria>
  <done>runtime.ts is room-free; detectWorkspace is exported and ready for Wave 3 consumers.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Strip RoomsConfig + per-room triggers from daemon/loop.ts</name>
  <files>src/daemon/loop.ts</files>
  <read_first>
    - src/daemon/loop.ts (entire file — ingest tick + tick handlers)
    - .planning/phases/phase-24/24-RESEARCH.md (line 269 — Wave 1c daemon delta)
    - .planning/phases/phase-24/24-RESEARCH.md (line 621 — note on ensureSessionsRoom becoming a no-op)
  </read_first>
  <action>
    In `src/daemon/loop.ts`:

    1. Drop `import { RoomsConfig } from '../infrastructure/rooms-config.js'` and any system-rooms.ts imports (those files are deleted in 24-02).

    2. Find the daemon tick handler. Remove:
       - any `roomIds: string[]` field on the tick context
       - any `triggerRoom(roomId, ...)` ingestion-path branch (this was the per-room trigger logic; sources now run flat)
       - any `defaultRoom()` calls

    3. **Session room handling (per research line 621):** `ensureSessionsRoom` and `nodesInRoom(graph, 'sessions')` are dead. In v5 the daemon's session-sources tick still runs, but it no longer pins a "sessions room." The session adapter emits nodes with `source_uri: claude_sessions:...` — that's the source-uri-scheme filter for "show me sessions" (handled in Wave 3 surgical edits to recent-sessions.ts).

       In daemon/loop.ts: remove any `ensureSessionsRoom(...)` call. If the call site has a meaningful side effect beyond pinning a room (e.g., creating a folder), preserve that side effect but drop the room creation part.

    4. The daemon ingest loop now runs each registered source (codebase, claude-sessions, hackernews, etc.) with no per-room dispatch — the source adapter writes nodes to the global graph. Update tick types accordingly.

    5. The bandwidth + connection-health logic from Phase 18 is room-independent — leave it untouched.

    6. Hard cutover: do not maintain a `defaultRoomId` fallback. The function is GONE.
  </action>
  <verify>
    <automated>! grep -E "RoomsConfig|defaultRoom|triggerRoom|ensureSessionsRoom|nodesInRoom" src/daemon/loop.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "RoomsConfig|defaultRoom|triggerRoom" src/daemon/loop.ts` returns 0
    - `grep -cE "ensureSessionsRoom|nodesInRoom" src/daemon/loop.ts` returns 0
    - `grep -c "from.*rooms-config\|from.*system-rooms\|from.*rooms\\.js" src/daemon/loop.ts` returns 0
    - daemon's tick loop still iterates registered sources: `grep -c "for.*const.*source\|sources\\.forEach\\|Object\\.entries.*sources" src/daemon/loop.ts` returns ≥ 1
  </acceptance_criteria>
  <done>daemon/loop.ts has no room concepts; tick still runs each source against the flat graph.</done>
</task>

</tasks>

<verification>
- `grep -rnE "fileRoomsConfig|ensureSystemRoomsShared|RoomsConfig|defaultRoom\\(|triggerRoom\\(" src/cli/runtime.ts src/daemon/loop.ts` returns 0 lines
- `grep -c "detectWorkspace" src/cli/runtime.ts` ≥ 1
- The runtime self-compiles (errors only in CALLERS of changed exports — those are Wave 3)
</verification>

<success_criteria>
1. runtime.ts: zero rooms imports; detectWorkspace exported
2. daemon/loop.ts: zero rooms imports; no per-room trigger path
3. Boot path no longer reads ~/.wellinformed/rooms.json or shared-rooms.json
</success_criteria>

<output>
After completion, create `.planning/phases/phase-24/24-04-SUMMARY.md` with:
- Before/after deps shape (paste the AppDeps type)
- Confirmation that detectWorkspace works on a sample git repo (run it inline as a sanity check)
- LOC delta for both files
</output>
