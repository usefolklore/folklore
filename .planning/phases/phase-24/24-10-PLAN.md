---
phase: phase-24
plan: 10
type: execute
wave: 3
depends_on: [24-02, 24-03, 24-04, 24-06]
files_modified:
  - src/infrastructure/vector-index.ts
  - src/infrastructure/peer-reputation-store.ts
  - src/infrastructure/sources-config.ts
  - src/infrastructure/recall-sync.ts
  - src/infrastructure/search-gossip.ts
  - src/infrastructure/federation-sim.ts
  - src/daemon/job-runner.ts
  - src/telegram/commands.ts
  - src/telegram/capture.ts
autonomous: true
requirements:
  - ROOMS-DEL-04
  - ROOMS-DEL-05
must_haves:
  truths:
    - "vector-index.ts has no searchByRoom / searchByRoomHybrid / searchByRoomHybridBinary methods"
    - "peer-reputation-store.ts subject keys are entity-only; room: prefix never written"
    - "sources-config.ts has no `room` field on any source descriptor"
    - "recall-sync.ts has no loadShared (was room-keyed) function"
    - "search-gossip.ts compiles against V5 wire types"
    - "federation-sim.ts compiles (model simplified — no room dimension)"
    - "daemon/job-runner.ts has no per-room job routing"
    - "telegram/commands.ts + telegram/capture.ts have no defaultRoom / findRoom / roomIds references"
  artifacts:
    - path: "src/infrastructure/vector-index.ts"
      provides: "searchGlobal only — workspace filter is caller responsibility"
    - path: "src/infrastructure/peer-reputation-store.ts"
      provides: "Peer reputation with entity-only subject keys"
  key_links:
    - from: "vector-index.searchGlobal"
      to: "read-side CLI (workspace filter applied AFTER index hit)"
      via: "Plan 09 ask/recall/discover/report"
      pattern: "searchGlobal"
---

<objective>
Wave 3b: surgical edits to ~9 infrastructure + daemon + telegram files. Together with Plan 09 this resolves all remaining tsc --noEmit errors from Wave 0.

**Open Question 3 RESOLVED:** federation-sim.ts gets surgical-edited here. The sim model loses its room dimension; benchmark scoring logic is deferred to Phase 25 (FolkloreBench-F) — the sim still compiles, just doesn't model rooms.
**Open Question 5 RESOLVED (final piece):** peer-pull-telemetry.ts was rewritten in 24-03 (peer-only). Confirmed.

Purpose: complete the surgical-edit phase. After Plan 09 + Plan 10 land together, `tsc --noEmit` should exit 0 across `src/`.
Output: ~9 infra/daemon/telegram files updated to V5 schema.
</objective>

<execution_context>
@/Users/saharbarak/.claude/get-shit-done/workflows/execute-plan.md
@/Users/saharbarak/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/phase-24/24-CONTEXT.md
@.planning/phases/phase-24/24-RESEARCH.md
@/tmp/phase24-wave0-blastradius.txt
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Strip searchByRoom helpers from vector-index.ts</name>
  <files>src/infrastructure/vector-index.ts</files>
  <read_first>
    - src/infrastructure/vector-index.ts (entire file — lines 43-85 for method declarations, lines 376/451/571 for impls per research)
    - .planning/phases/phase-24/24-RESEARCH.md (lines 388-397 — vector-index.ts decision)
  </read_first>
  <action>
    In vector-index.ts:

    1. Drop the `searchByRoom(room, query, k)` method from the public interface and its implementation (~line 376).
    2. Drop `searchByRoomHybrid(...)` (~line 451) and `searchByRoomHybridBinary(...)` (~line 571) — both are wrappers around searchGlobal with post-filter, the callers now apply the workspace filter themselves.
    3. Keep `searchGlobal(query, k)` and its hybrid variants UNCHANGED — they're the V5 primary API.
    4. Drop any imports from rooms.ts / system-rooms.ts.
    5. Update any TraversalOptions / SearchOptions types here to remove a `room` field.
    6. The "moving the workspace filter UP the stack" research note: callers now do `searchGlobal(...).then(hits => hits.filter(h => !workspaceSlug || h.workspace === workspaceSlug))`. Confirm vector-index.ts no longer knows about workspace either.

    Target: file shrinks by ~150-300 lines from these helper deletions.
  </action>
  <verify>
    <automated>bash -c "grep -cE 'searchByRoom' src/infrastructure/vector-index.ts"</automated>
  </verify>
  <acceptance_criteria>
    - grep -cE "searchByRoom" src/infrastructure/vector-index.ts returns 0
    - grep -cE "searchByRoomHybrid|searchByRoomHybridBinary" src/infrastructure/vector-index.ts returns 0
    - grep -cE "searchGlobal" src/infrastructure/vector-index.ts returns greater than 0 (preserved)
    - grep -cE "from.*rooms\.js|from.*system-rooms" src/infrastructure/vector-index.ts returns 0
    - npx tsc --noEmit src/infrastructure/vector-index.ts produces 0 errors IN this file
  </acceptance_criteria>
  <done>vector-index.ts is the workspace-agnostic search primitive; workspace filter is caller responsibility.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Flatten peer-reputation-store.ts subject keys (drop room:* prefix)</name>
  <files>src/infrastructure/peer-reputation-store.ts</files>
  <read_first>
    - src/infrastructure/peer-reputation-store.ts (entire file)
    - .planning/phases/phase-24/24-RESEARCH.md (lines 406-426 — peer-reputation-store.ts spec)
    - docs/p2p/peer-reputation-design.md (read the entity:* primary subject key section — lines 84-87)
  </read_first>
  <action>
    In peer-reputation-store.ts:

    1. Verify the on-disk schema (subject key → score+count+last_seen tuples per peer).
    2. In any code that WRITES a subject key, drop the `room:*` write path entirely. Only `entity:*` (and any non-room subject schemes) survive.
    3. In any code that READS a subject key (e.g., the score lookup), if a `room:*` key is encountered (from pre-v5 data), IGNORE it (skip it). The migration command (Plan 11) is responsible for cleaning the on-disk file; the runtime just doesn't write or surface those keys.
    4. Update the SubjectKey union type (which is from subject-key.ts changes in Plan 09 Task 4) — confirm it no longer admits 'room'.
    5. If there are tests/asserts referring to `room:*` keys inline, drop them (the test plan 24-12 handles the test-side).
    6. Keep neverthrow Result return types — match the file's existing style.
  </action>
  <verify>
    <automated>bash -c "grep -cE \"'room:'|\\\"room:\\\"|kind:\\s*'room'\" src/infrastructure/peer-reputation-store.ts"</automated>
  </verify>
  <acceptance_criteria>
    - grep -cE "'room:'" src/infrastructure/peer-reputation-store.ts returns 0
    - grep -cE "\"room:\"" src/infrastructure/peer-reputation-store.ts returns 0
    - grep -cE "kind:\\s*['\"]room['\"]" src/infrastructure/peer-reputation-store.ts returns 0
    - grep -cE "entity:|kind:\\s*['\"]entity['\"]" src/infrastructure/peer-reputation-store.ts returns greater than 0 (entity path preserved)
    - npx tsc --noEmit src/infrastructure/peer-reputation-store.ts produces 0 errors IN this file
  </acceptance_criteria>
  <done>Reputation store writes entity-only subject keys; gracefully skips legacy room:* keys at read time.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Update sources-config.ts + recall-sync.ts + search-gossip.ts + federation-sim.ts + daemon/job-runner.ts</name>
  <files>src/infrastructure/sources-config.ts, src/infrastructure/recall-sync.ts, src/infrastructure/search-gossip.ts, src/infrastructure/federation-sim.ts, src/daemon/job-runner.ts</files>
  <read_first>
    - Each file (entire content)
    - .planning/phases/phase-24/24-RESEARCH.md (lines 311 — group G5)
    - .planning/phases/phase-24/24-RESEARCH.md (line 653 — federation-sim decision: surgical edit OR stub; planner picks surgical edit)
  </read_first>
  <action>
    sources-config.ts:
    1. Drop the `room` field from any source descriptor type/interface.
    2. Drop the `forRoom(...)` helper / sources-by-room grouping if present.
    3. The config no longer maps sources to rooms.

    recall-sync.ts:
    4. Drop `loadShared` (which loaded the shared-rooms.json — file gone).
    5. Drop any room-keyed sync state.
    6. The recall-sync logic is now graph-wide; sync state keyed on peer or globally.

    search-gossip.ts:
    7. Drop any room field on the gossip envelope.
    8. Drop imports from system-rooms / share-store.
    9. Gossip becomes peer-level (which peer has what hot content) without partitioning.

    federation-sim.ts (Open Question 3 RESOLVED — surgical edit here):
    10. The simulator currently models room-based propagation. After this edit, it models a flat graph.
    11. Drop the `room` axis from the simulation state.
    12. Where the sim measured "niche evaporation" (a room-membership metric), this metric is gone — replace with a degenerate-OK stub function `simulateNicheEvaporation() => { return { ratio: 0, deferred: true, reason: 'Phase 25 will redesign without room dimension' }; }` OR comment-out the metric path (planner discretion).
    13. The sim must still COMPILE; behavior is allowed to be a no-op.
    14. Add a top-of-file comment: `// V5: room dimension removed; benchmark scoring logic deferred to Phase 25 (FolkloreBench-F redesign).`

    daemon/job-runner.ts:
    15. Drop per-room job routing.
    16. Jobs (ingest, share-sync, search-gossip) run flat against the global graph.
    17. Drop RoomsConfig import.
  </action>
  <verify>
    <automated>npx tsc --noEmit src/infrastructure/sources-config.ts src/infrastructure/recall-sync.ts src/infrastructure/search-gossip.ts src/infrastructure/federation-sim.ts src/daemon/job-runner.ts 2>&1 | grep -cE "src/(infrastructure|daemon)/.*\\.ts"</automated>
  </verify>
  <acceptance_criteria>
    - For each of the 5 files: grep -cE "defaultRoom|findRoom|RoomId\\b|nodesInRoom\\b" returns 0
    - For each: grep -cE "from.*rooms\.js|from.*system-rooms|from.*share-store|from.*rooms-config" returns 0
    - federation-sim.ts: grep -cE "Phase 25|niche evaporation|room dimension removed" returns greater than 0 (decision documented in file)
    - tsc --noEmit error count IN these 5 files === 0
  </acceptance_criteria>
  <done>Infrastructure + daemon: workspace-agnostic; federation-sim compiles with stubbed niche-evaporation.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Update telegram/commands.ts + telegram/capture.ts</name>
  <files>src/telegram/commands.ts, src/telegram/capture.ts</files>
  <read_first>
    - src/telegram/commands.ts (entire file — note line 9 imports per research)
    - src/telegram/capture.ts (entire file — note line 12 imports per research)
    - .planning/phases/phase-24/24-RESEARCH.md (line 656 — telegram strip spec)
  </read_first>
  <action>
    In telegram/commands.ts:
    1. Drop imports: defaultRoom, findRoom, roomIds, RoomsConfig (all from deleted modules).
    2. Drop any /room command from the Telegram bot's command list.
    3. Drop any --room flag passing to internal save invocations.
    4. The bot's save flow now writes to the global graph with no room tag. Workspace would not apply (Telegram has no cwd).

    In telegram/capture.ts:
    5. Drop the room imports.
    6. Drop the auto-room-assignment logic (the capture pipeline that tagged Telegram messages with a room).
    7. Captured messages land flat. `private: false` by default; the user can mark a chat private via a separate flag/setting if needed (defer the multi-tier privacy work — synthesis already deferred it).
  </action>
  <verify>
    <automated>bash -c "grep -cE 'defaultRoom|findRoom|roomIds|RoomsConfig' src/telegram/commands.ts src/telegram/capture.ts"</automated>
  </verify>
  <acceptance_criteria>
    - grep -cE "defaultRoom|findRoom|roomIds|RoomsConfig" src/telegram/commands.ts returns 0
    - grep -cE "defaultRoom|findRoom|roomIds|RoomsConfig" src/telegram/capture.ts returns 0
    - grep -cE "from.*rooms\.js|from.*system-rooms" src/telegram/commands.ts src/telegram/capture.ts returns 0
    - npx tsc --noEmit on both files produces 0 errors IN them
  </acceptance_criteria>
  <done>Telegram surface no longer references rooms; captures land in the global graph.</done>
</task>

</tasks>

<verification>
- npx tsc --noEmit (full project) error count should now be near-zero — only remaining errors should be in files outside this plan's scope (caught by Plan 09)
- The combination of Plan 09 + Plan 10 lands the full surgical-edits pass; Wave 4 (migration + tests) can proceed
- grep -rnE "from.*\\./domain/(rooms|system-rooms)\\.js|from.*\\./infrastructure/(rooms-config|share-store)\\.js" src/ returns 0
</verification>

<success_criteria>
1. vector-index.ts: workspace-agnostic, searchByRoom* gone
2. peer-reputation-store.ts: entity-only subject keys
3. federation-sim.ts: compiles with stubbed niche-evaporation (Open Question 3 — surgical edit)
4. Telegram surface: no rooms vocabulary
5. After this plan + Plan 09: `npx tsc --noEmit` exits 0 (or with only test-file errors that Plan 12 fixes)
</success_criteria>

<output>
After completion, create .planning/phases/phase-24/24-10-SUMMARY.md with:
- File-by-file change list
- Open Question 3 resolution note (federation-sim surgical-edited with niche-evaporation stubbed)
- Confirmation that `npx tsc --noEmit` exits 0 across src/ (post Plan 09 + Plan 10 merge)
- LOC delta totals across all 9 files
</output>
