---
phase: phase-24
plan: 06
type: execute
wave: 2
depends_on: [24-02, 24-03, 24-04]
files_modified:
  - src/infrastructure/share-sync.ts
autonomous: true
requirements:
  - ROOMS-DEL-03
must_haves:
  truths:
    - "share-sync.ts has no loadSharedRooms import"
    - "share-sync.ts has no per-room Y.Doc Map — single global Y.Doc only"
    - "share-sync.ts gates sharing on node.private === false (NOT on room-membership)"
    - "ShareSyncRegistry has no sharedRoomsPath field"
    - "Subscribe negotiation has no rooms array"
    - "Stream key uses peerId only (not peerId:room)"
    - "Bandwidth limiter key is peerId only"
    - "Outbound envelopes use the V5 SearchRequest/SearchResponse types from 24-03"
  artifacts:
    - path: "src/infrastructure/share-sync.ts"
      provides: "V5 sharing engine — single global Y.Doc, private-flag gate, peer-only stream keying"
      min_lines: 350
      max_lines: 500
  key_links:
    - from: "share-sync.ts.collectShareable"
      to: "graph nodes"
      via: "filter on n.private === false"
      pattern: "n\\.private|node\\.private"
    - from: "share-sync.ts ydoc loader"
      to: "single graph.ydoc file"
      via: "join(home, 'graph.ydoc')"
      pattern: "graph\\.ydoc"
---

<objective>
Wave 2a: rewrite share-sync.ts (869 lines) down to ~350-450 lines on the V5 contract.

Purpose: this is the SINGLE highest-risk file in the phase. It's the sharing pipeline — get this wrong and federation breaks silently (no live peers to detect the regression). The room-authorization gate disappears; the new gate is per-node node.private === false. The per-room Y.Doc map collapses to a single global graph.ydoc. The (peer, room) stream key collapses to peerId.
Output: V5-ready share-sync.ts that consumes the V5 envelope types from 24-03 and the deleted share-store/system-rooms imports.
</objective>

<execution_context>
@/Users/saharbarak/.claude/get-shit-done/workflows/execute-plan.md
@/Users/saharbarak/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/phase-24/24-CONTEXT.md
@.planning/phases/phase-24/24-RESEARCH.md
@src/infrastructure/share-sync.ts
@src/infrastructure/ydoc-store.ts
@src/infrastructure/bandwidth-limiter.ts

<interfaces>
Current pre-V5 shape (verified from research):
- Line 60: import loadSharedRooms from share-store.js (DELETED in 24-02)
- Lines 147-178: SubscribeRequest with rooms: string[] field + parseSubscribe returning rooms list
- Lines 291-340: outbound sendShareableNode keyed on room
- Lines 350-444: inbound receive flow with `v.room ?? room` propagation
- Lines 537-554: StreamEntry keyed `${peer}::${room}`
- Lines 547-610: ShareSyncRegistry { docs: Map<room, Y.Doc>, streams: Map<peerRoomKey>, sharedRoomsPath }
- Line 616: ydocPathFor(registry, room) → `${room}.ydoc`
- Lines 619-628: getOrLoadYDoc(registry, room) — per-room Y.Doc cache

Target V5 shape:
- Single global Y.Doc at ~/.akashik/graph.ydoc
- SubscribeRequest carries protocol_version: 5 and NO rooms array
- StreamEntry keyed on peerId only
- Gate: node.private === false (replaces loadSharedRooms + auditRoom)
- Bandwidth limiter key: peerId only
- searchByRoom helpers gone — caller filters on workspace at read site
- KEEP: secrets scanner, bandwidth limiter, REMOTE_ORIGIN echo prevention, Y.Doc V1 encoding (encodeStateAsUpdate/applyUpdate only), debounced flush
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Excise room concept from share-sync.ts type surface + ShareSyncRegistry</name>
  <files>src/infrastructure/share-sync.ts</files>
  <read_first>
    - src/infrastructure/share-sync.ts (entire file — 869 lines; deep read is REQUIRED for safe rewrite)
    - src/infrastructure/ydoc-store.ts (V1 encoding contract — must be preserved)
    - .planning/phases/phase-24/24-RESEARCH.md (lines 367-386 — exact current shape and target shape)
    - .planning/phases/phase-24/24-RESEARCH.md (lines 615-619 — high-risk landmines for this rewrite)
  </read_first>
  <action>
    Update share-sync.ts type definitions and registry:

    1. Remove import: loadSharedRooms from share-store.js (file deleted).
    2. Remove any import: system-rooms.js or RoomsConfig (deleted).
    3. SubscribeRequest type: drop rooms: string[] field; add protocol_version: 5 literal field. parseSubscribe: no longer parses rooms; only parses + validates protocol_version. Outbound subscribe: send { protocol_version: 5 }.
    4. ShareSyncRegistry: change docs: Map<RoomId, Y.Doc> to doc: Y.Doc (single global). Change streams: Map<peerRoomKey, StreamEntry> to streams: Map<PeerId, StreamEntry>. Drop sharedRoomsPath field.
    5. StreamEntry: drop the room field; key is peerId-only.
    6. ydocPathFor: simplify to a constant — join(home, 'graph.ydoc').
    7. getOrLoadYDoc(registry): no room param. Returns/lazy-creates the single global Y.Doc.
    8. Preserve V1 encoding semantics (encodeStateAsUpdate/applyUpdate only) per Phase 16 decision.
    9. Preserve writeQueues serialization pattern per Phase 16 decision (still keyed per-path; only one path now).
  </action>
  <verify>
    <automated>npx tsc --noEmit src/infrastructure/share-sync.ts 2>&1 | grep -c "in src/infrastructure/share-sync.ts"</automated>
  </verify>
  <acceptance_criteria>
    - grep -cE "from.*share-store|loadSharedRooms" src/infrastructure/share-sync.ts returns 0
    - grep -cE "rooms\s*:\s*(string\[\]|RoomId\[\]|Room\[\])" src/infrastructure/share-sync.ts returns 0
    - grep -cE "Map<RoomId|Map<Room\b" src/infrastructure/share-sync.ts returns 0
    - grep -c "sharedRoomsPath" src/infrastructure/share-sync.ts returns 0
    - grep -c "graph\.ydoc" src/infrastructure/share-sync.ts returns greater than 0 (the global path constant)
    - grep -c "protocol_version" src/infrastructure/share-sync.ts returns greater than 0
    - File self-types compile (errors only in callers): npx tsc --noEmit src/infrastructure/share-sync.ts 2>&1 | grep "src/infrastructure/share-sync.ts" returns no errors
  </acceptance_criteria>
  <done>Type surface and registry are V5-shaped; the file declares only V5-compatible state.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Rewrite outbound/inbound flow on the private-flag gate</name>
  <files>src/infrastructure/share-sync.ts</files>
  <read_first>
    - src/infrastructure/share-sync.ts (after Task 1 edits — see new registry shape)
    - .planning/phases/phase-24/24-RESEARCH.md (lines 375-386 — outbound/inbound rewrite spec)
    - .planning/phases/phase-24/24-RESEARCH.md (lines 282-287 — Wave 2a keep list: secrets scanner, REMOTE_ORIGIN, bandwidth, V1 encoding)
  </read_first>
  <action>
    Rewrite the outbound and inbound flows:

    1. Replace the loadSharedRooms-based authorization gate with the private-flag filter:
       - collectShareable(graph): returns graph.nodes.filter(n => !n.private) projected to ShareableNode shape (the SEC-03 projection from Phase 15 — only id, label, workspace, embedding_id, source_uri, fetched_at; never raw text or raw vectors).
       - Apply secrets scanner (Phase 15 scanNode) on each candidate; blocked nodes drop out per SEC-02.

    2. Outbound sendShareableNode: no room parameter. Just (peerId, node) — the framed-stream envelope is { protocol_version: 5, node }. Bandwidth limiter key: peerId only.

    3. Inbound receive: parse envelope, validate protocol_version === 5 (reject with errAsync(SE.protocolMismatch(...)) otherwise — reuse the error from 24-03). For each received node:
       - Skip if origin === REMOTE_ORIGIN (the Phase 16 echo-prevention guard).
       - Scan with scanNode; blocked nodes drop.
       - Write to the global graph.ydoc via Y.Map keyed on node.id.
       - Mark origin REMOTE_ORIGIN before flushing to the graph repo (Phase 16 contract).

    4. Subscribe negotiation: outbound sends { protocol_version: 5 }; inbound validates and either accepts (continue sync stream) or rejects with protocolMismatch.

    5. Bandwidth limiter (per Phase 18): re-export createRateLimiter via Semaphore — key is peerId only. The per-room sub-limit is gone.

    6. The debounced flush from share-sync to graph repo: unchanged in mechanism; just operating on the single global Y.Doc now.

    7. The 10-peer Phase 18 integration test should still pass against this rewrite (it does not assert on room semantics) — Wave 4 surgical-edits to phase16.share-crdt and other tests handle any test-side assertions.

    Target final size: 350-500 lines (down from 869).
  </action>
  <verify>
    <automated>npx tsc --noEmit src/infrastructure/share-sync.ts 2>&1 | grep "src/infrastructure/share-sync.ts" | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - grep -cE "node\.private|n\.private" src/infrastructure/share-sync.ts returns greater than 0 (private gate is the new filter)
    - grep -cE "REMOTE_ORIGIN" src/infrastructure/share-sync.ts returns greater than 0 (echo prevention preserved)
    - grep -cE "scanNode|secretsScanner" src/infrastructure/share-sync.ts returns greater than 0 (SEC-01 preserved)
    - grep -cE "encodeStateAsUpdate|applyUpdate" src/infrastructure/share-sync.ts returns greater than 0 (V1 encoding preserved)
    - grep -cE "searchByRoom" src/infrastructure/share-sync.ts returns 0 (per-room search helpers gone)
    - wc -l src/infrastructure/share-sync.ts returns less than 510 (target: under 500 per project CLAUDE.md rule)
    - File self-compiles: tsc --noEmit error count in this file === 0
  </acceptance_criteria>
  <done>share-sync.ts is V5-clean: single global Y.Doc, private-flag gate, peer-only keys, secrets+REMOTE_ORIGIN+V1 contracts preserved.</done>
</task>

</tasks>

<verification>
- npx tsc --noEmit src/infrastructure/share-sync.ts produces no errors in this file (caller errors may persist, but those are Wave 3)
- The file is under 500 lines (project rule)
- All 4 preserved invariants are grep-detectable (REMOTE_ORIGIN, scanNode, encodeStateAsUpdate, n.private)
</verification>

<success_criteria>
1. share-sync.ts < 500 lines, V5 envelope only, private-flag gate
2. Single global Y.Doc at ~/.akashik/graph.ydoc
3. Phase 16 / Phase 18 invariants preserved (REMOTE_ORIGIN, V1 encoding, secrets scanner, bandwidth limiter)
4. No imports of share-store, system-rooms, rooms-config
</success_criteria>

<output>
After completion, create .planning/phases/phase-24/24-06-SUMMARY.md with:
- Before/after LOC count (869 → N)
- Paste of the new ShareSyncRegistry shape
- Paste of the new SubscribeRequest shape
- Confirmation that REMOTE_ORIGIN, scanNode, V1 encoding are all still wired (grep evidence inline)
</output>
