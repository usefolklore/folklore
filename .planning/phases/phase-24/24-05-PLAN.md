---
phase: phase-24
plan: 05
type: execute
wave: 1
depends_on: [24-01]
files_modified:
  - .claude/hooks/wellinformed-smart-hook.cjs
  - .claude/hooks/wellinformed-prompt-submit.cjs
  - .claude/hooks/wellinformed-mcp-pre.cjs
  - .claude/hooks/wellinformed-session-start.sh
  - .claude/hooks/wellinformed-session-capture.sh
  - .claude/hooks/wellinformed-post-fetch.cjs
autonomous: true
requirements:
  - ROOMS-DEL-08
must_haves:
  truths:
    - "smart-hook formatter renders hits with workspace, not room"
    - "prompt-submit drops the auto-touch-rooms block + drops the h.room guard"
    - "mcp-pre uses topics.join only (no rooms fallback)"
    - "session-start.sh statusline omits the rooms count + ROOMS env probe"
    - "session-capture.sh drops the rooms.json python probe and --room arg"
    - "post-fetch.cjs drops the ROOM constant and does not pass --room"
    - "All 5 hooks pass node --check / bash -n syntax checks"
  artifacts:
    - path: ".claude/hooks/wellinformed-smart-hook.cjs"
      provides: "V5 hit formatter — workspace, not room"
    - path: ".claude/hooks/wellinformed-prompt-submit.cjs"
      provides: "Hint rendering without room"
    - path: ".claude/hooks/wellinformed-post-fetch.cjs"
      provides: "Auto-save without --room flag"
  key_links:
    - from: "5 hook scripts"
      to: "Claude Code MCP integration"
      via: "stdout formatting + save-CLI arg construction"
      pattern: "renderHit / --room"
---

<objective>
Wave 1d: update the 5 wellinformed Claude Code hooks so they format hits and invoke the save CLI without any room references.

Purpose: hooks are an external boundary. If they pass --room to a v5 CLI that no longer accepts it, the boot-time auto-save flow fails. They must update atomically with the wire-protocol break.
Output: 6 hook scripts updated to the new V5 hit-formatter contract and the new save-CLI invocation contract.
</objective>

<execution_context>
@/Users/saharbarak/.claude/get-shit-done/workflows/execute-plan.md
@/Users/saharbarak/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/phase-24/24-CONTEXT.md
@.planning/phases/phase-24/24-RESEARCH.md
@.claude/hooks/wellinformed-smart-hook.cjs
@.claude/hooks/wellinformed-prompt-submit.cjs
@.claude/hooks/wellinformed-mcp-pre.cjs
@.claude/hooks/wellinformed-session-start.sh
@.claude/hooks/wellinformed-session-capture.sh
@.claude/hooks/wellinformed-post-fetch.cjs
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Update smart-hook.cjs + prompt-submit.cjs hit formatters</name>
  <files>.claude/hooks/wellinformed-smart-hook.cjs, .claude/hooks/wellinformed-prompt-submit.cjs</files>
  <read_first>
    - .claude/hooks/wellinformed-smart-hook.cjs (entire file — hit formatter line ~178)
    - .claude/hooks/wellinformed-prompt-submit.cjs (entire file — lines 164/175/233 per research)
    - .planning/phases/phase-24/24-RESEARCH.md (lines 478-490 — exact before/after)
  </read_first>
  <action>
    In smart-hook.cjs (line ~178): replace the hit formatter template literal so the bracket triple becomes workspace, age, peer. Use h.workspace fallback "-" in place of h.room fallback "?". Replace every other h.room occurrence in this file with h.workspace.

    In prompt-submit.cjs:
    1. Line ~164: drop the trailing "&& h.room" clause from the condition; keep the leading !h.summary check.
    2. Lines ~169-182: DELETE the "Pull rooms touched by these hits" block entirely. Replace with a single comment line about V5 removing auto-touch-by-room.
    3. Line ~233: hit display formatter — render the workspace fallback "-" instead of the room segment.
    4. Remove every --room arg passed to wellinformed touch / wellinformed save. Do NOT add --workspace (cwd-derived at CLI side).
  </action>
  <verify>
    <automated>node --check .claude/hooks/wellinformed-smart-hook.cjs && node --check .claude/hooks/wellinformed-prompt-submit.cjs</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "h\.room" .claude/hooks/wellinformed-smart-hook.cjs returns 0
    - grep -c "h\.room" .claude/hooks/wellinformed-prompt-submit.cjs returns 0
    - grep -c "h\.workspace" .claude/hooks/wellinformed-smart-hook.cjs returns greater than 0
    - grep -cE -- "--room" .claude/hooks/wellinformed-prompt-submit.cjs returns 0
    - grep -c "Pull rooms touched" .claude/hooks/wellinformed-prompt-submit.cjs returns 0
    - node --check passes for both files (exit 0)
  </acceptance_criteria>
  <done>Both cjs hooks render hits with workspace; no --room is passed to any sub-CLI.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Update mcp-pre.cjs + post-fetch.cjs (drop rooms fallback / ROOM constant)</name>
  <files>.claude/hooks/wellinformed-mcp-pre.cjs, .claude/hooks/wellinformed-post-fetch.cjs</files>
  <read_first>
    - .claude/hooks/wellinformed-mcp-pre.cjs (entire file — lines 201/207 per research)
    - .claude/hooks/wellinformed-post-fetch.cjs (entire file — lines 7, 34, 72 per research)
    - .planning/phases/phase-24/24-RESEARCH.md (lines 489-497 — exact diff guidance)
  </read_first>
  <action>
    In mcp-pre.cjs (lines ~201-208):
    - Drop the rooms fallback in the domain-extraction string. The line that reads roughly "rooms.join(', ') || 'local'" should become "topics.join(', ') || 'local'".
    - Remove any "rooms" array assembly that precedes that line (it's dead code after the fallback drop).

    In post-fetch.cjs:
    - Drop the "const ROOM = 'research';" constant at line ~7.
    - Drop --room and ROOM from the wellinformed save argv array (lines ~34, 72). The post-fetch save now lands as a global node.
    - The save invocation does NOT add --private (web fetches are shareable by default).
    - Replace any "land in research room" comment with: "V5: web fetches land in global graph; public (not --private)."
  </action>
  <verify>
    <automated>node --check .claude/hooks/wellinformed-mcp-pre.cjs && node --check .claude/hooks/wellinformed-post-fetch.cjs</automated>
  </verify>
  <acceptance_criteria>
    - grep -cE "rooms\.join" .claude/hooks/wellinformed-mcp-pre.cjs returns 0
    - grep -cE "const ROOM\s*=" .claude/hooks/wellinformed-post-fetch.cjs returns 0
    - grep -cE -- "--room" .claude/hooks/wellinformed-post-fetch.cjs returns 0
    - grep -cE -- "--room" .claude/hooks/wellinformed-mcp-pre.cjs returns 0
    - node --check passes for both files (exit 0)
  </acceptance_criteria>
  <done>mcp-pre uses topics-only fallback; post-fetch saves to the global graph without a room flag.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Update session-start.sh + session-capture.sh (drop rooms.json probes)</name>
  <files>.claude/hooks/wellinformed-session-start.sh, .claude/hooks/wellinformed-session-capture.sh</files>
  <read_first>
    - .claude/hooks/wellinformed-session-start.sh (entire file — lines 6, 38 per research)
    - .claude/hooks/wellinformed-session-capture.sh (entire file — lines 15, 22-23, 42 per research)
    - .planning/phases/phase-24/24-RESEARCH.md (lines 491-497 — exact diff guidance)
  </read_first>
  <action>
    In session-start.sh:
    - Drop the ROOMS environment-variable probe (line ~6) — any shell statement that reads rooms.json (via python or jq) to count rooms.
    - Drop the "${ROOM_COUNT} rooms" segment of the statusline string (line ~38).
    - Replace the statusline segment with a workspace segment IF the script already has a workspace var available; otherwise just drop the segment.
    - Drop any export ROOMS=... line.

    In session-capture.sh:
    - DELETE the python block at line ~15 that probes ~/.wellinformed/rooms.json for DEFAULT_ROOM.
    - DELETE lines ~22-23 that set DEFAULT_ROOM=... from that probe.
    - On line ~42 (the save invocation), remove the --room "$DEFAULT_ROOM" argument pair entirely.
    - The save invocation now runs without any room/workspace flag — the CLI's detectWorkspace handles it from cwd.

    Both files: keep shebang, keep all error handling that is not room-related.
  </action>
  <verify>
    <automated>bash -n .claude/hooks/wellinformed-session-start.sh && bash -n .claude/hooks/wellinformed-session-capture.sh</automated>
  </verify>
  <acceptance_criteria>
    - grep -c "rooms.json" .claude/hooks/wellinformed-session-start.sh returns 0
    - grep -c "rooms.json" .claude/hooks/wellinformed-session-capture.sh returns 0
    - grep -cE "DEFAULT_ROOM|ROOM_COUNT|export ROOMS" .claude/hooks/wellinformed-session-start.sh returns 0
    - grep -cE "DEFAULT_ROOM|--room" .claude/hooks/wellinformed-session-capture.sh returns 0
    - bash -n passes (syntax-check) for both files
  </acceptance_criteria>
  <done>Both shell hooks no longer touch rooms.json or pass --room to the save CLI.</done>
</task>

</tasks>

<verification>
- grep -rnE "h\.room|--room\b|rooms\.json|DEFAULT_ROOM|ROOM_COUNT|const ROOM\s*=|rooms\.join" .claude/hooks/wellinformed-*.cjs .claude/hooks/wellinformed-*.sh returns 0 lines
- All 4 cjs files pass node --check (exit 0)
- All 2 sh files pass bash -n (exit 0)
</verification>

<success_criteria>
1. Hit formatters render workspace (or "-") in place of room
2. No hook script passes --room to any sub-CLI
3. No hook script reads ~/.wellinformed/rooms.json
4. All hooks pass syntax checks (node --check / bash -n)
</success_criteria>

<output>
After completion, create .planning/phases/phase-24/24-05-SUMMARY.md with:
- Listed changes per hook (one-liner each)
- Sample stdout from each cjs hook fed a fake hits-array (verifies the new formatter doesn't crash)
- LOC delta across all 6 hook files
</output>
