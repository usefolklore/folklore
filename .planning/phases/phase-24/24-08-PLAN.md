---
phase: phase-24
plan: 08
type: execute
wave: 2
depends_on: [24-02, 24-03]
files_modified:
  - src/mcp/server.ts
autonomous: true
requirements:
  - ROOMS-DEL-01
  - ROOMS-DEL-05
must_haves:
  truths:
    - "mcp/server.ts does not register the list_rooms tool"
    - "mcp/server.ts does not register the find_tunnels tool"
    - "mcp/server.ts does not register the trigger_room tool"
    - "MCP tool count is 13 (down from 16)"
    - "search MCP tool has no `room` parameter"
    - "federated_search MCP tool has no `room` parameter"
    - "search_recent MCP tool has no `room` parameter"
    - "entity-first-lookup MCP tool (or its renamed equivalent) has no `room` parameter"
    - "Response formatting does not emit a `room` field on hits"
  artifacts:
    - path: "src/mcp/server.ts"
      provides: "13-tool MCP surface, no room param on any tool"
      max_lines: 1100
  key_links:
    - from: "MCP server"
      to: "Claude Code consumer"
      via: "tool registration list"
      pattern: "registerTool|server\\.tool"
---

<objective>
Wave 2c: rewrite mcp/server.ts to drop 3 tools (list_rooms, find_tunnels, trigger_room) and strip the room parameter from the 4 remaining tools that accepted it.

Purpose: this is an EXTERNAL boundary — Claude Code and other MCP consumers depend on the tool registry. Tool count goes 16 → 13. The release note for Phase 24 must call this out as a breaking MCP API change.

Output: mcp/server.ts compiles against V5 graph + V5 wire types. The Phase 17 C2 invariant "MCP tool count: 14" → was bumped 15 (Phase 19) → 16 (Phase 20) → now 13 (Phase 24).
</objective>

<execution_context>
@/Users/saharbarak/.claude/get-shit-done/workflows/execute-plan.md
@/Users/saharbarak/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/phase-24/24-CONTEXT.md
@.planning/phases/phase-24/24-RESEARCH.md
@src/mcp/server.ts
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Drop 3 tools (list_rooms, find_tunnels, trigger_room) from MCP registry</name>
  <files>src/mcp/server.ts</files>
  <read_first>
    - src/mcp/server.ts (entire file — 1175 lines; need full picture before deletion)
    - .planning/phases/phase-24/24-RESEARCH.md (lines 290-298 — Wave 2c exact line numbers)
    - .planning/phases/phase-24/24-RESEARCH.md (line 629 — observability note: this is breaking MCP API)
  </read_first>
  <action>
    In mcp/server.ts:

    1. Find the tool registration for `list_rooms`. Delete its registration block AND its handler function definition.

    2. Find the tool registration for `find_tunnels`. Delete its registration block AND its handler function definition.

    3. Find the tool registration for `trigger_room`. Delete its registration block AND its handler function definition.

    4. Drop imports that only those handlers used:
       - `roomSummary` (no other consumers)
       - `nodesInRoomFn` dynamic import (line ~833)
       - `slugifyRoomName` (line ~597)

    5. Drop the `belongsToSystemRoom`, `nodesInSystemRoom`, `TOOLSHED`, `RESEARCH`, `ORACLE` imports (system-rooms.ts is deleted). The `oracle` substrate (Open Question 2) is resolved by deletion: oracle Q&A becomes a source_uri filter, not a system-room membership.

    6. Verify the file's tool-count-emitting code (if any — Phase 17 had a "tool count" log line at startup) now emits 13.

    7. Do NOT change any tool other than the 4 specified in Task 2 below — keep the rest of the file's behavior intact.
  </action>
  <verify>
    <automated>bash -c "grep -c '\"list_rooms\"\\|\"find_tunnels\"\\|\"trigger_room\"' src/mcp/server.ts"</automated>
  </verify>
  <acceptance_criteria>
    - grep -c '"list_rooms"' src/mcp/server.ts returns 0
    - grep -c '"find_tunnels"' src/mcp/server.ts returns 0
    - grep -c '"trigger_room"' src/mcp/server.ts returns 0
    - grep -cE "roomSummary|nodesInRoomFn|slugifyRoomName" src/mcp/server.ts returns 0
    - grep -cE "from.*system-rooms|TOOLSHED|belongsToSystemRoom|nodesInSystemRoom" src/mcp/server.ts returns 0
  </acceptance_criteria>
  <done>3 tools gone; system-rooms imports gone; oracle (Open Question 2) resolved by deletion.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Strip room param from search / federated_search / search_recent / entity-first-lookup; drop room from response format</name>
  <files>src/mcp/server.ts</files>
  <read_first>
    - src/mcp/server.ts (after Task 1 edits)
    - .planning/phases/phase-24/24-RESEARCH.md (lines 293-296 — exact line numbers for strip targets)
  </read_first>
  <action>
    In mcp/server.ts, for each of these 4 tools:

    1. `search` (line ~91): drop the `room?: string` field from the Zod input schema. Drop any `if (input.room) ...` branching in the handler.

    2. `federated_search` (line ~311): drop `room?: string` from the input schema; drop any room branching. The fan-out request envelope already has no room (per 24-03).

    3. `search_recent` (line ~159): drop `room?: string` from input schema; drop room branching.

    4. `entity-first-lookup` (line ~447): drop `room?: string` from input schema; drop room branching.

    5. Response formatting (line ~193 — the helper that shapes hits for MCP response): drop the `room` field from the output object. Add `workspace?: string` (optional) and `private: boolean` (always) per the new GraphNode shape.

    6. Add `--workspace <slug>` support to `search` / `federated_search`? Optional — research says workspace is a CLI-side concern, but MCP could expose it. RECOMMENDATION: do NOT add a workspace param to MCP tools yet; MCP consumers are usually IDE/agent-level and operate cross-workspace by default. Defer to Phase 25+ if MCP consumers request it. Document this decision in 24-08-SUMMARY.

    7. Strip any `--room` related help text / description strings from the tool registration metadata.
  </action>
  <verify>
    <automated>bash -c "grep -cE 'room\\??:\\s*z\\.string|room\\??:\\s*string' src/mcp/server.ts"</automated>
  </verify>
  <acceptance_criteria>
    - grep -cE "room\\??:\\s*z\\.string" src/mcp/server.ts returns 0
    - grep -cE "input\\.room\\b|args\\.room\\b" src/mcp/server.ts returns 0
    - wc -l src/mcp/server.ts returns less than 1100 (we drop ~80-200 lines from the original 1175)
    - npx tsc --noEmit src/mcp/server.ts produces 0 errors IN THIS FILE (caller errors are Wave 3)
  </acceptance_criteria>
  <done>4 tools have no room param; response formatter emits workspace+private; tool count is 13.</done>
</task>

</tasks>

<verification>
- grep -cE '"list_rooms"|"find_tunnels"|"trigger_room"' src/mcp/server.ts returns 0
- grep -cE "room\\??:" src/mcp/server.ts returns 0 (all room params gone)
- npx tsc --noEmit src/mcp/server.ts produces no errors in this file
- File still registers ≥ 13 MCP tools (verify by `grep -cE "registerTool\\(|server\\.tool\\(" src/mcp/server.ts` returns ≥ 13)
</verification>

<success_criteria>
1. 3 tools removed: list_rooms, find_tunnels, trigger_room
2. 4 tools stripped of room param: search, federated_search, search_recent, entity-first-lookup
3. Response format emits workspace/private, not room
4. Tool count = 13 (decision: do not add --workspace MCP param yet; defer to user feedback)
</verification>

<success_criteria>
1. MCP tool count is 13
2. Zero `room` parameters on any tool
3. Zero imports of deleted modules (system-rooms, share-store, rooms)
4. File still under the project rule (<500 lines is impossible for this file — it's an MCP registry; aim for under 1100)
</success_criteria>

<output>
After completion, create .planning/phases/phase-24/24-08-SUMMARY.md with:
- Tool count before/after (16 → 13)
- Names of removed tools (3)
- Names of tools with room param stripped (4)
- Decision: no --workspace MCP param added (defer to user feedback)
- Note for Phase 24 release notes: BREAKING MCP API change
</output>
