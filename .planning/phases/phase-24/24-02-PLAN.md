---
phase: phase-24
plan: 02
type: execute
wave: 1
depends_on: [24-01]
files_modified:
  - src/domain/rooms.ts
  - src/domain/system-rooms.ts
  - src/infrastructure/rooms-config.ts
  - src/infrastructure/share-store.ts
  - src/cli/commands/room.ts
  - src/cli/index.ts
  - tests/phase4.rooms.test.ts
  - tests/phase1.graph-rooms.test.ts
  - tests/phase36.system-rooms.test.ts
  - tests/phase37.share-picker.test.ts
  - src/cli/tui/share-picker-tty.ts
autonomous: true
requirements:
  - ROOMS-DEL-01
  - ROOMS-DEL-02
  - ROOMS-DEL-03
must_haves:
  truths:
    - "src/domain/rooms.ts no longer exists on disk"
    - "src/domain/system-rooms.ts no longer exists on disk"
    - "src/infrastructure/rooms-config.ts no longer exists on disk"
    - "src/infrastructure/share-store.ts no longer exists on disk"
    - "src/cli/commands/room.ts no longer exists on disk"
    - "src/cli/index.ts has no `room` dispatch case"
    - "The 3 phase tests (phase1.graph-rooms, phase4.rooms, phase36.system-rooms) no longer exist"
    - "share-picker TUI is deleted (planner decision per Open Question 1)"
  artifacts:
    - path: ".gone-files-list.txt"
      provides: "Captured list of deleted file paths for audit trail"
  key_links:
    - from: "src/cli/index.ts"
      to: "removed `room` subcommand"
      via: "dispatcher case deletion"
      pattern: "case 'room'"
---

<objective>
Wave 1a: pure deletions. Remove the 5 doomed source files + 3 phase tests + share-picker (planner decision). Wipe the `room` dispatch case from cli/index.ts.

**Open Question 1 RESOLVED:** Delete `share-picker.ts`, `share-picker-tty.ts`, and `phase37.share-picker.test.ts`. Without rooms there is nothing to pick; sharing becomes `akashik share <peer>` direct command. Rationale: research recommendation accepted (24-RESEARCH.md line 885).

Purpose: collapse the import surface of room-vocabulary so subsequent edits cannot accidentally reach into deleted modules. Causes compile failures everywhere room-types were imported — those failures are resolved by Wave 3 surgical edits.
Output: 11 files removed from the tree; cli/index.ts no longer dispatches `room`.
</objective>

<execution_context>
@/Users/saharbarak/.claude/get-shit-done/workflows/execute-plan.md
@/Users/saharbarak/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/phase-24/24-CONTEXT.md
@.planning/phases/phase-24/24-RESEARCH.md
@src/cli/index.ts
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Delete 5 source files + 3 phase tests + share-picker TUI</name>
  <files>
    src/domain/rooms.ts,
    src/domain/system-rooms.ts,
    src/infrastructure/rooms-config.ts,
    src/infrastructure/share-store.ts,
    src/cli/commands/room.ts,
    tests/phase4.rooms.test.ts,
    tests/phase1.graph-rooms.test.ts,
    tests/phase36.system-rooms.test.ts,
    src/cli/tui/share-picker-tty.ts,
    tests/phase37.share-picker.test.ts
  </files>
  <read_first>
    - .planning/phases/phase-24/24-CONTEXT.md (lines 55-65 — locked file-deletion list)
    - .planning/phases/phase-24/24-RESEARCH.md (lines 256-261 — Wave 1a delete list)
    - .planning/phases/phase-24/24-RESEARCH.md (line 885-889 — Open Question 1 on share-picker, recommendation: delete)
  </read_first>
  <action>
    Run via Bash, sequentially (one git rm per line, capture to a list):

    ```bash
    git rm src/domain/rooms.ts
    git rm src/domain/system-rooms.ts
    git rm src/infrastructure/rooms-config.ts
    git rm src/infrastructure/share-store.ts
    git rm src/cli/commands/room.ts
    git rm tests/phase4.rooms.test.ts
    git rm tests/phase1.graph-rooms.test.ts
    git rm tests/phase36.system-rooms.test.ts
    ```

    Then for share-picker (Open Question 1 — DELETE):
    ```bash
    git rm src/cli/tui/share-picker-tty.ts  # if exists
    git rm tests/phase37.share-picker.test.ts  # if exists
    ```

    Use `git rm` (not `rm`) so deletions are staged.

    Note: `src/domain/share-picker.ts` (166 lines) is NOT deleted here — it has a different ownership (Wave 2b deals with share.ts/unshare.ts surface and decides share-picker's domain layer fate). If share-picker.ts is purely a TUI feeder with no other consumers, Wave 2b's planner deletes it then. Wave 1a only takes the TTY adapter + test.

    If any `git rm` fails because the file doesn't exist (e.g., share-picker-tty.ts was already gone), use `git rm --ignore-unmatch` or skip and note it.

    Do NOT touch any other files. Do NOT attempt to fix the compile errors that result from these deletions — those are Waves 2/3 problems.
  </action>
  <verify>
    <automated>test ! -f src/domain/rooms.ts && test ! -f src/domain/system-rooms.ts && test ! -f src/infrastructure/rooms-config.ts && test ! -f src/infrastructure/share-store.ts && test ! -f src/cli/commands/room.ts && test ! -f tests/phase4.rooms.test.ts && test ! -f tests/phase1.graph-rooms.test.ts && test ! -f tests/phase36.system-rooms.test.ts && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `test ! -f src/domain/rooms.ts` exits 0
    - `test ! -f src/domain/system-rooms.ts` exits 0
    - `test ! -f src/infrastructure/rooms-config.ts` exits 0
    - `test ! -f src/infrastructure/share-store.ts` exits 0
    - `test ! -f src/cli/commands/room.ts` exits 0
    - `test ! -f tests/phase4.rooms.test.ts` exits 0
    - `test ! -f tests/phase1.graph-rooms.test.ts` exits 0
    - `test ! -f tests/phase36.system-rooms.test.ts` exits 0
    - `git status --short | grep -c "^D "` returns ≥ 8 (8 confirmed deletes; 2 more if share-picker TUI files existed)
  </acceptance_criteria>
  <done>11 files (or 8 + however many share-picker TUI files existed) removed from the working tree and staged via git rm.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Remove `room` dispatch case from cli/index.ts</name>
  <files>src/cli/index.ts</files>
  <read_first>
    - src/cli/index.ts (entire file — small dispatcher; need to see the Record-key dispatch pattern referenced in STATE.md Phase 19 decision)
    - src/cli/runtime.ts (just to confirm runtime no longer needs the room import — Wave 1c handles runtime; this task just removes the dispatch case)
  </read_first>
  <action>
    In `src/cli/index.ts`:

    1. Find the dispatcher Record (per Phase 19 pattern — `const commands: Record<string, (...) => ...> = { ... }`). Remove the `room: roomCommand` (or whichever key dispatches to `src/cli/commands/room.ts`) entry.

    2. Remove the `import { roomCommand } from './commands/room.js'` line (or equivalent import name).

    3. If there is a help/usage string that lists `room`, remove that listing too.

    4. Do NOT yet remove the `share-picker` import (that's Wave 2b). Do NOT yet add the `migrate` dispatch (that's Wave 4).

    5. Leave all other dispatcher entries intact.
  </action>
  <verify>
    <automated>! grep -E "from\s+['\"]\\./commands/room(\\.js)?['\"]" src/cli/index.ts && ! grep -E "'room'\\s*:" src/cli/index.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE "from\s+['\"]\\./commands/room(\\.js)?['\"]" src/cli/index.ts` returns 0
    - `grep -cE "'room'\\s*:" src/cli/index.ts` returns 0
    - `grep -cE "roomCommand" src/cli/index.ts` returns 0
    - File diff shows only deletions (no additions): `git diff src/cli/index.ts | grep -c "^+[^+]"` returns 0
  </acceptance_criteria>
  <done>No code path in cli/index.ts can route to a deleted room command.</done>
</task>

</tasks>

<verification>
- `ls src/domain/rooms.ts src/domain/system-rooms.ts src/infrastructure/rooms-config.ts src/infrastructure/share-store.ts src/cli/commands/room.ts 2>/dev/null | wc -l` returns 0
- `grep -rn "from.*\.\./domain/rooms\.js\|from.*\.\./domain/system-rooms\.js" src/ | wc -l` may still be > 0 (those are Wave 3 fix targets) — that is EXPECTED for Wave 1a
- The build is broken on purpose; `tsc --noEmit` is dirty until Wave 3 finishes
</verification>

<success_criteria>
1. All 8 confirmed-doomed files are deleted via git rm
2. share-picker TUI + test deleted (Open Question 1 resolved: delete)
3. cli/index.ts no longer dispatches `room`
4. Wave 1b, 1c, 1d can now proceed in parallel
</success_criteria>

<output>
After completion, create `.planning/phases/phase-24/24-02-SUMMARY.md` listing the deleted file paths + line counts before deletion (for the post-phase LOC delta report).
</output>
