---
phase: phase-24
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - src/domain/graph.ts
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - ROOMS-DEL-04
must_haves:
  truths:
    - "GraphNode type no longer has a `room` field"
    - "GraphNode type has required `private: boolean` field"
    - "GraphNode type has optional `workspace?: string` field"
    - "REQUIREMENTS.md contains ROOMS-DEL-01..08 entries"
    - "tsc --noEmit produces a catalogued list of consumer errors (the blast radius)"
  artifacts:
    - path: "src/domain/graph.ts"
      provides: "FolkloreNodeFields with workspace + private, no room"
      contains: "private: boolean"
    - path: ".planning/REQUIREMENTS.md"
      provides: "ROOMS-DEL-01..08 added under new 'Rooms Deletion (V5)' section"
      contains: "ROOMS-DEL-04"
  key_links:
    - from: "src/domain/graph.ts"
      to: "every downstream consumer"
      via: "compile errors from removed `room` field"
      pattern: "tsc --noEmit"
---

<objective>
Wave 0 wedge: land the schema change that makes every subsequent edit compiler-driven.

Purpose: Removing `room?: Room` from `FolkloreNodeFields` makes `tsc --noEmit` produce the definitive blast radius — every error becomes a Wave 1/2/3 task. This is the only plan in Wave 0 and BLOCKS all subsequent waves.
Output: Updated `src/domain/graph.ts` + REQUIREMENTS.md with the 8 new requirement IDs. A catalogued `tsc --noEmit` error report committed to the plan's summary file.
</objective>

<execution_context>
@/Users/saharbarak/.claude/get-shit-done/workflows/execute-plan.md
@/Users/saharbarak/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/phase-24/24-CONTEXT.md
@.planning/phases/phase-24/24-RESEARCH.md
@.planning/debates/rooms-deprecation-2026-05-27/SYNTHESIS.md
@src/domain/graph.ts
@.planning/REQUIREMENTS.md

<interfaces>
<!-- Current shape (graph.ts lines 31-53) — to be replaced -->

```typescript
export type Room = string;
export type Wing = string;

export interface FolkloreNodeFields {
  readonly room?: Room;
  readonly wing?: Wing;
  readonly source_uri?: string;
  readonly fetched_at?: string;
  readonly embedding_id?: string;
}
```

<!-- Functions on graph.ts to remove: -->
- `nodesInRoom` (line ~213)
- `roomFilter` (line ~430)
- `TraversalOptions.room` (line ~132-134)
- BFS/DFS room guards (`if (!room) return true`, lines ~232, ~262)

<!-- Target shape: -->

```typescript
export type Wing = string;  // keep — still used by sources

export interface FolkloreNodeFields {
  readonly wing?: Wing;
  readonly source_uri?: string;
  readonly fetched_at?: string;
  readonly embedding_id?: string;
  /** Optional workspace tag — populated from cwd's git toplevel basename at write time.
   *  LOCAL-ONLY. Never enters federation wire envelope. */
  readonly workspace?: string;
  /** Sharing gate. True = never federates. Defaults to false at write time. */
  readonly private: boolean;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add ROOMS-DEL-01..08 requirements to REQUIREMENTS.md</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (current state — see existing structure: per-section ID lists, status table at bottom)
    - .planning/phases/phase-24/24-CONTEXT.md (lines 207-218 — the 8 ROOMS-DEL-* requirements with descriptions, verbatim)
  </read_first>
  <action>
    Insert a new section after the "Session Persistence" section (currently ends ~line 78) titled `### Rooms Deletion (V5 Wire-Protocol Break)`. Copy the 8 ROOMS-DEL-* requirements verbatim from 24-CONTEXT.md lines 207-218 using the same `- [ ] **ID**: Description` format as other sections (unchecked — they ship at phase end).

    Update the "Traceability" table (~line 98) to add row: `| ROOMS-DEL-01..08 | Phase 24 | Pending |`.

    Update "Coverage" totals (~line 109): 38 → 46 requirements, mapped to phases: 38 → 46.

    Do NOT modify any other section. Do NOT alter existing requirement statuses.
  </action>
  <verify>
    <automated>grep -c "ROOMS-DEL-0[1-8]" .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "ROOMS-DEL-0[1-8]" .planning/REQUIREMENTS.md` returns 8
    - `grep "### Rooms Deletion" .planning/REQUIREMENTS.md` returns 1 hit
    - `grep "ROOMS-DEL-01..08" .planning/REQUIREMENTS.md` returns ≥1 hit (traceability row)
    - File diff shows only additions, no deletions of existing requirements (run `git diff --stat .planning/REQUIREMENTS.md` shows insertions-only)
  </acceptance_criteria>
  <done>REQUIREMENTS.md contains all 8 ROOMS-DEL-* IDs in a dedicated section with traceability row.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Excise room from GraphNode schema; add workspace + private</name>
  <files>src/domain/graph.ts</files>
  <read_first>
    - src/domain/graph.ts (entire file — need to see FolkloreNodeFields interface, nodesInRoom, roomFilter, TraversalOptions, BFS/DFS guards, fromJson validator)
    - .planning/phases/phase-24/24-RESEARCH.md (lines 330-365 — exact before/after target shape)
  </read_first>
  <action>
    In `src/domain/graph.ts`:

    1. **Drop the `Room` type alias.** Search for `export type Room = string;` — delete the line. Keep `export type Wing = string;`.

    2. **Update `FolkloreNodeFields` interface:**
       - Remove `readonly room?: Room;`
       - Add `readonly workspace?: string;` with JSDoc: `/** Optional workspace tag — populated from cwd's git toplevel basename at write time. LOCAL-ONLY. Never enters federation wire envelope. */`
       - Add `readonly private: boolean;` (REQUIRED, not optional) with JSDoc: `/** Sharing gate. True = never federates. Defaults to false at write time. */`

    3. **Remove `nodesInRoom` function** (around line 213). Find `export const nodesInRoom = ` or `export function nodesInRoom(` — delete the whole function block.

    4. **Remove `roomFilter` function** (around line 430). Find `export const roomFilter = ` or similar — delete the whole function block.

    5. **Update `TraversalOptions`:** Find the type/interface (around line 132). Drop the `room?: Room` field.

    6. **Update BFS/DFS guards:** Find `if (!room) return true` (lines ~232, ~262 per research). Remove the entire guard branch since the room param is gone from TraversalOptions.

    7. **Keep `Readonly<Record<string, unknown>>` extension on `GraphNode`.** This is intentional — it allows v4 JSON to round-trip without crashing (the migrate command and doctor command rely on this).

    8. Do NOT add backward-compat shims. Do NOT add `Room` re-export. Hard cutover.

    After edits, run `npx tsc --noEmit 2>&1 | tee /tmp/phase24-wave0-blastradius.txt`. Commit `/tmp/phase24-wave0-blastradius.txt` content (or its line count) into the plan summary as documented evidence.
  </action>
  <verify>
    <automated>grep -c "room?:\s*Room\|room:\s*Room\|export type Room\|nodesInRoom\|roomFilter" src/domain/graph.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "room?:\s*Room\|room:\s*Room\|export type Room\|nodesInRoom\|roomFilter" src/domain/graph.ts` returns 0
    - `grep -c "readonly workspace?: string" src/domain/graph.ts` returns 1
    - `grep -c "readonly private: boolean" src/domain/graph.ts` returns 1
    - `grep -c "export type Wing" src/domain/graph.ts` returns 1 (Wing kept)
    - `npx tsc --noEmit` exits NON-ZERO (this is expected — the blast radius is the input for Wave 3); the error count is captured in /tmp/phase24-wave0-blastradius.txt
    - File size delta: 4-8 lines net (5 lines removed, 2 added with JSDoc) — verify with `git diff --stat src/domain/graph.ts`
  </acceptance_criteria>
  <done>graph.ts schema has the V5 shape; the compiler produces a definitive list of consumer errors that downstream plans will resolve.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit 2>&1 | wc -l` > 50 lines of errors (the wedge worked — consumers are broken and need fixing)
- `grep -rn "GraphNode" src/domain/graph.ts` still shows the type is exported
- `grep "ROOMS-DEL-0" .planning/REQUIREMENTS.md | wc -l` returns ≥ 9 (8 requirements + traceability row)
</verification>

<success_criteria>
1. graph.ts has no `Room` type, no `nodesInRoom`, no `roomFilter`, no `TraversalOptions.room`
2. graph.ts has `workspace?: string` (optional) and `private: boolean` (required)
3. REQUIREMENTS.md lists ROOMS-DEL-01 through ROOMS-DEL-08
4. /tmp/phase24-wave0-blastradius.txt captures the compile-error list for Wave 1-3 planning
</success_criteria>

<output>
After completion, create `.planning/phases/phase-24/24-01-SUMMARY.md` with:
- Commit hash(es)
- Final shape of FolkloreNodeFields (paste the interface)
- Line count of tsc --noEmit errors (the blast radius metric)
- Any decisions deviating from the plan (none expected)
</output>
