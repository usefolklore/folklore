---
phase: phase-24
plan: 07
type: execute
wave: 2
depends_on: [24-02, 24-03, 24-06]
files_modified:
  - src/cli/commands/share.ts
  - src/cli/commands/unshare.ts
  - src/domain/share-picker.ts
  - src/cli/index.ts
autonomous: true
requirements:
  - ROOMS-DEL-03
must_haves:
  truths:
    - "akashik share <peer> shares all nodes with private === false; no --room flag exists"
    - "akashik share has no `room` subcommand"
    - "akashik unshare <peer> is peer-removal only; does not modify any rooms file"
    - "src/domain/share-picker.ts is deleted (or rewritten to peer-only, planner DECISION: delete per Open Question 1)"
    - "cli/index.ts has no `share-picker` import"
  artifacts:
    - path: "src/cli/commands/share.ts"
      provides: "share <peer> command — no rooms"
      max_lines: 150
    - path: "src/cli/commands/unshare.ts"
      provides: "unshare <peer> command — peer-removal only"
      max_lines: 60
  key_links:
    - from: "share.ts"
      to: "share-sync.ts.collectShareable"
      via: "private-flag filter"
      pattern: "collectShareable|n\\.private"
---

<objective>
Wave 2b: rewrite share.ts and unshare.ts to peer-only commands. DELETE share-picker.ts (Open Question 1 resolved in plan 02 — TUI deleted; this plan completes the deletion by removing the domain layer too).

Purpose: the share CLI surface was ceremony-heavy (--room flag, audit-on-room, room subcommand). V5 collapses it to `akashik share <peer>` which shares every non-private node. unshare becomes peer-removal only. The share-picker (which projected rooms for interactive selection) has no purpose in a flat-namespace world.

Output: share.ts (~100 lines, down from 331), unshare.ts (~30 lines, down from 61), share-picker.ts DELETED.
</objective>

<execution_context>
@/Users/saharbarak/.claude/get-shit-done/workflows/execute-plan.md
@/Users/saharbarak/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/phase-24/24-CONTEXT.md
@.planning/phases/phase-24/24-RESEARCH.md
@src/cli/commands/share.ts
@src/cli/commands/unshare.ts
@src/domain/share-picker.ts
@src/cli/index.ts
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Rewrite share.ts to peer-only command</name>
  <files>src/cli/commands/share.ts</files>
  <read_first>
    - src/cli/commands/share.ts (entire file — 331 lines)
    - src/cli/commands/peer.ts (peer command pattern — match its style for arg parsing)
    - .planning/phases/phase-24/24-RESEARCH.md (lines 286-289 — share.ts rewrite spec)
    - .planning/phases/phase-24/24-CONTEXT.md (lines 71-75 — what to rewrite)
  </read_first>
  <action>
    Rewrite share.ts:

    1. Drop the --room flag and all its parsing.
    2. Drop the `room` subcommand (line ~117 roomCmd per research).
    3. Drop the audit-on-room subroutine.
    4. Drop imports: loadSharedRooms, mutateSharedRooms (share-store.js deleted), Room / RoomId from rooms.ts (deleted).
    5. New command shape: `akashik share <peer>` where <peer> is a peerId (matching the peer command's parser).
    6. Behavior: connect to the peer (via the existing libp2p transport from peer-transport.ts), then invoke share-sync's outbound publish path on every node where private === false (collectShareable from 24-06). Bandwidth + secrets gates from Phase 15/18 still apply.
    7. Keep the existing audit-style output (list nodes that would share, count blocked-by-secrets, count private-skipped). Drop the "shared rooms" / "shared-rooms.json" sections.
    8. Drop the keep "share audit" subcommand — replaced with default-to-audit-then-confirm flow. The audit-as-CLI-mode (--audit-only flag) can remain if simple to keep; planner discretion.
    9. The CLI's exported entrypoint signature: shareCommand(args, deps) where deps now lacks the rooms config slot.
    10. Update help text to drop room mentions.

    Target: ~80-150 lines (down from 331).
  </action>
  <verify>
    <automated>npx tsc --noEmit src/cli/commands/share.ts 2>&1 | grep "src/cli/commands/share.ts" | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - grep -cE "from.*share-store|loadSharedRooms|mutateSharedRooms" src/cli/commands/share.ts returns 0
    - grep -cE "from.*rooms\.js|RoomId\b|Room\b" src/cli/commands/share.ts returns 0
    - grep -cE -- "--room" src/cli/commands/share.ts returns 0
    - grep -cE "roomCmd\\b|room subcommand" src/cli/commands/share.ts returns 0
    - wc -l src/cli/commands/share.ts returns less than 160
    - file self-compiles: npx tsc --noEmit src/cli/commands/share.ts has 0 errors IN THIS FILE
  </acceptance_criteria>
  <done>share.ts is peer-only; under 150 lines; no rooms vocabulary anywhere.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Rewrite unshare.ts to peer-removal only + DELETE share-picker.ts</name>
  <files>src/cli/commands/unshare.ts, src/domain/share-picker.ts, src/cli/index.ts</files>
  <read_first>
    - src/cli/commands/unshare.ts (entire file — 61 lines)
    - src/domain/share-picker.ts (entire file — 166 lines; understand its current exports before deleting)
    - src/cli/index.ts (current state after 24-02 — verify the dispatch table; confirm where share-picker is referenced)
    - .planning/phases/phase-24/24-CONTEXT.md (lines 73-74 — what to rewrite for unshare)
  </read_first>
  <action>
    Rewrite unshare.ts:

    1. Drop imports: loadSharedRooms, mutateSharedRooms, removeSharedRoom (all from deleted share-store.js).
    2. Drop the rooms-related code paths entirely.
    3. New command shape: `akashik unshare <peer>` — invokes the peer-transport.ts disconnect + removes the peer from peers.json (the existing PeerRecord store).
    4. This may make unshare a near-alias of `peer remove`. Acceptable; the user-facing distinction (unshare = stop sharing with peer; peer remove = remove peer from registry) is semantic.
    5. Target: ~20-40 lines (down from 61).

    Delete share-picker.ts:

    6. Open Question 1 RESOLVED (24-02 plan). Without rooms there is nothing to pick. Run `git rm src/domain/share-picker.ts`.
    7. Verify no remaining importers via grep before deletion. If share-picker.ts is still imported by share.ts after 24-07 Task 1, that import must be removed first.

    Clean cli/index.ts:

    8. Verify no `from './tui/share-picker-tty.js'` import remains (24-02 deleted the TTY adapter; this also drops the domain layer).
    9. Verify no `from '../domain/share-picker.js'` import remains.
    10. If any remain, drop them.
  </action>
  <verify>
    <automated>test ! -f src/domain/share-picker.ts && grep -cE "share-picker" src/cli/index.ts src/cli/commands/share.ts src/cli/commands/unshare.ts</automated>
  </verify>
  <acceptance_criteria>
    - test ! -f src/domain/share-picker.ts exits 0 (file gone)
    - grep -cE "share-picker" src/cli/index.ts returns 0
    - grep -cE "from.*share-store|loadSharedRooms|mutateSharedRooms|removeSharedRoom" src/cli/commands/unshare.ts returns 0
    - wc -l src/cli/commands/unshare.ts returns less than 50
    - npx tsc --noEmit src/cli/commands/unshare.ts produces 0 errors IN unshare.ts itself
  </acceptance_criteria>
  <done>unshare.ts is peer-only; share-picker.ts deleted; cli/index.ts has no dangling share-picker references.</done>
</task>

</tasks>

<verification>
- All three files (share.ts, unshare.ts) are under 150 / 50 lines respectively
- share-picker.ts is gone from disk
- No file in src/ imports share-picker.ts or share-picker-tty.ts (grep returns 0)
- npx tsc --noEmit shows no errors IN these two files (caller errors elsewhere are still Wave 3)
</verification>

<success_criteria>
1. share.ts: peer-only, no --room, no rooms imports, under 150 lines
2. unshare.ts: peer-removal only, no rooms imports, under 50 lines
3. share-picker.ts deleted
4. cli/index.ts has no dangling share-picker imports
</success_criteria>

<output>
After completion, create .planning/phases/phase-24/24-07-SUMMARY.md with:
- Before/after LOC for share.ts and unshare.ts
- Confirmation share-picker.ts is gone (ls + git status output)
- Decision note: Open Question 1 fully resolved (TUI + domain layer + test deleted across 24-02 + 24-07)
</output>
