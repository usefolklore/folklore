---
phase: phase-24
plan: 12
type: execute
wave: 4
depends_on: [24-09, 24-10, 24-11]
files_modified:
  - tests/phase24.rooms-deleted.test.ts
  - tests/phase16.share-crdt.test.ts
  - tests/phase34.save-note.test.ts
  - tests/phase6.daemon.test.ts
  - tests/phase38.oracle.test.ts
  - tests/consolidator.test.ts
  - tests/phase17.federated-search.test.ts
  - tests/phase18.production-net.test.ts
  - tests/phase19.codebase-indexing.test.ts
  - tests/phase20.sessions.test.ts
  - tests/phase15.peer-foundation.test.ts
  - tests/phase35.entity-extraction.test.ts
  - tests/phase33.recall-rerank.test.ts
autonomous: true
requirements:
  - ROOMS-DEL-01
  - ROOMS-DEL-02
  - ROOMS-DEL-03
  - ROOMS-DEL-04
  - ROOMS-DEL-05
  - ROOMS-DEL-06
  - ROOMS-DEL-07
  - ROOMS-DEL-08
must_haves:
  truths:
    - "tests/phase24.rooms-deleted.test.ts exists, covers all 8 ROOMS-DEL-* requirements"
    - "phase24 test file has at least 30 tests across at least 8 describe groups"
    - "The 3 deleted phase tests (phase1, phase4, phase36) are gone (verified in 24-02)"
    - "phase16.share-crdt.test.ts has room assertions replaced with private-flag assertions"
    - "phase37.share-picker.test.ts is deleted (verified in 24-02)"
    - "All other modified tests pass (the ~10 remaining surgical-edit tests)"
    - "npm test exits 0 (full suite green)"
    - "Phase 17 C2 invariant updated: MCP tool count 16 → 13"
  artifacts:
    - path: "tests/phase24.rooms-deleted.test.ts"
      provides: "Phase 24 acceptance test — V5 cutover regression lock"
      min_lines: 400
  key_links:
    - from: "phase24.rooms-deleted.test.ts"
      to: "all 8 ROOMS-DEL-* requirements"
      via: "describe groups (1 per requirement OR semantically grouped 9 groups per research)"
      pattern: "describe\\('Phase 24"
---

<objective>
Wave 4b: write the new phase 24 acceptance test + surgically edit ~12 existing tests to remove room assertions. Final goal: `npm test` exits 0 with the full suite green.

Purpose: phase 24's regression lock. Every ROOMS-DEL-* requirement gets at least one passing assertion in tests/phase24.rooms-deleted.test.ts. Existing tests that asserted on room semantics need surgical fixes — they were never wrong, just operating on a deleted contract.

Output: 1 new test file + ~12 edited test files. Full suite green.
</objective>

<execution_context>
@/Users/saharbarak/.claude/get-shit-done/workflows/execute-plan.md
@/Users/saharbarak/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/phase-24/24-CONTEXT.md
@.planning/phases/phase-24/24-RESEARCH.md
@src/domain/graph.ts
@src/cli/commands/migrate.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write tests/phase24.rooms-deleted.test.ts (new — ~500 lines, 9 describe groups)</name>
  <files>tests/phase24.rooms-deleted.test.ts</files>
  <behavior>
    - **Group 1 (Schema, ROOMS-DEL-04):** GraphNode has no `room` field; has `workspace?: string` + `private: boolean`; nodesInRoom + roomFilter are not exported from domain/graph.js.
    - **Group 2 (CLI dispatch, ROOMS-DEL-01):** `akashik room` is unknown subcommand (exec returns non-zero + 'unknown' in stderr); `akashik save` rejects --room flag; `akashik save --private` sets node.private = true; `akashik ask --workspace all` does NOT apply pre-filter.
    - **Group 3 (Storage, ROOMS-DEL-02/03):** No code path in src/ (except migrate.ts) references rooms.json; same for shared-rooms.json; share-store.ts file does not exist.
    - **Group 4 (V5 wire protocol, ROOMS-DEL-05):** SearchRequest / SearchResponse / PeerMatch / TouchRequest types have no `room` field (structural assertion via TS import); pre-V5 SearchRequest with `room` elicits ProtocolMismatchError.
    - **Group 5 (Sharing gate, ROOMS-DEL-03):** Sharing filters out nodes with private: true; share command does not accept --room; unshare does not modify shared-rooms.json.
    - **Group 6 (Workspace, ROOMS-DEL-07):** detectWorkspace returns slug in git repo; returns undefined outside git repo; ask in workspace context filters by workspace; --workspace all bypasses pre-filter.
    - **Group 7 (Migration, ROOMS-DEL-06):** On v4 fixture: strips room, sets private:false, infers workspace; idempotent (second run exits 0 "Already on V5"); backup graph.v4-backup.json exists; reputation flattening drops room:* keys; --rollback restores graph.json.
    - **Group 8 (Hooks, ROOMS-DEL-08):** smart-hook formats hits without [room, ...] segment; post-fetch does not pass --room; session-start statusline omits "rooms" count.
    - **Group 9 (MCP boundary):** list_rooms / find_tunnels / trigger_room not registered; search / federated_search / search_recent / entity-first-lookup have no `room` param; tool count = 13.
  </behavior>
  <read_first>
    - tests/phase20.sessions.test.ts (existing test style — fixture pattern, atomic tmp dirs, mkdtempSync/rmSync, Node test runner conventions)
    - tests/phase16.share-crdt.test.ts (existing structural-assertion patterns — block-comment stripping decision from Phase 16)
    - .planning/phases/phase-24/24-RESEARCH.md (lines 661-829 — recommended test structure, almost verbatim)
    - src/domain/graph.ts (after Plan 01 — to know the V5 schema)
    - src/cli/commands/migrate.ts (after Plan 11 — to know the migration interface to test)
  </read_first>
  <action>
    Write `tests/phase24.rooms-deleted.test.ts` following the structure in 24-RESEARCH.md lines 661-829 EXACTLY for the 9 describe groups.

    File header:
    ```ts
    /**
     * Phase 24 acceptance test — rooms deleted, V5 wire protocol, migration.
     *
     * Covers ROOMS-DEL-01..08. Canonical regression-lock for the V5 cutover.
     */

    import assert from 'node:assert/strict';
    import { describe, test } from 'node:test';
    import { mkdtempSync, rmSync, writeFileSync, existsSync, readFileSync, mkdirSync, cpSync } from 'node:fs';
    import { tmpdir } from 'node:os';
    import { join } from 'node:path';
    import { execFileSync } from 'node:child_process';
    ```

    For each group, implement the tests described in <behavior>. Key implementation notes:

    - For structural assertions about deleted functions / imports: use dynamic `await import('../src/domain/graph.js')` and assert keys are absent.
    - For CLI-exec tests: use `execFileSync('npx', ['tsx', 'src/cli/index.ts', ...args], { cwd: tmp, encoding: 'utf8' })` and wrap in try/catch for non-zero exits.
    - For grep-based structural tests (storage group): use `execFileSync('grep', ['-rn', 'rooms.json', 'src/'], { encoding: 'utf8' })` and filter out comment-only matches AND the migrate.ts file (the one allowed exception).
    - For migration tests: synthesize a v4 fixture inline (JSON with `room` fields), set AKASHIK_HOME to a tmp dir, run migrate, assert end state.
    - For hook tests: pipe sample stdin (JSON hits array) to the cjs hook via execFileSync('node', [hook_path], { input: stdin }) and parse stdout.
    - For MCP boundary tests: build an MCP server in-process or count registered tools via reflection over the registry export.

    Target: 30+ tests across 9 describe groups. ~500 lines.

    Test pattern from Phase 20: each test must be hermetic (create tmp dir at start, delete at end). Use the per-test cleanup `t.after(() => rmSync(tmp, { recursive: true, force: true }))` pattern.

    Block-comment stripping decision from Phase 16: when doing structural grep assertions inside JSDoc / block-comment strings, strip block comments before applying the regex (`text.replace(/\/\*[\s\S]*?\*\//g, '')` first). Mentions of forbidden APIs in comments are NOT failures.
  </action>
  <verify>
    <automated>node --import tsx --test tests/phase24.rooms-deleted.test.ts 2>&1 | grep -E "^(ok|not ok|# pass|# fail)" | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - test -f tests/phase24.rooms-deleted.test.ts exits 0
    - grep -c "describe(.Phase 24" tests/phase24.rooms-deleted.test.ts returns greater than 8 (≥9 describe groups)
    - grep -cE "test\\(.{1,200}', async" tests/phase24.rooms-deleted.test.ts returns greater than 25
    - grep -cE "ROOMS-DEL-01|ROOMS-DEL-02|ROOMS-DEL-03|ROOMS-DEL-04|ROOMS-DEL-05|ROOMS-DEL-06|ROOMS-DEL-07|ROOMS-DEL-08" tests/phase24.rooms-deleted.test.ts returns 8 (each requirement referenced in at least one test)
    - wc -l tests/phase24.rooms-deleted.test.ts returns greater than 350
    - node --import tsx --test tests/phase24.rooms-deleted.test.ts exits 0 (all phase24 tests pass)
  </acceptance_criteria>
  <done>Phase 24 acceptance test exists and passes; covers all 8 ROOMS-DEL-* requirements.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Surgical edits to ~12 existing test files</name>
  <files>tests/phase16.share-crdt.test.ts, tests/phase34.save-note.test.ts, tests/phase6.daemon.test.ts, tests/phase38.oracle.test.ts, tests/consolidator.test.ts, tests/phase17.federated-search.test.ts, tests/phase18.production-net.test.ts, tests/phase19.codebase-indexing.test.ts, tests/phase20.sessions.test.ts, tests/phase15.peer-foundation.test.ts, tests/phase35.entity-extraction.test.ts, tests/phase33.recall-rerank.test.ts</files>
  <read_first>
    - Each affected test file (entire content — these are 200-1000 line files; READ THEM)
    - .planning/phases/phase-24/24-CONTEXT.md (line 90 — "Edit ~13 other tests to remove room assertions and add private/workspace assertions")
    - .planning/phases/phase-24/24-RESEARCH.md (lines 644-647 — phase16 + phase38 specific notes)
  </read_first>
  <action>
    For each existing test file, surgically edit to:

    1. Remove imports of deleted modules (rooms, system-rooms, share-store, rooms-config).
    2. Drop test cases that assert on deleted contracts:
       - phase16.share-crdt.test.ts:166 (unshare modifies shared-rooms.json) → delete this test OR re-target to "unshare removes peer from peer-store"
       - phase38.oracle.test.ts: oracle was system-room mediated. Either delete the file OR re-target to "oracle Q&A nodes filter by source_uri scheme `oracle-question:` / `oracle-answer:`" — research line 649 says the oracle PRIMITIVE may survive; verify behaviorally. Planner decision: keep the file but re-target.
    3. Drop room assertions, add equivalent private/workspace assertions where the test's intent still applies.
    4. For C2 invariant tests (Phase 17 search-recent test which Phase 20 bumped to 16 MCP tools) → update assertion from `tools.length === 16` to `tools.length === 13`.
    5. For Phase 18 production-net test: the 10-peer integration test should still pass against the V5 share-sync (no room assertions in that test).
    6. For Phase 20 sessions test: the `nodesInRoom(graph, 'sessions')` assertions in tests pointing at recent-sessions.ts behavior — update to assert `source_uri.startsWith('claude_sessions:')` filter.

    Per-file scope (read first to confirm):
    - phase15: drop any room-shared peer assertions
    - phase16.share-crdt: drop shared-rooms.json assertions; re-target unshare tests
    - phase17.federated-search: drop room-based fan-out assertions; tool count → 13
    - phase18.production-net: minimal/none if no room assertions
    - phase19.codebase-indexing: drop the "attach codebase to room" tests OR re-target (codebases attach to global graph in V5)
    - phase20.sessions: update source_uri filter in recent-sessions tests; C2 tool count update; drop ensureSessionsRoom assertions
    - phase33.recall-rerank: drop HALF_LIFE_BY_ROOM assertions; assert uniform half-life
    - phase34.save-note: drop room field assertion; add private:false default + workspace?:string assertions
    - phase35.entity-extraction: drop subjectFromRoom assertions; entity-only subjects
    - phase38.oracle: re-target to source_uri scheme filter
    - phase6.daemon: drop RoomsConfig assertions
    - consolidator: drop room-keyed consolidation assertions
  </action>
  <verify>
    <automated>npm test 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - For each of the 12 files: grep -cE "defaultRoom|findRoom|RoomId\\b|nodesInRoom\\b|HALF_LIFE_BY_ROOM|subjectFromRoom|loadSharedRooms|shared-rooms\\.json" returns 0
    - `npm test` exits 0 (full suite green)
    - The total test count is within ±100 of pre-phase count (Phase 20 had 313; expect 280-340 after deletions + additions)
  </acceptance_criteria>
  <done>All affected existing tests updated to V5 schema; full suite green.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Final cutover validation</name>
  <files>(none — this is a verification task; produces no file changes)</files>
  <read_first>
    - (no files needed — runs commands only)
  </read_first>
  <action>
    Run the final cutover validation checks per 24-RESEARCH.md lines 322-324:

    1. `npm run build` exits 0
    2. `npm test` exits 0
    3. `grep -rn "room" src/ tests/ | grep -v -E "(\\b/\\*|^[^:]+:\\s*//|comment|\\.test\\.ts:.*describe.*'.*room|workspace|private|protocol|akashik-prompt|migrate\\.ts|peer-reputation|V4-PROTOCOL|V5-PROTOCOL|phase24)"` returns a sane low number (target: only string-literal matches in migrate.ts, V4-PROTOCOL.md mentions, phase24 test, etc.)
    4. `grep -rnE "from\\s+['\"].*\\./(domain/(rooms|system-rooms)|infrastructure/(rooms-config|share-store))['\"]" src/` returns 0 (no live imports of deleted modules)
    5. `npx tsc --noEmit` exits 0
    6. Sample run: `node --import tsx src/cli/index.ts migrate v5` against a synthesized v4 ~/.akashik in /tmp — verify the migration succeeds and is idempotent.

    Capture all outputs into /tmp/phase24-cutover-evidence.txt for the summary.

    If any check fails: report the failure to the calling orchestrator; do NOT silently mask.
  </action>
  <verify>
    <automated>npm run build && npm test && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits 0
    - `npm test` exits 0
    - `npx tsc --noEmit` exits 0
    - `grep -rnE "from\\s+['\"].*\\./(domain/(rooms|system-rooms)|infrastructure/(rooms-config|share-store))['\"]" src/` returns 0 lines
    - A synthesized v4 migrate test passes (script + expected output captured in /tmp/phase24-cutover-evidence.txt)
  </acceptance_criteria>
  <done>Phase 24 V5 cutover is complete. Build, test, and tsc all green. Migration verified on synthetic v4 data.</done>
</task>

</tasks>

<verification>
- npm test exits 0
- npx tsc --noEmit exits 0
- All 8 ROOMS-DEL-* requirements covered by at least one passing test in phase24.rooms-deleted.test.ts
- Full suite test count: 280-340 (per research projection)
</verification>

<success_criteria>
1. phase24.rooms-deleted.test.ts: ≥30 tests, ≥9 describe groups, all pass
2. All 12 surgically-edited tests pass
3. npm test full suite green
4. npx tsc --noEmit exits 0
5. Phase 24 V5 cutover is observable, verified, and locked by regression tests
</success_criteria>

<output>
After completion, create .planning/phases/phase-24/24-12-SUMMARY.md with:
- Phase 24 summary: 8 requirements complete (ROOMS-DEL-01..08)
- Full test suite count before / after
- Phase 24 LOC delta (deletions + additions across all 12 plans)
- Open Questions resolution: all 6 resolved (see plans 02, 03, 09, 10, 11)
- MCP tool count: 16 → 13
- Final cutover evidence (paste /tmp/phase24-cutover-evidence.txt summary)
- ROADMAP.md update note (move Phase 24 from "Plans 0/N" to "Plans N/N complete")
</output>
