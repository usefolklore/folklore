---
phase: 27-site-build-out
plan: 04
type: execute
wave: 4
depends_on: ["27-03"]
files_modified: [site/index.html]
autonomous: true
requirements: [SITE-01]
must_haves:
  truths:
    - "Every section renders cleanly at 390px — no horizontal overflow, content stacks to a single column"
    - "The new Guidebook, Culture, and Store sections are swept along with the pre-existing sections"
    - "The composition pass is applied at 1512px: consistent rhythm, no oversize/clipped elements, balanced sections"
    - "The folk-pop brand and clamped type scale are preserved (nothing enlarged, no new deps)"
  artifacts:
    - path: "site/index.html"
      provides: "Responsive + composition fixes (media queries / clamps / overflow guards) across all sections"
      contains: "overflow-x:hidden"
  key_links:
    - from: "body / sections"
      to: "no horizontal scroll at 390px"
      via: "overflow guards + single-column stacking media queries"
      pattern: "@media\\(max-width"
---

<objective>
Final composition pass + mobile responsive sweep across ALL sections (the original page PLUS the new Guidebook, Culture, and Store from 27-01/02/03). Verify with headless Chrome at 1512px and 390px; fix every horizontal-overflow, mis-stacking, and oversize-element issue; tighten composition so the whole page reads as one confident folk-pop document.

Purpose: SITE-01 — runs LAST so the new sections are swept too. No section should overflow horizontally at 390px; everything stacks to a single column; the desktop composition is balanced.
Output: Responsive/composition edits in site/index.html, evidenced by before/after screenshots at both widths.

CRITICAL: edits site/index.html; runs AFTER 27-03. Sole editor in wave 4.
</objective>

<execution_context>
@/Users/saharbarak/.claude/get-shit-done/workflows/execute-plan.md
@/Users/saharbarak/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-site-build-out/27-CONTEXT.md
@docs/brand/README.md
@.planning/phases/27-site-build-out/27-01-SUMMARY.md
@.planning/phases/27-site-build-out/27-02-SUMMARY.md
@.planning/phases/27-site-build-out/27-03-SUMMARY.md

<interfaces>
<!-- Existing responsive infrastructure already in the file — extend it, don't replace it. -->
  body{...overflow-x:hidden}           (~line 32) — page-level horizontal clamp already present
  html{scroll-snap-type:y proximity;scroll-padding-top:70px}  (~30)
  Existing breakpoints already in the file:
    @media(max-width:900px){.herogrid → 1fr}            (~101)
    @media(max-width:820px){.hlinks{display:none}}      (~72) — nav hides on mobile
    @media(max-width:880px){.beat → 1fr}                (~137)
    @media(max-width:760px){.illum → 1fr}               (~148)
    @media(max-width:760px){.ledger → 1fr 1fr}          (~159)
    @media(max-width:760px){.store → 1fr}               (~188)
  Clamp helpers in use: clamp() on .wrap padding, h1/h2/.body/.lede font sizes, section padding — these are the LOCKED type scale; do NOT raise the clamp ceilings.
  Known overflow-risk spots to check specifically:
    - .insc pre (~171) uses white-space:pre-wrap;word-break:break-word — long command lines (the libp2p multiaddr in #join ~369) can still push width on narrow screens.
    - .marq .track / animated marquee — fixed-width content, must not create body scroll (parent has overflow:hidden, verify).
    - .bars .bar grid `120px 1fr 56px` (~161) — the 120px label column can crush at 390px.
    - hero .install (~109) max-width:480px with a long npm command + copy chip — check it doesn't overflow at 390px.
    - any new fixed-width images/grids added by 27-01/02/03.
    - .farshape / .fg absolute-positioned decor with vw/px widths and left:60%/64% — can extend past the right edge; body overflow-x:hidden clips them but confirm they don't add scrollable width on mobile.

Screenshot harness (the executor's primary evidence tool):
  CHROME="/Applications/Google Chrome.app/Contents/MacOS/Google Chrome"
  "$CHROME" --headless=new --hide-scrollbars --screenshot=/tmp/desk.png --window-size=1512,8000 "file://$(pwd)/site/index.html"
  "$CHROME" --headless=new --hide-scrollbars --screenshot=/tmp/mob.png  --window-size=390,12000 "file://$(pwd)/site/index.html"
  (tall window heights capture the full scroll; Read the PNGs to inspect.)
</interfaces>

@site/index.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit current state at 1512px and 390px and record the issue list</name>
  <read_first>site/index.html (full, post-27-03) — all section markup and the @media blocks listed in <interfaces></read_first>
  <files>site/index.html</files>
  <action>
This is a measure-first task — no fixes yet beyond zero-cost confirmations.

1. Capture full-page screenshots at BOTH widths using the harness in <interfaces> (desktop 1512px, mobile 390px). Use tall window heights so the whole page is captured; if the page is taller than one PNG can hold, also capture per-section crops by scrolling (or take multiple tall captures). Read every PNG.

2. Detect horizontal overflow at 390px programmatically as hard evidence (screenshots can hide it). Run a headless check via Chrome's DevTools-free approach: use a tiny inline check by loading the file and dumping scrollWidth vs clientWidth. Since we cannot easily script the DOM headlessly without extra tooling, instead:
   - Read the 390px screenshot and look for: content bleeding past the right edge, a visible horizontal scrollbar (run WITHOUT --hide-scrollbars for this one diagnostic capture so a scrollbar would show), text clipped at the edge, side-by-side elements that should have stacked.
   - For each section id (hero, problem, alone, together, culture, mechanism, proof, guidebook, join, name, store, memes, and #coin if still present), note pass/fail for: (a) no horizontal overflow, (b) single-column stacking, (c) no clipped/oversize element.

3. Write the concrete issue list into the eventual SUMMARY (collect it now as you go). Each issue = {section, width, symptom, suspected CSS line/rule}.

Do not enlarge type. Do not add deps. This task changes nothing structural unless you spot a trivially-safe fix; defer real fixes to Task 2.
  </action>
  <verify>
    <automated>test -f /tmp/mob.png && test -f /tmp/desk.png && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - /tmp/desk.png (1512px) and /tmp/mob.png (390px) both exist and were Read by the executor.
    - A written issue list exists (in working notes / draft SUMMARY) enumerating every section's pass/fail at 390px for overflow + stacking, including the new #guidebook, #culture, and #store sections.
    - The known overflow-risk spots from <interfaces> (.insc pre multiaddr, .bars 120px column, hero .install, decor shapes, any new 27-0x grids) are each explicitly checked and recorded.
  </acceptance_criteria>
  <done>Both-width screenshots captured and read; a concrete, per-section issue list (overflow/stacking/oversize at 390px and composition notes at 1512px) is recorded, covering all sections including the three new ones.</done>
</task>

<task type="auto">
  <name>Task 2: Fix overflow + stacking + composition, re-verify with screenshots</name>
  <read_first>site/index.html (full) and the Task 1 issue list</read_first>
  <files>site/index.html</files>
  <action>
Apply minimal, targeted CSS fixes for every issue from Task 1. Constraints: extend the EXISTING @media blocks / clamps; do NOT raise type-scale clamp ceilings; do NOT introduce a CSS framework or new deps; keep the folk-pop skin (hard shadows, ink borders, stamps, misregistered headers).

Likely fixes (apply only those the audit confirmed needed):
- Ensure single-column stacking at ≤760px for any multi-column grid that 27-01/02/03 added (Guidebook .illum already collapses at 760px via the existing rule; Store cards must collapse — confirm the 27-03 grid uses auto-fit minmax or add a `@media(max-width:760px){...→1fr}` rule).
- Long command lines: ensure `.insc pre` and hero `.install` wrap and never exceed viewport at 390px (the multiaddr in #join is the worst case — `overflow-wrap:anywhere` / smaller mono size at the existing clamp floor, or `max-width:100%;overflow-x:auto` on the code block as a last resort — prefer wrapping over inner scroll).
- `.bars .bar` grid: at ≤480px collapse the `120px 1fr 56px` template so the label/value don't crush (e.g. stack label above the track, or reduce the label column) via a new `@media(max-width:480px)` rule.
- Decorative `.farshape`/`.fg`: confirm none add horizontal scroll at 390px; if any do, constrain with the existing `body{overflow-x:hidden}` (already there) and/or hide oversized decor under a `@media(max-width:600px)` rule (`display:none` on the largest margin shapes is acceptable — they are pure decoration and `prefers-reduced-motion` already disables their animation).
- Composition at 1512px: tighten any section that reads thin or oversized vs its neighbors — adjust `margin-top` rhythm, grid gaps, or image `max-width` to keep a consistent vertical cadence. Keep each section confident within ~100svh (do not force exact 100svh if content needs more, but avoid huge empty gaps).
- Verify the navbar still collapses cleanly (links hidden ≤820px) and the brand + GitHub CTA still fit at 390px without overflow.

After edits, RE-RUN the screenshot harness at 1512px and 390px and Read the PNGs to confirm each prior issue is resolved and nothing regressed. Iterate until clean.

Final guardrails: hero/commons/graph `<video>` intact; #guidebook/#culture/#store all still present and styled; clamped type unchanged; no src/ changes; no new external host in `<link>`/`<script src>`.
  </action>
  <verify>
    <automated>grep -q 'overflow-x:hidden' site/index.html && grep -c 'id="guidebook"' site/index.html | grep -q '^1$' && grep -c 'id="culture"' site/index.html | grep -q '^1$' && grep -c 'id="store"' site/index.html | grep -q '^1$' && grep -Eq 'video[^>]*poster="assets/gen/hero' site/index.html && test -f /tmp/mob.png && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - At 390px (Read the re-captured /tmp/mob.png): NO horizontal overflow on any section, all content single-column, no clipped text, the #join multiaddr command wraps instead of overflowing, the proof bars are legible (not crushed).
    - At 1512px (Read the re-captured /tmp/desk.png): composition is balanced — no section reads broken/oversize; the three new sections (Guidebook, Culture, Store) match the cadence of the originals.
    - Brand preserved: `grep 'overflow-x:hidden'` still present; the hero/commons/graph `<video poster="assets/gen/...">` elements still present (`grep -E 'video[^>]*poster="assets/gen/hero'` passes); clamp() type rules not raised (diff shows no font-size clamp ceiling increases).
    - All new sections survive: `grep -c 'id="guidebook"'`==1, `grep -c 'id="culture"'`==1, `grep -c 'id="store"'`==1.
    - No new external dependency: `git diff` adds no `<link>`/`<script src>` to any host other than fonts.googleapis.com / fonts.gstatic.com.
  </acceptance_criteria>
  <done>Every audited overflow/stacking/composition issue is fixed and re-verified by fresh 1512px + 390px screenshots; the page is mobile-clean at 390px and composition-balanced at 1512px across all sections including the three new ones; folk-pop skin, clamped type, videos, and all section anchors intact.</done>
</task>

</tasks>

<verification>
- Re-captured headless screenshots at 1512px and 390px show: zero horizontal overflow at 390px, single-column stacking everywhere, balanced composition at 1512px.
- `grep` confirms overflow-x:hidden retained; #guidebook/#culture/#store all == 1; hero video poster retained.
- `git diff` touches only site/index.html and adds no non-font external resource.
</verification>

<success_criteria>
SITE-01 satisfied: the site renders cleanly at 390px across ALL sections (no horizontal overflow, correct stacking) and a composition pass is applied at desktop width, with the new Guidebook/Culture/Store sections swept along with the originals — folk-pop skin and clamped type preserved.
</success_criteria>

<output>
After completion, create `.planning/phases/27-site-build-out/27-04-SUMMARY.md` (include the Task 1 issue list and the before/after resolution per section).
</output>
