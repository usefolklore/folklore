---
phase: 27-site-build-out
plan: 05
type: execute
wave: 1
depends_on: []
files_modified: [wrangler.toml, site/_headers]
autonomous: true
requirements: [SITE-05]
must_haves:
  truths:
    - "wrangler.toml declares the Cloudflare Pages build output dir as the site/ directory"
    - "site/_headers is present and valid for Cloudflare Pages"
    - "There are no Vercel (or other non-Cloudflare host) deploy remnants in the repo deploy surface"
    - "The site serves locally as a static directory (no build step needed) — verified without deploying"
  artifacts:
    - path: "wrangler.toml"
      provides: "Cloudflare Pages config pointing output at site/"
      contains: "pages_build_output_dir"
    - path: "site/_headers"
      provides: "Cloudflare Pages headers file"
      contains: "X-Content-Type-Options"
  key_links:
    - from: "wrangler.toml pages_build_output_dir"
      to: "the site/ directory"
      via: "config value"
      pattern: "pages_build_output_dir\\s*=\\s*\"site\""
---

<objective>
Verify (and fix only if needed) the Cloudflare Pages build configuration so the site is provably buildable/servable, WITHOUT deploying (deploy is blocked on user auth + domain). Confirm wrangler.toml output dir = site/, site/_headers is present and valid, and there are no Vercel or other non-Cloudflare deploy remnants.

Purpose: SITE-05 — give the launch a green, Cloudflare-correct config so the user's eventual deploy is a one-step action, with confidence the static output is correct.
Output: A verified (or corrected) wrangler.toml + site/_headers, plus evidence of a clean local static serve. Does NOT touch site/index.html, so this plan runs in wave 1 parallel with 27-01.

CRITICAL: DO NOT deploy. No `wrangler pages deploy`, no `wrangler login`, no network publish. Verification is local only.
</objective>

<execution_context>
@/Users/saharbarak/.claude/get-shit-done/workflows/execute-plan.md
@/Users/saharbarak/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-site-build-out/27-CONTEXT.md

<interfaces>
<!-- Current deploy surface (verified at plan time): -->
wrangler.toml (repo root):
  name = "folklore"
  compatibility_date = "2026-06-15"
  pages_build_output_dir = "site"     ← already correct; verify it stays this

site/_headers (already present):
  /*
    X-Content-Type-Options: nosniff
    Referrer-Policy: strict-origin-when-cross-origin
    X-Frame-Options: DENY

Cloudflare Pages for a static directory needs NO build command — pages_build_output_dir = "site"
means CF serves the site/ directory as-is. The _headers file is the CF Pages mechanism for custom
response headers (https://developers.cloudflare.com/pages/configuration/headers/).

Vercel/Netlify-remnant patterns to grep for and confirm ABSENT (remove ONLY if a genuine deploy
remnant, never unrelated config): vercel.json, .vercel/, .vercelignore, "vercel" deploy script in
package.json, netlify.toml, a site/_redirects pointing at a non-CF host.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify Cloudflare Pages config and absence of Vercel remnants</name>
  <read_first>wrangler.toml, site/_headers</read_first>
  <files>wrangler.toml, site/_headers</files>
  <action>
1. Confirm wrangler.toml exists at repo root and `pages_build_output_dir = "site"` is set (it is, per <interfaces> — assert it; if it ever differs, correct it to "site"). Confirm `name` and `compatibility_date` are present and sane.

2. Confirm site/_headers exists, is non-empty, and uses valid Cloudflare Pages _headers syntax (a path-pattern line like `/*` followed by indented `Header-Name: value` lines). It currently carries X-Content-Type-Options / Referrer-Policy / X-Frame-Options — keep them.

3. Grep the repo for non-Cloudflare deploy remnants and confirm absent (remove only deploy-config remnants if genuinely present, nothing else):
     - files: vercel.json, .vercel, .vercelignore, netlify.toml, site/_redirects
     - strings: a vercel/netlify deploy script in package.json
   Record the grep results. If a real remnant exists, remove it and note why; if none, state "clean".

4. Confirm the static output is well-formed: list site/ and confirm index.html + assets/gen/ are present and that index.html references assets with RELATIVE paths (`assets/gen/...`), which is correct for CF Pages serving the site/ root. Spot-check that no asset is referenced via an absolute filesystem path or a localhost URL.

If everything is already correct (expected), this task makes zero file changes and just records evidence. Do NOT add a build command (a static dir needs none). Do NOT add a CNAME/domain (blocked on user).
  </action>
  <verify>
    <automated>grep -Eq 'pages_build_output_dir[[:space:]]*=[[:space:]]*"site"' wrangler.toml && test -s site/_headers && grep -q 'X-Content-Type-Options' site/_headers && ! test -e vercel.json && ! test -e netlify.toml && ! test -d .vercel && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E 'pages_build_output_dir[[:space:]]*=[[:space:]]*"site"' wrangler.toml` matches.
    - `site/_headers` is non-empty and `grep 'X-Content-Type-Options' site/_headers` matches.
    - No Vercel/Netlify remnants: vercel.json, netlify.toml, .vercel/, .vercelignore all absent (verified by test/ls), and no vercel/netlify deploy script in package.json.
    - site/index.html and site/assets/gen/ exist; assets referenced relatively — `grep -E 'src="(/Users|file://|http://localhost)' site/index.html` returns nothing.
  </acceptance_criteria>
  <done>wrangler.toml output dir is "site", site/_headers is valid and present, no non-Cloudflare deploy remnants exist, and the static output references assets relatively. Evidence recorded.</done>
</task>

<task type="auto">
  <name>Task 2: Prove the site serves locally (Cloudflare-Pages-equivalent), no deploy</name>
  <read_first>wrangler.toml, site/index.html (head + asset references only)</read_first>
  <files>wrangler.toml</files>
  <action>
Demonstrate the site is servable the way Cloudflare Pages would serve it — a static serve of the site/ directory — WITHOUT deploying.

Optional preferred path: `npx --yes wrangler pages dev site --port <free-port>` IF wrangler is installed and runs fully offline. `wrangler pages dev` runs locally and does NOT publish — but if it attempts any login/account/network step or is not available offline, ABORT it and use the fallback. Never run a deploy/publish/login command.

Guaranteed fallback (no external deps): serve the directory with python's stdlib static server on an ephemeral port, request the root and one asset, and assert. The verify command below does exactly this in one shot — use it as the canonical evidence. If you serve manually instead, do the equivalent: GET `/` → 200 and body contains `<title>Folklore`; GET `/assets/gen/hero.png` → 200; then stop the server.

Note: a plain static server does NOT apply site/_headers (that is CF-Pages-specific) — so do NOT assert the security headers over the local serve; their presence in the file is already asserted in Task 1.

Record: which serve method was used, the HTTP codes, and the title-match result. Make no changes to wrangler.toml unless Task 1 found a defect (it should not). DO NOT deploy/publish/login.
  </action>
  <verify>
    <automated>python3 ./scripts/_cf_local_serve_check.py 2>/dev/null || python3 -c "import http.server,socketserver,threading,urllib.request,os; os.chdir('site'); srv=socketserver.TCPServer(('127.0.0.1',0),http.server.SimpleHTTPRequestHandler); p=srv.server_address[1]; threading.Thread(target=srv.serve_forever,daemon=True).start(); import time; time.sleep(0.5); r=urllib.request.urlopen('http://127.0.0.1:%d/'%p); body=r.read().decode('utf-8','ignore'); a=urllib.request.urlopen('http://127.0.0.1:%d/assets/gen/hero.png'%p); srv.shutdown(); print('PASS' if (r.status==200 and '<title>Folklore' in body and a.status==200) else 'FAIL')"</automated>
  </verify>
  <acceptance_criteria>
    - The verify command prints PASS: a local static serve of site/ returns HTTP 200 at `/`, the served root body contains `<title>Folklore`, and `/assets/gen/hero.png` returns HTTP 200 (a relative asset resolves at the path CF Pages will use).
    - No deploy/publish/login command was run (the SUMMARY states explicitly that deploy stayed blocked-on-user).
    - wrangler.toml unchanged unless Task 1 found a defect.
  </acceptance_criteria>
  <done>A local static serve of the site/ directory returns 200 for the root (with the Folklore title) and for a relative asset, proving the Cloudflare Pages static output is well-formed — with no deploy performed.</done>
</task>

</tasks>

<verification>
- `grep -E 'pages_build_output_dir[[:space:]]*=[[:space:]]*"site"' wrangler.toml` matches; site/_headers present with security headers.
- No vercel.json / netlify.toml / .vercel/ / .vercelignore present.
- Local static serve of site/ returns 200 at `/` (Folklore title) and 200 for a relative asset.
- No deploy/publish/login command executed; deploy remains blocked-on-user.
</verification>

<success_criteria>
SITE-05 satisfied: Cloudflare Pages deploy config verified buildable — wrangler.toml output dir = site/, site/_headers present and valid, no non-Cloudflare deploy remnants, and the static output serves locally — with the actual deploy deferred to user auth/domain.
</success_criteria>

<output>
After completion, create `.planning/phases/27-site-build-out/27-05-SUMMARY.md`.
</output>
