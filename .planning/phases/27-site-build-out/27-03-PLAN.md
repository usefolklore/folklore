---
phase: 27-site-build-out
plan: 03
type: execute
wave: 3
depends_on: ["27-02"]
files_modified: [site/index.html]
autonomous: true
requirements: [SITE-04]
must_haves:
  truths:
    - "The Store reads like a real shop: product cards with name, image, price placeholder, and a (inert) buy CTA"
    - "There are at least three product cards covering tee, sticker pack, and pin"
    - "A $LORE block has a bags.fm CTA and a clear 'not financial advice' disclaimer"
    - "Link points are wired (real hrefs / data attributes) but inert/placeholder, ready for the blocked-on-user launch"
    - "Everything stays in the folk-pop section system and clamped type"
  artifacts:
    - path: "site/index.html"
      provides: "An evolved <section id=\"store\"> with a real product-card grid + a $LORE bags.fm block with disclaimer"
      contains: "not financial advice"
  key_links:
    - from: "store product cards"
      to: "a price placeholder + buy CTA"
      via: "card markup with price + button"
      pattern: "data-price|class=\"price"
    - from: "$LORE block"
      to: "bags.fm"
      via: "CTA anchor"
      pattern: "bags\\.fm"
---

<objective>
Evolve the existing Store stub (two "Coming soon" panels at `<section id="store">`) into a real shop layout: product cards (tee, sticker pack, pin) each with name, image, price placeholder, and an inert buy CTA — plus a $LORE memecoin block with a bags.fm CTA and a "not financial advice" disclaimer. Structure it so real prices/links drop in later (blocked on user: merch fulfilment + $LORE launch).

Purpose: SITE-04 — the store must read like a genuine shop a visitor could buy from once products go live, not a placeholder. Phase 28 will swap in real product designs into this scaffold.
Output: A rebuilt `<section id="store">` (and consolidation of the separate `#coin` $LORE content into the store, or a clearly-linked $LORE sub-block within store) in site/index.html.

CRITICAL: edits site/index.html; runs AFTER 27-02. Sole editor in wave 3.
</objective>

<execution_context>
@/Users/saharbarak/.claude/get-shit-done/workflows/execute-plan.md
@/Users/saharbarak/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-site-build-out/27-CONTEXT.md
@docs/brand/README.md
@.planning/phases/27-site-build-out/27-02-SUMMARY.md

<interfaces>
<!-- Existing store + coin markup to evolve. -->

Existing Store stub (~line 384-393): a <section id="store"> with .store grid (1.4fr 1fr) holding two .merch panels (.merch.a pink, .merch.b blue), each with a "Coming soon" .soon chip and an image. Existing CSS for it (~lines 179-188):
  .store{display:grid;grid-template-columns:1.4fr 1fr;gap:24px;margin-top:30px}
  .merch{border:3px solid var(--ink);padding:36px;min-height:260px;display:flex;flex-direction:column;justify-content:space-between}
  .merch.a{background:var(--pink);...box-shadow:10px 10px 0 var(--ink)}
  .merch.b{background:var(--blue);...}
  .merch .soon{...yellow chip...}
  .merch .tee{font-weight:800;font-style:italic;font-size:2rem}
  @media(max-width:760px){.store{grid-template-columns:1fr}}

Existing $LORE / Coin section (~line 413-428): <section id="coin"> with a .beat, an h3 "$LORE — minted on bags.fm.", a bags.fm CTA button, and the disclaimer text "Memecoin · for fun & culture, not financial advice · contract address at launch", plus assets/gen/coin.png. The "not financial advice" disclaimer ALREADY EXISTS here — preserve that exact spirit/wording.

Product imagery available in site/assets/gen/: tee.png (the shirt print), sun.png / char1.png / fishflower.png / bird.png (sticker/print candidates), coin.png ($LORE coin). There is NO dedicated pin image — use a folk SVG symbol (#star or #flower from defs ~205) or one of the sticker images as the pin placeholder, and label it clearly as a pin.

Reusable building blocks: .stamp, h2+.mis, .em-*, .body, .rv/data-d, .btn/.btn-primary/.btn-ghost (the bordered hard-shadow buttons ~103), .soon chip. The IntersectionObserver reveals .rv.
</interfaces>

@site/index.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rebuild the Store as a real product-card shop</name>
  <read_first>site/index.html (full, post-27-02) — the #store section + .store/.merch CSS (~179-188, ~384-393), the #coin section (~413-428), and the .btn classes (~103-108)</read_first>
  <files>site/index.html</files>
  <action>
Replace the contents of `<section id="store">` (keep the `<section id="store">` wrapper, the leading `.folkband` divider before it, the `.stamp`, and the h2) with a real shop grid. Goal: a card grid of at least THREE product cards — Tee, Sticker Pack, Pin — each card containing:
  - product image (bordered with the standard `border:3px solid var(--ink)` + a colored hard shadow), reuse tee.png for the tee, a sticker image (e.g. sun.png/fishflower.png) for the sticker pack, and #star SVG or char1.png for the pin;
  - a product name (e.g. "Never-Research-Twice Tee");
  - a price PLACEHOLDER — render a visible price slot you can later fill, e.g. `<span class="price" data-price="">$—</span>` or a "$XX" placeholder, clearly a placeholder not a fake real price;
  - a "Coming soon" / "Drops to peers first" `.soon` chip (keep the existing chip style);
  - an INERT buy CTA — a `.btn` styled button/anchor that is wired but does nothing real yet. Use `href="#"` with an explicit inert marker, OR `aria-disabled="true"`, OR a `data-buy="<product-id>"` attribute and `href="#store"`. Do NOT link to a fake store URL. The link point must be obviously ready for a real URL drop-in later (comment it: `<!-- TODO: real product URL at fulfilment launch -->`).

Add a small scoped CSS block for the product-card grid IF the existing `.store`/`.merch` 2-panel grid doesn't cleanly extend to a 3-up card layout. If you add CSS, put it next to the existing store rules (~line 188) and keep it minimal (a responsive `grid-template-columns:repeat(auto-fit,minmax(220px,1fr))` is ideal and already used by the memes grid ~401 — you may mirror that). Reuse `.merch`/`.soon`/`.btn` where possible to avoid new classes.

$LORE consolidation: bring the $LORE memecoin pitch into the Store experience. EITHER (a) move the substance of the `#coin` section's $LORE block (coin image, "minted on bags.fm" pitch, bags.fm CTA, and the "not financial advice" disclaimer) into a `$LORE` sub-block inside `#store` and delete the now-redundant standalone `#coin` section; OR (b) keep `#coin` as its own section but ensure the Store section explicitly links to it and the bags.fm CTA + disclaimer are present and intact. Prefer (a) — a single coherent Store that holds both merch and $LORE reads most like a real shop. Whichever you choose:
  - the bags.fm CTA MUST remain (a `.btn-primary` anchor to https://bags.fm);
  - the disclaimer "not financial advice" (the existing wording: "Memecoin · for fun & culture, not financial advice · contract address at launch") MUST remain visible;
  - the coin.png image treatment (round, bordered, hard shadow) MUST be preserved if kept.
  - If you delete `#coin`, also remove the `<a href="#coin">` if any nav/anchor referenced it (grep first) — currently nav does NOT link #coin, so likely nothing to update, but verify.

Keep clamped type — no enlarging overrides. Keep folk-pop skin (hard shadows, ink borders, the stamp + misregistered heading). Do NOT break the Guidebook (#guidebook) or Culture (#culture) sections from 27-01/27-02, the hero/commons/graph videos, or the #memes section.

Do NOT touch src/. No new external deps.
  </action>
  <verify>
    <automated>grep -qi 'not financial advice' site/index.html && grep -q 'bags\.fm' site/index.html && grep -c 'id="store"' site/index.html | grep -q '^1$' && grep -Eq 'data-buy|aria-disabled|TODO: real product URL' site/index.html && grep -Eiq 'price|\$—|\$XX' site/index.html && grep -c 'id="guidebook"' site/index.html | grep -q '^1$' && grep -c 'id="culture"' site/index.html | grep -q '^1$' && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'id="store"' site/index.html` == 1; Store section still present.
    - At least three product entries: the strings for Tee, Sticker (pack), and Pin all appear as product names/labels within #store.
    - Each product has a price placeholder: `grep -Ei 'class="price"|\$—|\$XX|data-price'` matches (a deliberate placeholder, not a fabricated real price).
    - Each product has an inert buy CTA: `grep -E 'data-buy|aria-disabled="true"|TODO: real product URL'` matches and there is NO fabricated external store URL (no `href="https://shop...` to a non-existent store).
    - `$LORE` is present in the shop: `grep 'bags\.fm'` matches AND `grep -i 'not financial advice'` matches.
    - 27-01 + 27-02 sections intact: `grep -c 'id="guidebook"'` == 1 and `grep -c 'id="culture"'` == 1.
    - Headless screenshots: at 1512px (`--window-size=1512,3600 --screenshot=/tmp/store-1512.png`) AND at 390px (`--window-size=390,3600 --screenshot=/tmp/store-390.png`). Read both — at 1512px the store shows a multi-card shop grid + $LORE block with disclaimer; at 390px the cards stack to a single column with no horizontal overflow.
  </acceptance_criteria>
  <done>The Store renders as a real shop: ≥3 product cards (tee/sticker/pin) each with image, name, price placeholder, and inert wired buy CTA, plus a $LORE bags.fm block with "not financial advice" disclaimer. Folk-pop skin and clamped type preserved; Guidebook + Culture untouched. Screenshots at 1512 and 390 confirm clean layout.</done>
</task>

</tasks>

<verification>
- `grep -i 'not financial advice'` and `grep 'bags\.fm'` both match (disclaimer + CTA survive the rebuild).
- `grep -c 'id="store"'` == 1; `grep -c 'id="guidebook"'` == 1; `grep -c 'id="culture"'` == 1 (no clobbering).
- Product placeholders present (price + inert CTA grep passes); no fabricated real store URL.
- Headless screenshots at 1512px and 390px confirm a real-shop layout and clean mobile stacking.
- `git diff` touches only site/index.html.
</verification>

<success_criteria>
SITE-04 satisfied: a Store section structured for live products — product cards with price placeholders and inert-but-wired buy CTAs (tee/sticker/pin), plus a $LORE bags.fm CTA with "not financial advice" disclaimer — in the locked folk-pop skin, ready for the blocked-on-user launch.
</success_criteria>

<output>
After completion, create `.planning/phases/27-site-build-out/27-03-SUMMARY.md`.
</output>
