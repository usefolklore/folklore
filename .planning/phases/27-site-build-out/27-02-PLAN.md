---
phase: 27-site-build-out
plan: 02
type: execute
wave: 2
depends_on: ["27-01"]
files_modified: [site/index.html]
autonomous: true
requirements: [SITE-03]
must_haves:
  truths:
    - "A visitor can scroll to a Platform Culture section telling the lore / the commons / the folk story"
    - "The section frames the bet: works alone day one, compounds when peers join — knowledge passed on, never relearned"
    - "It renders within the existing section system (100svh, scroll-snap, stamp header, hard shadows, clamped type) in the folk-pop skin"
    - "The navbar links to the new Platform Culture section"
  artifacts:
    - path: "site/index.html"
      provides: "A new <section id=\"culture\"> with stamp header, misregistered h2, and lore/commons/folk narrative beats"
      contains: "id=\"culture\""
  key_links:
    - from: "header nav (.hlinks)"
      to: "#culture"
      via: "anchor link"
      pattern: "href=\"#culture\""
    - from: "section#culture"
      to: "existing section styling"
      via: "reuses .stamp / .mis / .beat or .il classes"
      pattern: "class=\"stamp"
---

<objective>
Add a Platform Culture section: the lore (knowledge that survives by being passed on), the commons (a shared, signed graph nobody pays for twice), and the folk (the peers who hold it). It is the narrative/community heart of the page, distinct from the mechanism/proof which are the technical heart.

Purpose: SITE-03 — the brand thesis ("the lore is the graph; the folk are the peers") currently appears only as a one-line Name flourish. This section gives it room to breathe as a stated culture/philosophy.
Output: A new `<section id="culture">` in site/index.html plus a navbar anchor link.

CRITICAL: this plan edits site/index.html and runs AFTER 27-01 (which also edits it). It is the sole editor in wave 2 — no other plan may touch site/index.html concurrently.
</objective>

<execution_context>
@/Users/saharbarak/.claude/get-shit-done/workflows/execute-plan.md
@/Users/saharbarak/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-site-build-out/27-CONTEXT.md
@docs/brand/README.md
@.planning/phases/27-site-build-out/27-01-SUMMARY.md

<interfaces>
<!-- Reuse the existing visual system. Key blocks (already styled): -->
  .stamp                — yellow sticker chapter label
  h2 + .mis             — misregistered heading; data-text MUST equal the visible plain text
  .em-blue/.em-pink/.em-teal — accent spans
  .body / .lede         — clamped body + lede type (do NOT enlarge)
  .rule                 — short ink bar
  .beat                 — 2-col text+visual grid (alternates side on :nth-of-type(even)); see #alone (~284), #together (~302), #coin (~413)
  .il / .illum          — numbered card grid (see #mechanism ~320)
  .rv / data-d          — scroll reveal (IntersectionObserver adds .in)
  Folk SVG symbol library (defs ~205-222): #sun #bird #flower #moon #star #fish #hand — usable via <svg class="fg COLOR" ...><use href="#sun"/></svg> for margin accents (see hero ~252, memes ~396). Keep accents in margins, never over text columns.
  Available imagery in site/assets/gen/: campfire.png (the lone reader by the fire — the hearth), commons.png (the gathering — poster for the commons video), char1/bird/sun/fishflower/sun/coin/tee/meme-*.png.

Brand thesis (docs/brand/README.md) — the source material for the copy:
  - Why "Folklore": knowledge that survives by being passed on — story to story, peer to peer — never relearned from scratch. The lore is the graph; the folk are the peers.
  - The bet (in this order): works ALONE on day one; COMPOUNDS when peers join. Product first, protocol second.
  - Logo = a hearth and its tales: a warm central node with story-nodes radiating out.
  - Voice: plainspoken, folk-warm, metaphor (hearth, tales, the commons, stone soup); never "revolutionary"/"paradigm"/cold protocol-speak.

Existing navbar after 27-01 (~line 243) will read:
  <a href="#guidebook">Guidebook</a><a href="#mechanism">Mechanism</a><a href="#proof">Proof</a><a href="#join">Join</a><a ...>RFC</a>
</interfaces>

@site/index.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the Platform Culture section</name>
  <read_first>site/index.html (full, post-27-01) — especially the .beat markup of #alone (~284) and #coin (~413) as layout templates, the .stamp/.mis/.body CSS, and the Name section (~375) which states the thesis you are expanding</read_first>
  <files>site/index.html</files>
  <action>
Insert `<section id="culture">` at a narrative-sensible spot: place it immediately AFTER the `#together` section's closing `</section>` (~line 318) and BEFORE `<section id="mechanism">` (~line 320). Rationale: problem → alone → together → **culture (why this matters / the commons)** → mechanism (how) → proof → guidebook → join. Culture bridges the emotional "together" beat into the technical mechanism.

Build it with existing classes. Structure it as three short narrative beats — the lore, the commons, the folk — using either the `.illum`/`.il` three-up grid OR stacked `.beat` rows (pick whichever reads better; `.illum` with 3 cards is the cleaner fit and the per-child shadows give pink/blue/yellow rhythm). Skeleton:

  <section id="culture">
    <div class="wrap">
      <div class="stamp rv">Platform Culture</div>
      <h2 class="rv" data-d="1"><span class="mis" data-text="a commons, not a cloud."> ... <span class="em-teal">not a cloud.</span></span></h2>
      <div class="rule rv"></div>
      <p class="lede rv" data-d="1">{the bet, in order: it works alone on day one; it compounds when peers join}</p>
      <div class="illum">
        <div class="il rv" data-d="1"><div class="num">·</div><h4>The Lore</h4><p>{knowledge that survives by being passed on — story to story, peer to peer — never relearned from scratch. The lore is the graph.}</p></div>
        <div class="il rv" data-d="2"><div class="num">·</div><h4>The Commons</h4><p>{a shared, signed pool of ground-out answers. Nobody pays for the same inference twice. Stone soup: each peer drops in what they learned, everyone eats.}</p></div>
        <div class="il rv" data-d="1"><div class="num">·</div><h4>The Folk</h4><p>{the peers who hold it. Every record signed by a real, named human — no central server, no anonymous scrape. The folk are the peers.}</p></div>
      </div>
    </div>
  </section>

Copy requirements:
- Voice strictly per docs/brand/README.md: plainspoken, folk-warm, metaphor-driven (hearth, tales, the commons, stone soup). NO "revolutionary"/"paradigm"/cold protocol-speak.
- State the bet in the right order (alone first, then compounds).
- Land the thesis line "the lore is the graph; the folk are the peers" somewhere (it may echo the Name section — that is fine, this section earns it).
- Keep type clamped — reuse `.lede` / `.body` / `.h4` scale, no enlarging overrides.
- Optionally add ONE margin folk-shape accent via `<svg class="fg COLOR" style="top:..;left/right:..;width:..px;--rot:..;--fdur:..s"><use href="#flower"/></svg>` inside the section but OUTSIDE `.wrap` (mirror hero ~252) — keep it in the margin, never over the text column. Optional, skip if it crowds.
- If you reuse an asset image (e.g. campfire.png for "the hearth"), border it with the standard `border:3px solid var(--ink);box-shadow:Npx Npx 0 var(--COLOR)` treatment used elsewhere, and keep it inside a `.rv` wrapper.

Then add the navbar link: in `.hlinks` (~line 243), insert `<a href="#culture">Culture</a>`. Suggested order: Guidebook, Culture, Mechanism, Proof, Join, RFC — but if the nav gets too wide at the 820px breakpoint it already hides on mobile (`@media(max-width:820px){.hlinks{display:none}}`), so width is only a desktop concern; keep all links on one line.

Do NOT touch src/. No new deps. Do NOT break any `<video>`.
  </action>
  <verify>
    <automated>grep -c 'id="culture"' site/index.html | grep -q '^1$' && grep -q 'href="#culture"' site/index.html && grep -q 'class="stamp' site/index.html && grep -qi 'the lore is the graph' site/index.html && grep -c 'id="guidebook"' site/index.html | grep -q '^1$' && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'id="culture"' site/index.html` returns exactly 1.
    - `grep 'href="#culture"' site/index.html` matches (navbar anchor wired).
    - The Guidebook from 27-01 is still intact: `grep -c 'id="guidebook"'` still returns 1 (this plan did not clobber the prior section).
    - The thesis line is present: `grep -i 'the lore is the graph' site/index.html` matches.
    - Three culture beats present: the strings "The Lore", "The Commons", "The Folk" (or equivalent h4 titles) all appear.
    - Headless screenshot evidence at 1512px: run Chrome `--headless=new --screenshot=/tmp/culture-1512.png --window-size=1512,3200 "file://$(pwd)/site/index.html"` and Read it — the Culture section renders with stamp + heading + three styled beats, folk-pop consistent, no overflow or broken cards.
  </acceptance_criteria>
  <done>A Platform Culture section exists between #together and #mechanism, tells the lore/commons/folk story in the brand voice with the thesis line, reuses the existing section system, is navbar-linked, and does not break the 27-01 Guidebook. Screenshot confirms consistency.</done>
</task>

</tasks>

<verification>
- `grep -c 'id="culture"' site/index.html` == 1 and `grep -c 'id="guidebook"' site/index.html` == 1 (both sections coexist).
- Navbar contains both `href="#culture"` and `href="#guidebook"`.
- Headless screenshot at 1512px confirms folk-pop consistency and no layout breakage.
- `git diff` touches only site/index.html; no new external dependency.
</verification>

<success_criteria>
SITE-03 satisfied: a Platform Culture section presents the lore, the commons, and the folk in the locked folk-pop section system, navbar-linked, in the brand voice.
</success_criteria>

<output>
After completion, create `.planning/phases/27-site-build-out/27-02-SUMMARY.md`.
</output>
