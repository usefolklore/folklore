---
phase: 27-site-build-out
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [site/index.html]
autonomous: true
requirements: [SITE-02]
must_haves:
  truths:
    - "A visitor can scroll to a Guidebook section that explains how Folklore works in steps"
    - "The Guidebook shows the four real get-started moves: install, hooks/onboard, ask, peer"
    - "The Guidebook renders within the existing section system (min-height:100svh, scroll-snap, stamp header, hard shadows, clamped type) and matches the folk-pop skin"
    - "The navbar links to the new Guidebook section via an in-page anchor"
  artifacts:
    - path: "site/index.html"
      provides: "A new <section id=\"guidebook\"> with stamp header, misregistered h2, and step cards covering install/hooks/ask/peer"
      contains: "id=\"guidebook\""
  key_links:
    - from: "header nav (.hlinks)"
      to: "#guidebook"
      via: "anchor link"
      pattern: "href=\"#guidebook\""
    - from: "section#guidebook"
      to: "existing section styling"
      via: "reuses .stamp / .mis / .il or .card classes"
      pattern: "class=\"stamp"
---

<objective>
Add a Guidebook section to the folk-pop site: how Folklore works and how to get started, presented as the four real moves (install, hooks/onboard, ask the graph, add a peer). It must live inside the existing section system and folk-pop skin — no new CSS framework, no new deps.

Purpose: SITE-02 — the site currently explains the *why* (problem/alone/together) and the *mechanism* abstractly, but never gives a visitor a concrete "here is how you actually use it" walkthrough. The Guidebook is that walkthrough.
Output: A new `<section id="guidebook">` in site/index.html plus a navbar anchor link to it.
</objective>

<execution_context>
@/Users/saharbarak/.claude/get-shit-done/workflows/execute-plan.md
@/Users/saharbarak/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/27-site-build-out/27-CONTEXT.md
@docs/brand/README.md

<interfaces>
<!-- The site is a single self-contained file. These are the existing CSS classes and
     DOM conventions the new section MUST reuse — do NOT invent a new visual system. -->

Existing section shell (site/index.html ~line 74):
  section{min-height:100svh;display:flex;flex-direction:column;justify-content:center;padding:clamp(56px,9vh,90px) 0;...;scroll-snap-align:start}
  Every content section wraps its content in: <div class="wrap"> ... </div>

Existing reusable building blocks (already styled, use as-is):
  .stamp        — the yellow sticker chapter label (e.g. <div class="stamp rv">The Store</div>)
  h2 + .mis     — misregistered display heading: <h2 class="rv" data-d="1"><span class="mis" data-text="plain text"><span class="em-blue">accent</span></span></h2>
                  NOTE: data-text MUST equal the visible plain text for the misregister offset to line up.
  .em-blue / .em-pink / .em-teal — accent color spans inside headings
  .rule         — short ink underline bar
  .body / .lede — body + lede paragraph type (already clamped — do NOT enlarge)
  .illum + .il  — 2-col card grid with numbered cards (.num roman numeral, colored per-child shadow). Used by the Mechanism section (~line 320). IDEAL pattern for a 4-step guidebook.
  .card         — mono code-style panel with hard shadow (used by alone/together beats)
  .rv / data-d  — scroll-reveal: add class="rv" (and optional data-d="1|2|3" for stagger). The IntersectionObserver at the bottom of the file adds .in.
  .copy / data-copy — copy-to-clipboard chip (see hero .install ~line 262): <span class="copy" data-copy="text to copy">copy</span>

Real CLI commands already shown elsewhere on the page (use these EXACT commands — they are the product's real surface; do not invent flags):
  npm install -g @usefolklore/folklore     (hero install line ~262)
  folklore onboard                          (join section ~362 — "daemon, hooks, identity")
  folklore save --type synthesis --label "..." --text "..."   (~365)
  folklore peer add /ip4/.../tcp/4001/p2p/12D3KooW...          (~369)
  folklore ask "..." --peers                                   (~370)

The deny-on-confidence hook behavior (from CLAUDE.md, for accurate copy):
  PreToolUse hook intercepts WebSearch/WebFetch; asks the graph first; denies the web call when satisfaction ≥ 0.85 with ≥ 2 hits; otherwise the fetch proceeds and a PostToolUse hook auto-saves the result. Local Read/Grep/Glob are never touched.

Existing navbar (~line 242-245):
  <nav class="hlinks">
    <a href="#mechanism">Mechanism</a><a href="#proof">Proof</a><a href="#join">Join</a>
    <a href="https://github.com/usefolklore/folklore/tree/main/docs/rfc">RFC</a>
  </nav>
</interfaces>

@site/index.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the Guidebook section</name>
  <read_first>site/index.html (full — especially the section/.stamp/.mis/.illum/.il/.card/.copy CSS ~lines 74-148 and the Mechanism section markup ~lines 320-332 as the layout template)</read_first>
  <files>site/index.html</files>
  <action>
Insert a new content section so the page reads: ... mechanism → proof → join → **guidebook** ... Place the new `<section id="guidebook">` immediately AFTER the `#join` section's closing `</section>` (~line 373) and BEFORE the `<section>` containing `<div class="wrap name">` (~line 375). This puts "here's how to use it" right after "join the telling," before the closing Name flourish.

Build the section using ONLY existing classes (no new CSS unless a small scoped style block is unavoidable — if so, add it just before `</head>` near the other component rules and keep it under ~12 lines):

  <section id="guidebook">
    <div class="wrap">
      <div class="stamp rv">The Guidebook</div>
      <h2 class="rv" data-d="1"><span class="mis" data-text="four moves to never research twice."> ... <span class="em-blue">never research twice.</span></span></h2>
      <div class="rule rv"></div>
      <p class="body rv" data-d="1">{one-sentence framing: it works alone on day one, compounds when peers join}</p>
      <div class="illum">
        <div class="il rv" data-d="1"><div class="num">i</div><h4>Install</h4><p>{copy} </p>{mono command line, reuse the .card / .install pattern with a .copy chip for `npm install -g @usefolklore/folklore`}</div>
        <div class="il rv" data-d="2"><div class="num">ii</div><h4>Hooks &amp; onboard</h4><p>{folklore onboard installs the daemon, the PreToolUse deny-on-confidence hook, and your signed identity. Explain in one line that the hook gates WebSearch/WebFetch and never touches local Read/Grep/Glob.}</p></div>
        <div class="il rv" data-d="1"><div class="num">iii</div><h4>Ask the graph</h4><p>{folklore ask "..." answers from your own graph first — milliseconds, zero tokens — and only falls back to the web on a miss, filing the result for next time.}</p></div>
        <div class="il rv" data-d="2"><div class="num">iv</div><h4>Add a peer</h4><p>{folklore peer add ... then folklore ask "..." --peers — the first hop becomes "what does the network already know?", every record signed by a named hand.}</p></div>
      </div>
    </div>
  </section>

Requirements for the copy:
- Use the EXACT real commands listed in <interfaces> — do not invent flags or rename the binary.
- Keep type clamped: do NOT add font-size overrides that enlarge .body/.h4/.h2. Reuse existing scale.
- At least one command must be copy-to-clipboard via a `.copy` chip with `data-copy` set to the exact command (mirror the hero `.install` markup ~line 262).
- Voice: plainspoken folk-pop per docs/brand/README.md ("answered from your own graph in milliseconds", "nobody pays for it twice", "signed by a real, named human"). Back the deny claim with the real 0.85 / 2-hits threshold.
- The four `.il` cards inherit the per-child colored shadows (pink/blue/yellow/teal) automatically from `.il:nth-child(n)` — do not override them.

Then add the navbar link. In the `.hlinks` nav (~line 243), insert `<a href="#guidebook">Guidebook</a>` before the `#mechanism` link (reading order: Guidebook, Mechanism, Proof, Join, RFC). Keep it on the same compact line.

Do NOT touch src/. Do NOT add external dependencies. Do NOT break the hero/commons/graph `<video>` elements.
  </action>
  <verify>
    <automated>grep -c 'id="guidebook"' site/index.html | grep -q '^1$' && grep -q 'href="#guidebook"' site/index.html && grep -q 'data-copy="npm install -g @usefolklore/folklore"' site/index.html && grep -Eq 'folklore (onboard|ask|peer add)' site/index.html && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'id="guidebook"' site/index.html` returns exactly 1.
    - `grep 'href="#guidebook"' site/index.html` matches (navbar anchor wired).
    - `grep 'class="stamp' site/index.html` shows the new section reuses the stamp label (folk-pop skin preserved).
    - `grep 'data-copy="npm install -g @usefolklore/folklore"' site/index.html` matches (a real, copyable install command is present).
    - All four real moves appear: `grep -Eq 'folklore (onboard|ask|peer add)'` passes AND the four `<h4>` step titles (Install / Hooks / Ask / peer) are present.
    - Headless screenshot evidence: run `"/Applications/Google Chrome.app/Contents/MacOS/Google Chrome" --headless=new --screenshot=/tmp/guide-1512.png --window-size=1512,2400 "file://$(pwd)/site/index.html"` then Read /tmp/guide-1512.png — the Guidebook section is visible with its stamp, heading, and a 2x2 (or stacked) grid of four numbered step cards, styled consistently with the Mechanism section, no broken layout.
  </acceptance_criteria>
  <done>A Guidebook section exists between #join and the Name section, presents install/hooks/ask/peer as four styled step cards using existing classes, has a copyable real install command, and is linked from the navbar. Screenshot confirms folk-pop consistency.</done>
</task>

</tasks>

<verification>
- `grep -c 'id="guidebook"' site/index.html` == 1; navbar `href="#guidebook"` present.
- Headless Chrome screenshot at 1512px width shows the Guidebook rendered in the existing section system with no layout breakage.
- No new external dependency added (no new `<link>`/`<script src>` to a non-Google-Fonts host). `git diff` touches only site/index.html.
</verification>

<success_criteria>
SITE-02 satisfied: a Guidebook section explains how Folklore works and how to get started (install, hooks/onboard, ask, peer), inside the locked folk-pop section system, linked from the navbar, with at least one copyable real command.
</success_criteria>

<output>
After completion, create `.planning/phases/27-site-build-out/27-01-SUMMARY.md`.
</output>
