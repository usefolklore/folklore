---
phase: 26-docs-and-benchmarks
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/profile/README.md
  - docs/REPO-SPLIT.md
autonomous: true
requirements: [DOCS-03]
must_haves:
  truths:
    - "A staged org-profile README exists at .github/profile/README.md"
    - "The README is product-first and folk-pop in voice (hearth/tales/commons metaphor; monochrome + one warm accent discipline; 'never research twice' line)"
    - "The README leads with what Folklore does (works alone day one, compounds with peers), not with the protocol pitch"
    - "Claims in the README are backed by real numbers consistent with the BENCHMARKS page (no invented metrics)"
    - "REPO-SPLIT.md notes that the staged profile ships to usefolklore/.github when the org exists"
  artifacts:
    - path: ".github/profile/README.md"
      provides: "Staged usefolklore org-profile landing README"
      min_lines: 25
    - path: "docs/REPO-SPLIT.md"
      provides: "Updated org-split note pointing the staged profile at usefolklore/.github"
      contains: "profile/README.md"
  key_links:
    - from: "docs/REPO-SPLIT.md"
      to: ".github/profile/README.md"
      via: "staging-location note"
      pattern: "profile/README.md"
---

<objective>
Author the usefolklore org-profile README (the landing text a visitor sees on the org's GitHub page) and stage it where the split plan can later lift it. The org doesn't exist yet (blocked on user), so it lands at `.github/profile/README.md` as a staging copy, and docs/REPO-SPLIT.md is updated to record that this file is what ships to the `usefolklore/.github` repo.

Purpose: DOCS-03 — org profile README for the usefolklore org landing. Folk-pop brand voice, product-first.
Output: .github/profile/README.md (new, staged) + a one-line note in docs/REPO-SPLIT.md pointing to it.
</objective>

<execution_context>
@/Users/saharbarak/.claude/get-shit-done/workflows/execute-plan.md
@/Users/saharbarak/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@docs/brand/README.md
@docs/REPO-SPLIT.md
@docs/product/BENCHMARKS.md
@CLAUDE.md

<facts>
<!-- Brand voice + the real numbers the README may quote. Sources: docs/brand/README.md, docs/product/BENCHMARKS.md, bench/README.md, STATE.md. -->

Brand (from docs/brand/README.md):
  - One-liner: "Your agent never researches the same thing twice."
  - The bet (in this order): it works ALONE on day one; it COMPOUNDS when peers join. Product first, protocol second.
  - Why "Folklore": knowledge that survives by being passed on — story to story, peer to peer — never relearned from scratch. The lore is the graph; the folk are the peers.
  - Logo metaphor: a hearth and its tales — a warm central node with story-nodes radiating out; campfire AND peer graph.
  - Voice: plainspoken, confident, a little folk-warm; explain by metaphor (hearth, tales, the commons, stone soup); back every claim with a falsifiable number; never oversell.
  - Color discipline: monochrome + ONE warm accent (Hearth #ff9f43). Resist cold blue/purple "AI protocol" default.
  - Merch line: "never research twice".

Real, quotable numbers (must stay consistent with the BENCHMARKS page — DO NOT invent):
  - 72.30% NDCG@10 on full BEIR SciFact (honest pure-Node hybrid headline). Optional Rust sidecar -> 75.22% (same dataset).
  - 11 ms p50 retrieval, zero GPU, zero cloud, 137M params.
  - Local-first: works alone day one, compounds peer-to-peer.
  - The network-before-web deny-on-confidence gate (PreToolUse hook).
  Caveat: if quoting a federation web-fallback number (17%->1%), it is a SIMULATOR figure — label it or omit it. Prefer omitting on a landing page; if used, mark it simulator.

Staging path: .github/profile/README.md (NOT to be confused with .github/README.md). GitHub renders profile/README.md from the special `.github` repo as the org/user landing. We stage it here; the physical push to usefolklore/.github is blocked on org creation.

REPO-SPLIT.md current state: the "Target repos" table row for `usefolklore/.github` says "Authored in DOCS-03 (Phase 26). Do NOT create `.github/profile` in this repo now." — that line predates this plan and must be updated, because DOCS-03 (this plan) DOES stage the file. Reconcile it: the file is now staged at .github/profile/README.md as the source-of-truth content that ships to usefolklore/.github at split time (no extraction needed — it's new content). Keep the "blocked on org creation" caveat.
</facts>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the staged org-profile README</name>
  <read_first>
    - docs/brand/README.md (positioning, one-liner, "the bet" order, hearth/tales metaphor, voice rules, color discipline, merch line)
    - docs/product/BENCHMARKS.md (the real numbers — quote only figures present there; keep them consistent)
    - The <facts> block (brand voice + quotable numbers + the simulator caveat)
  </read_first>
  <action>
    Create .github/profile/README.md — the landing README a visitor sees on the usefolklore GitHub org page. Product-first, folk-pop voice.

    Structure (lead with the product, end with the protocol):
    - A short warm hero: the org name / Folklore, the one-liner "Your agent never researches the same thing twice." Keep the hearth/tales framing (the lore is the graph; the folk are the peers).
    - "What it is" in 2-3 sentences: a local-first memory + research layer for AI agents that answers before the web and federates peer-to-peer.
    - "The bet" in the brand's order: works ALONE on day one; COMPOUNDS when peers join. Product first, protocol second.
    - A small proof line with REAL numbers: 72.30% NDCG@10 on full BEIR SciFact (pure-Node hybrid; 75.22% with the optional Rust sidecar), 11 ms p50, zero GPU/cloud, 137M params. Each number must match the BENCHMARKS page. Do NOT quote the federation 17%->1% figure as a measured result — omit it (it is simulator-only), or if mentioned, label it a simulator.
    - "How it works" in one folk-warm paragraph: the network-before-web deny-on-confidence gate — when your graph (or a peer's) already holds a confident answer, the agent reads it instead of paying the web trip; otherwise the web call goes through and the result is saved so nobody pays for it twice.
    - A short "Repos" / "Where to look" pointer block (folklore = the engine + CLI; folklore-spec = the protocol + RFCs; folklore-site = the site). These mirror the REPO-SPLIT target repos; keep it light (the repos may not all exist yet).
    - Close on the brand line "never research twice."

    Voice rules: plainspoken, confident, a little folk-warm; explain by metaphor; back every claim with a real number; never oversell ("revolutionary"/"paradigm" banned). Color discipline does not apply to Markdown but keep any emoji/visual restraint — monochrome + one warm accent in spirit; do NOT add a wall of badges. Keep it skimmable: this is a landing page, not a manual.

    Do NOT invent metrics. Do NOT claim the org or its sibling repos exist as live links if they don't — describe them, don't hard-link to unborn repos.
  </action>
  <acceptance_criteria>
    - File `.github/profile/README.md` exists and has at least 25 lines: `test "$(wc -l < .github/profile/README.md)" -ge 25` exits 0
    - The one-liner is present: `grep -qi "never research" .github/profile/README.md` exits 0
    - Product-first proof number present and consistent with BENCHMARKS: `grep -q "72.30" .github/profile/README.md` exits 0
    - The "works alone / compounds" bet is present: `grep -qiE "alone|compound" .github/profile/README.md` exits 0
    - The deny/network-before-web mechanism is described: `grep -qiE "before the web|network-before-web|deny|answers before" .github/profile/README.md` exits 0
    - No unlabeled federation simulator claim: `grep -q "17%" .github/profile/README.md && ! grep -qi "simulator" .github/profile/README.md` exits 1 (i.e. if 17% appears it MUST be near a "simulator" label; preferred: 17% absent)
    - Oversell words banned: `grep -qiE "revolutionary|paradigm" .github/profile/README.md` exits 1
  </acceptance_criteria>
  <done>.github/profile/README.md reads as a warm, product-first org landing in the Folklore voice, leads with what the tool does and the alone/compounds bet, quotes only real numbers consistent with the BENCHMARKS page, and closes on the brand line.</done>
</task>

<task type="auto">
  <name>Task 2: Point REPO-SPLIT.md at the staged profile</name>
  <read_first>
    - docs/REPO-SPLIT.md (the "Target repos" table row for `usefolklore/.github`, and the "usefolklore/.github (org profile)" subsection under Migration mechanics — both currently say "Do NOT create .github/profile in this repo now")
    - .github/profile/README.md (created in Task 1 — the staged source-of-truth)
  </read_first>
  <action>
    Reconcile docs/REPO-SPLIT.md with the fact that DOCS-03 has now staged the profile README. Two edits:

    1. In the "Target repos" table, the `usefolklore/.github` row currently reads (paraphrased) "Authored in DOCS-03 (Phase 26). Do NOT create .github/profile in this repo now." Update it to state the profile README is staged at `.github/profile/README.md` and ships to `usefolklore/.github` as new content (no git-filter-repo extraction needed) once the org exists.

    2. In the "Migration mechanics" -> "usefolklore/.github (org profile)" subsection, update the "Created fresh in Phase 26 (DOCS-03)" note to point at the staged path `.github/profile/README.md` as the source-of-truth content to copy into the new repo. Keep the "blocked on org creation" caveat — the physical push is deferred.

    Additive/surgical edits only. Do not restructure the document or change other rows.
  </action>
  <acceptance_criteria>
    - `grep -q "profile/README.md" docs/REPO-SPLIT.md` exits 0 (the staging path is referenced)
    - `grep -qi "staged\|staging" docs/REPO-SPLIT.md` exits 0
    - The blocked-on-org caveat survives: `grep -qi "org exists\|blocked on" docs/REPO-SPLIT.md` exits 0
    - The stale "Do NOT create .github/profile in this repo now" instruction no longer reads as current guidance: `grep -qi "do not create .github/profile" docs/REPO-SPLIT.md` exits 1
    - `git diff --name-only` lists only docs/REPO-SPLIT.md and .github/profile/README.md
  </acceptance_criteria>
  <done>docs/REPO-SPLIT.md records that .github/profile/README.md is the staged org-profile content that ships to usefolklore/.github at split time, the contradicting "do not create" instruction is resolved, and the blocked-on-org caveat is preserved.</done>
</task>

</tasks>

<verification>
- .github/profile/README.md exists (>= 25 lines), product-first, folk-pop voice, real numbers only, brand line present.
- No unlabeled federation simulator claim on the landing page.
- docs/REPO-SPLIT.md points at the staged path and keeps the blocked-on-org caveat; the stale "do not create" line is reconciled.
- No source/site files modified: `git diff --name-only` lists only .github/profile/README.md and docs/REPO-SPLIT.md.
- `npx tsc --noEmit` still exits 0 (sanity gate — no source touched).
</verification>

<success_criteria>
A staged org-profile README at .github/profile/README.md exists, suitable for the usefolklore org landing — product-first, folk-pop voice, backed by real numbers consistent with the BENCHMARKS page — and docs/REPO-SPLIT.md records it as the source-of-truth that ships to usefolklore/.github once the org exists. No source changes; tsc green. (DOCS-03)
</success_criteria>

<output>
After completion, create `.planning/phases/26-docs-and-benchmarks/26-03-SUMMARY.md`.
</output>
