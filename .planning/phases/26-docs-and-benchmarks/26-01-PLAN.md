---
phase: 26-docs-and-benchmarks
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/product/BENCHMARKS.md
autonomous: true
requirements: [DOCS-01]
must_haves:
  truths:
    - "A reader can see the single honest headline retrieval number and which pipeline produced it"
    - "The 72.30% Wave-2 hybrid figure and the 75.22% Phase-25 Rust figure are explicitly reconciled (same SciFact dataset, different pipeline), not presented as two unrelated claims"
    - "The site's 0.7522 LED is explained as the Phase-25 Rust bge-base figure, consistent with the page"
    - "The honest failures (Wave-3 reranker -1.92, Wave-4 routing null) are presented as failures, not buried"
    - "The FolkloreBench-F 17%->1% federation number is labeled a SIMULATOR figure, not a measured production result"
    - "Every headline claim has a copy-paste reproduction command pointing at bench/"
  artifacts:
    - path: "docs/product/BENCHMARKS.md"
      provides: "Reconciled, honesty-audited benchmarks page"
      contains: "72.30"
  key_links:
    - from: "docs/product/BENCHMARKS.md"
      to: "bench/bench-beir-sota.mjs"
      via: "repro command"
      pattern: "bench/bench-beir-sota.mjs scifact --hybrid"
    - from: "docs/product/BENCHMARKS.md"
      to: "bench/bench-beir-rust.mjs"
      via: "repro command"
      pattern: "bench/bench-beir-rust.mjs scifact --model bge-base"
---

<objective>
Make docs/product/BENCHMARKS.md launch-honest: reconcile the two SciFact NDCG@10 figures that currently coexist without explanation (Wave-2 hybrid 72.30% vs Phase-25 Rust bge-base 75.22%, which is also the site's 0.7522 LED), surface the documented failures as failures, and label the FolkloreBench-F federation figure (17%->1%) explicitly as a simulator result. Every headline claim gets a copy-paste repro command into bench/.

Purpose: DOCS-01 — a BENCHMARKS page a skeptical reader trusts. The numbers must match the repo; nothing invented; the honesty is the differentiator (the page already documents 13 null attacks — extend that discipline to the headline reconciliation).
Output: An edited docs/product/BENCHMARKS.md with a clear "which number is the headline and why" reconciliation block, a simulator label on FolkloreBench-F, and verified repro commands.
</objective>

<execution_context>
@/Users/saharbarak/.claude/get-shit-done/workflows/execute-plan.md
@/Users/saharbarak/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/product/BENCHMARKS.md
@bench/README.md
@docs/architecture/RETRIEVAL-MODULES.md

<facts>
<!-- Authoritative, cross-checked numbers. Use these EXACTLY. Do not invent or round differently. -->
<!-- Sources: bench/README.md "Honest headline number", RETRIEVAL-MODULES.md §5, MILESTONES.md v2.0 SOTA table, STATE.md "Retrieval / benchmarks". -->

SciFact NDCG@10 progression (all full BEIR SciFact, 5,183 docs × 300 queries):
  - Baseline MiniLM-L6 dense only ......................... 64.82%
  - Wave 1  + nomic-embed-text-v1.5 (768d, Xenova) ........ 69.98%  (+5.16)
  - Wave 2  + BM25 FTS5 hybrid, RRF k=60 ................. 72.30%  (+2.32)  <- HONEST CPU-LOCAL HEADLINE (pure Node, no Rust)
  - Phase 25 + Rust bge-base sidecar + hybrid ............ 75.22%  (+2.92)  <- = the site's 0.7522 LED (requires Rust sidecar built)
  - Calibrated gpt-oss:20b judge (κ=0.7053) ............. ~81.06%  (instrument-corrected ceiling, 50-query subset)

The retired 96.8% (v1.1) came from a 15-passage × 10-query mini-harness — NOT leaderboard-comparable. Already noted as retired; keep it noted.

Documented FAILURES (must appear as failures):
  - Wave 3  bge-reranker-base cross-encoder ......... -1.92 NDCG@10  (MS-MARCO domain mismatch on scientific text)
  - Wave 4  oracle room routing on CQADupStack ...... +0.34 NDCG@10  (below the +3pt gate -> null; rooms/workspaces are UX/permissions/discovery, NOT a retrieval signal)
  - (plus the existing 13-null-attacks table — leave intact)

FolkloreBench-F federation: web-fallback 17% -> 1% is a SIMULATOR figure.
  - It is a federation-level simulator (10 peers, 20% offline churn, Zipfian demand) measuring web_fallback_rate(t).
  - docs/whitepaper.html §7.2/Figure 2 labels it "a demonstration, not validated evidence" — under v1's boolean-retrieval abstraction the decay is partly true by construction.
  - On the BENCHMARKS page it MUST be labeled SIMULATOR (not a measured production result).

Repro commands (verified against bench/README.md — these runners exist on disk):
  - Wave 2 hybrid (pure Node):     node bench/bench-beir-sota.mjs scifact --hybrid
  - Phase 25 Rust headline:        FOLKLORE_RUST_BIN=$(pwd)/folklore-rs/target/release/embed_server node bench/bench-beir-rust.mjs scifact --model bge-base   (after: cd folklore-rs && cargo build --release && cd ..)
  - Wave 3 reranker null:          node bench/bench-beir-sota.mjs scifact --hybrid --rerank
  - Wave 4 routing null:           node bench/bench-room-routing.mjs --datasets-dir ~/.folklore/bench/cqadupstack/cqadupstack --rooms mathematica,webmasters,gaming
  - Value-model trio:              node bench/bench-compounding.mjs ; node bench/bench-subgraph-transfer.mjs ; node bench/bench-value-model.mjs
  - All runners need `npm run build` first (they import ../dist/).
</facts>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add a headline reconciliation block to BENCHMARKS.md</name>
  <read_first>
    - docs/product/BENCHMARKS.md (full file — note: it already has the progression ASCII box at top, the "Phase 25 detail" leaderboard table, and the 13-null-attacks table)
    - bench/README.md "Honest headline number" section (the canonical reconciliation source)
    - The <facts> block in this plan's context (authoritative numbers)
  </read_first>
  <action>
    The page currently shows BOTH 72.30% (Wave 2, in the ASCII box) and 75.22% (Phase 25 Rust, in the box + the leaderboard table) without a single sentence stating which is "the headline" and why a reader sees 0.7522 on the site but 72.30% in bench/README.md. Fix that ambiguity.

    Add a short prose reconciliation block immediately after the opening ASCII progression box (before "## Value model"). Use a `### Which number is the headline` subsection. State plainly, in folk-pop-but-precise voice:
    - The honest CPU-only, no-Rust, pure-Node headline is **72.30% NDCG@10** on full BEIR SciFact (Wave 2: nomic-embed-text-v1.5 dense + BM25 FTS5 hybrid, RRF k=60). This is the number a fresh clone reproduces with zero extra build steps.
    - With the optional Rust bge-base sidecar built, the same SciFact dataset and hybrid pipeline reaches **75.22% NDCG@10** — this is the number the site's `0.7522` LED shows. Same dataset, same hybrid fusion, heavier embedder + native acceleration.
    - Both are full-BEIR-SciFact (5,183 × 300), directly comparable to the MTEB BEIR leaderboard. The retired 96.8% (v1.1) is NOT comparable (15-passage mini-harness) and is not used.
    - Point the reader at the two repro commands (Wave 2 pure-Node; Phase 25 Rust) so they can verify either tier themselves.

    Do NOT invent any number. Use only the figures in <facts>. Do NOT delete the existing ASCII box or the leaderboard table — this block reconciles them, it does not replace them.
  </action>
  <acceptance_criteria>
    - `grep -c "72.30" docs/product/BENCHMARKS.md` returns >= 2 (existing box + new block)
    - `grep -q "0.7522\|75.22" docs/product/BENCHMARKS.md` exits 0 AND the new block mentions the site LED
    - `grep -qi "headline" docs/product/BENCHMARKS.md` exits 0 (the reconciliation subsection exists)
    - `grep -q "bench/bench-beir-sota.mjs scifact --hybrid" docs/product/BENCHMARKS.md` exits 0
    - `grep -q "bench/bench-beir-rust.mjs scifact --model bge-base" docs/product/BENCHMARKS.md` exits 0
    - `git diff --stat docs/product/BENCHMARKS.md` shows only insertions/edits to that one file
  </acceptance_criteria>
  <done>A reader sees one explicit statement of which number is the headline (72.30% pure-Node), why 75.22%/0.7522 also appears (Rust sidecar, same dataset), and a repro command for each.</done>
</task>

<task type="auto">
  <name>Task 2: Label FolkloreBench-F as a simulator and audit the failures + repro coverage</name>
  <read_first>
    - docs/product/BENCHMARKS.md (after Task 1's edit — re-read; the file changed)
    - The <facts> block (FolkloreBench-F simulator labeling + the Wave-3/Wave-4 failure rows)
    - docs/whitepaper.html lines ~333-395 (the existing "demonstration, not validated evidence" framing for FolkloreBench-F — mirror that honesty)
  </read_first>
  <action>
    Three honesty edits in docs/product/BENCHMARKS.md:

    1. FolkloreBench-F simulator label. The 17%->1% federation web-fallback number must be present AND explicitly labeled a SIMULATOR figure. If the page does not already present this federation number in prose, add a short `### Federation web-fallback (simulator)` subsection near the value-model section stating: FolkloreBench-F is a federation-level *simulator* (10 peers, 20% offline churn, Zipfian demand) measuring web_fallback_rate(t); first run shows web fallback decaying from ~17% to ~1% over the stream; this is illustrative simulator output, not a measured production result (cite the whitepaper §7.2 framing — partly true by construction under v1's boolean-retrieval abstraction). Word it so a reader cannot mistake it for a live measurement.

    2. Failures stay failures. Confirm the Wave-3 reranker (-1.92) and Wave-4 routing (+0.34, null) rows are present in the "13 null attacks" table (they already are) and that the surrounding prose frames them as honest failures, not footnotes. If the reconciliation block from Task 1 mentions Wave 3/Wave 4, ensure it does not soften them — they are failures by design ("Documented null > hypothetical positive", which the page already says).

    3. Repro-command audit. Every headline/value claim in the page must have a runnable bench/ command. Verify the existing "Reproduce" and value-model command blocks cover: Wave 2 hybrid, Phase 25 Rust, Wave 3 reranker null, Wave 4 routing null, and the value-model trio (compounding / subgraph-transfer / value-model). Add any missing command using the exact invocations in <facts>. Note that all runners require `npm run build` first (the page should say this once — bench/README.md says it; mirror it).

    Do NOT change any numeric value. Do NOT touch source or site files — this task edits only docs/product/BENCHMARKS.md.
  </action>
  <acceptance_criteria>
    - `grep -niE "folklorebench-f|web.?fallback|federation" docs/product/BENCHMARKS.md | grep -i "simulator"` returns at least one line (the simulator label is co-located with the federation figure)
    - `grep -q "17%" docs/product/BENCHMARKS.md && grep -q "1%" docs/product/BENCHMARKS.md` exits 0
    - `grep -q -- "−1.92\|-1.92" docs/product/BENCHMARKS.md` exits 0 (Wave 3 failure present)
    - `grep -q "bench/bench-beir-sota.mjs scifact --hybrid --rerank" docs/product/BENCHMARKS.md` exits 0 (Wave 3 repro)
    - `grep -q "bench/bench-room-routing.mjs" docs/product/BENCHMARKS.md` exits 0 (Wave 4 repro)
    - `grep -q "bench/bench-compounding.mjs" docs/product/BENCHMARKS.md && grep -q "bench/bench-value-model.mjs" docs/product/BENCHMARKS.md` exits 0
    - `grep -qi "npm run build" docs/product/BENCHMARKS.md` exits 0
    - `git diff --name-only` lists only docs/product/BENCHMARKS.md (no source, no site changes)
  </acceptance_criteria>
  <done>FolkloreBench-F is unmistakably labeled a simulator figure; Wave-3/Wave-4 failures read as failures; every headline and value claim has a copy-paste bench/ repro command, all gated behind `npm run build`.</done>
</task>

</tasks>

<verification>
- `grep -c "72.30" docs/product/BENCHMARKS.md` >= 2 and the headline reconciliation subsection exists.
- FolkloreBench-F 17%->1% present AND labeled "simulator".
- Wave-3 (-1.92) and Wave-4 (null) present as failures with repro commands.
- No source/site files modified: `git diff --name-only` lists only docs/product/BENCHMARKS.md.
- `npx tsc --noEmit` still exits 0 (no source touched — this is a sanity gate, run from repo root).
</verification>

<success_criteria>
docs/product/BENCHMARKS.md presents real, reconciled numbers: 72.30% as the honest pure-Node headline, 75.22%/0.7522 explained as the Rust-sidecar tier on the same dataset, the documented failures kept as failures, FolkloreBench-F labeled a simulator figure, and a copy-paste bench/ repro command behind every headline claim. No invented numbers. No source changes; tsc green. (DOCS-01)
</success_criteria>

<output>
After completion, create `.planning/phases/26-docs-and-benchmarks/26-01-SUMMARY.md`.
</output>
