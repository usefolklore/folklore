---
gsd_state_version: 1.0
milestone: v2.0
milestone_name: milestone
status: unknown
last_updated: "2026-06-15T08:21:00.000Z"
progress:
  total_phases: 7
  completed_phases: 0
  total_plans: 0
  completed_plans: 0
---

# State

## Project Reference

See: .planning/PROJECT.md (updated 2026-06-15)

**Core value:** Your coding agent answers from your actual research and codebase, not its training data.
**Current focus:** Phase 28 — Merch & Meme-Agent (COMPLETE 3/3 on disk; this session completed 28-03 site memes.json integration → AGENT-02 DONE, after 28-01 MERCH-01 earlier this session and 28-02 AGENT-01 in a prior session). Phase 28 closed: MERCH-01 + AGENT-01 + AGENT-02 all satisfied.

## Current Position

This session: completed 28-01 (MERCH-01) and 28-03 (AGENT-02). Phase 28 (Merch & Meme-Agent) COMPLETE — 3/3 plans on disk (28-01 MERCH-01 + 28-03 AGENT-02 this session + 28-02 AGENT-01 from a prior session):

- 28-03 (AGENT-02): Site memes.json integration — DONE (commit 9e1cb3e). Made the `#memes` grid data-driven via progressive enhancement: marked the grid wrapper `id="memegrid" data-static="1"` and added a guarded async IIFE to the trailing `<script>` that `fetch('assets/memes.json',{cache:'no-store'})` and, on a successful non-empty MemeEntry[] array, `replaceChildren` the six seeded static cards with one folk-pop card per entry — mirroring the exact inline card markup (3px ink border, var(--paper) bg, 8px hard shadow cycling pink/blue/teal/yellow, overflow:hidden, `img{width:100%;display:block}`), binding the MemeEntry contract from 28-02 (`image`/`alt`, `caption`→img `title`, `loading=lazy`) with NO schema drift, then `removeAttribute('data-static')`. Every failure path (file:// fetch rejection, !res.ok / 404, bad JSON, empty or non-array, no renderable entries) returns early or is swallowed by try/catch + `.catch(()=>{})`, so the six seeded cards stay in place and the section is never blank — the single-file site keeps working at file://. No browser storage (grep-clean), no new dep, no new external host, no build step; image refs stay relative; Store/$LORE block (28-01) and hero/commons/graph videos untouched. Verified four ways with headless Chrome: (a) `--dump-dom` of the REAL index.html over http → data-static removed, exactly 2 cards == memes.json length, seed alts present; (b) `--dump-dom` at file:// → data-static retained, all 6 fallback cards present (not blank); (c) agent round-trip — `node --import tsx src/agents/meme-agent/run.ts --text "round-trip check"` appended a 3rd MemeEntry (source:svg) and the UNCHANGED site rendered it over http (count 2→3, new alt in DOM), then memes.json restored to the 2-entry seed + the test SVG removed (git clean); (d) 390px full-page PNG width == 390 → zero horizontal overflow, cards stack single-column via the existing auto-fit minmax grid. tsc 0; only site/index.html committed (+32/-1). SUMMARY: .planning/phases/28-merch-and-meme-agent/28-03-SUMMARY.md.

Earlier this session (28-01 MERCH-01) and prior-session (28-02 AGENT-01) details:

- 28-01 (MERCH-01): Store merch mockups + print specs — DONE (commit c655cc1). Upgraded the three Phase-27 Store `.prod .shot` images from bare `assets/gen/*.png` catalogue art into distinct CSS-framed merch mockups built entirely from existing repo art (zero new files, zero higgsfield credits, no new external host): `.shot.tee` (dark cotton field + collar `::before` hint, art scaled to a centered ~58% chest PRINT via object-fit:contain), `.shot.sheet` (hatched paper sheet holding two dashed die-cut sticker chips, slightly rotated ±5–6°), `.shot.pin` (round metal bezel — border-radius:50% + 4px ink border + glossy inset box-shadow — framing char1.png as a hard-enamel pin). Added `position:relative` to the base `.prod .shot` rule so the tee collar pseudo-element anchors inside the framed box without affecting the other cards. Added one concrete `<p class="spec">` print line per card (between `.desc` and `.foot`) with grep-checkable material truth — Tee "Heavyweight 100% cotton · screen-printed front · S–3XL · ink on dark", Sticker Pack "Die-cut vinyl · matte laminate · weatherproof · 6-sticker sheet ~3in", Pin "Hard enamel · 1.25in · gold plating · double rubber clutch backing" — plus a scoped `.prod .spec{}` mono rule. Per-card pink/blue/teal hard shadows, the inert `data-buy` CTAs, the three `<!-- TODO: real product URL -->` comments, and the `$LORE` bags.fm sub-block all preserved by construction. Verified headless Chrome at 1512px (the three shots read as tee-print / sticker-sheet / round-pin) and 390px (single-column stack, full-page PNG width == 390 → zero horizontal overflow, spec lines render under each desc). tsc green; only site/index.html touched (+30/-4). SUMMARY: .planning/phases/28-merch-and-meme-agent/28-01-SUMMARY.md.

Phase 27 (Site Build-Out) COMPLETE — 5/5 plans (27-01..27-04 serialized on site/index.html; 27-05 is config-only, runs Wave 1 parallel):

- 27-01 (SITE-02): Guidebook section — DONE (commit 43fa0d5). Added `<section id="guidebook">` between #join and the Name flourish presenting the four real get-started moves (install / hooks-onboard / ask / add-peer) as a 2x2 grid of numbered .il step cards reusing the Mechanism layout. Card i carries a copyable `npm install -g @usefolklore/folklore` via the existing .copy chip; cards ii–iv quote the real `folklore onboard`/`folklore ask "…"`/`folklore peer add … --peers` surface with accurate deny-on-confidence copy (gates WebSearch/WebFetch, never local Read/Grep/Glob). Navbar gains a #guidebook anchor as the first link. Folk-pop skin preserved by construction (per-child colored shadows, clamped type, misregistered heading); no new CSS framework, no new deps, hero/commons/graph videos untouched. Verified by headless Chrome at 1512px (2x2 grid renders) and 390px (single column, full-page PNG width == 390 → no horizontal overflow). SUMMARY: .planning/phases/27-site-build-out/27-01-SUMMARY.md.
- 27-02 (SITE-03): Platform Culture section — DONE (commit f64582b). Added `<section id="culture">` between #together and #mechanism telling the lore / the commons / the folk story, reusing the locked `.illum`/`.il` 3-card grid (per-child pink/blue/yellow shadows) so the folk-pop brand lock holds by construction — no new CSS, no new deps. Yellow stamp "The Folklore — Platform Culture", misregistered h2 "a commons, not a cloud." (teal accent + pink misregister), ink `.rule`, and a `.lede` framing the bet in order (works **alone** day one → **compounds** when peers join → nobody grinds out the same answer twice). Three beats: The Lore (lore is the graph), The Commons (stone soup for machines; shared pool nobody pays for twice; not a vendor's cloud), The Folk (signed by a real, named hand; folk are the peers). One teal `#flower` margin accent outside `.wrap`. Navbar gains a #culture anchor (Guidebook · Culture · Mechanism · Proof · Join · RFC). Copy strictly per docs/brand/README.md voice (hearth/stone-soup/commons metaphors, no protocol-speak). 27-01 Guidebook and hero/commons/graph `<video>` untouched. Verified by headless Chrome at 1512px (stamp + heading + three colored-shadow cards, no overflow) and 390px (full-page PNG width == 390 → no horizontal overflow; cards stack single-column). SUMMARY: .planning/phases/27-site-build-out/27-02-SUMMARY.md.
- 27-03 (SITE-04): Store — DONE (commit 1477579). Rebuilt `<section id="store">` from two "Coming soon" `.merch` panels into a real product-card shop: a `.shop` grid (`repeat(auto-fit,minmax(240px,1fr))`) of three `.prod` cards — Never-Research-Twice Tee (tee.png), Folk Sticker Pack (fishflower.png + sun.png paired), The Teller Enamel Pin (char1.png) — each with a "DROPS TO PEERS FIRST" `.soon` chip, a bordered colored-hard-shadow image (pink/blue/teal), a name, a description, a `$—` price placeholder (`data-price=""`), and an INERT-but-wired buy CTA (`aria-disabled="true"` + `data-buy="<id>"` + `href="#store"` + a `<!-- TODO: real product URL at fulfilment launch -->` comment — no fabricated shop URL). Consolidated the standalone `#coin` section into a `.lore` $LORE sub-block inside `#store` (bags.fm `.btn-primary` CTA + round/bordered/hard-shadow coin.png + the verbatim "Memecoin · for fun & culture, not financial advice · contract address at launch" disclaimer) and DELETED `#coin` (nav never linked it — grep-verified, so no anchor cleanup). Small scoped `.shop`/`.prod`/`.buy`/`.lore` CSS added next to the old store rules; reused `.soon`/`.btn-primary`/`.stamp`/`.mis`/`.rule`/`.body`/`.rv`; no new deps; clamped type + folk-pop skin preserved by construction. Guidebook (#guidebook), Culture (#culture), #memes, and hero/commons/graph `<video>` untouched. Verified by headless Chrome at 1512px (three-card shop row + $LORE block with disclaimer) and 390px (full-page PNG width == 390 → no horizontal overflow; cards stack single-column, $LORE block stacks with coin first). Store is structured for the blocked-on-user launch (merch fulfilment URLs + $LORE token). SUMMARY: .planning/phases/27-site-build-out/27-03-SUMMARY.md.
- 27-04 (SITE-01): Composition + mobile responsive sweep — DONE (commit e2fa693). Final polish pass over the whole page (originals + the new Guidebook/Culture/Store) verified by headless Chrome at 1512px and 390px. Audit-first: desktop composition already balanced across all twelve sections (no desktop edits); the 390px audit found exactly two real issues — the #join `.insc pre` libp2p multiaddr + daemon/hooks comment clipping at the right edge, and the proof `.bar` 120px label column crowding the track. Fixed with a 17-line surgical diff (+15/-2): `overflow-wrap:anywhere` on `.insc pre` + `.install` so unbreakable tokens wrap inside the box, and a new `@media(max-width:480px)` block dropping the inscription mono to 12px + tighter padding, trimming the proof bar label column 120px→96px (track ~170px→~284px, labels still fit), and an `img,video{max-width:100%}` guard. Type scale, palette, folk-pop skin (hard shadows, ink borders, misregistered headers, stamps), scroll-snap, and the hero/commons/graph poster `<video>`s all untouched (grep-confirmed: overflow-x:hidden retained, guidebook/culture/store==1, hero poster video retained, no font-size clamp ceiling raised, no new external host). Verified the ≤480 rule under Chrome's 500px-min headless DOM via a throwaway 520-breakpoint clone + computed-style probe (preFont=12px, barCols=96px 284px 50px, DOC overflow=0). Document-level horizontal overflow = 0 at both widths; every section stacks single-column on mobile. SUMMARY: .planning/phases/27-site-build-out/27-04-SUMMARY.md.
- 27-05 (SITE-05): Cloudflare Pages config verified buildable — DONE (verify-only, zero file changes; deploy stays blocked-on-user). Confirmed wrangler.toml `pages_build_output_dir = "site"` (name=folklore, compat 2026-06-15), site/_headers present + valid (`/*` → X-Content-Type-Options/Referrer-Policy/X-Frame-Options), zero Vercel/Netlify remnants (vercel.json/.vercel/.vercelignore/netlify.toml/site/_redirects all absent; repo-wide vercel|netlify grep clean; package.json build script is just `tsc`). Static output well-formed — index.html + assets/gen/ present, all assets RELATIVE, no absolute/localhost refs. Proved CF-Pages-equivalent local serve: GET / → 200 (title `Folklore · oral tradition, set down for machines`), GET /assets/gen/hero.png → 200. No deploy/login/publish run. SUMMARY: .planning/phases/27-site-build-out/27-05-SUMMARY.md.

Phase 26 (Docs & Benchmarks) COMPLETE (3/3 plans). Phase 25 (Cleanup & Repo Restructure) COMPLETE (5/5 plans).

Phase: 25 (Cleanup & Repo Restructure) — COMPLETE (5/5 plans)
Plan: 5 of 5 complete (25-01: claude-flow cruft stripped from CLAUDE.md/settings.json/.mcp.json, Folklore hooks documented; CLEAN-01/02/03/06 satisfied. 25-02: retrieval module layout documented; CLEAN-04 satisfied. 25-03: 29 standalone benchmark runners consolidated under bench/ with repro README, docs repointed; CLEAN-05 satisfied. 25-04: akashikprotocol-clean layout map (docs/architecture/REPO-LAYOUT.md) + spec/ and examples/ surfaces + written usefolklore org-split plan (docs/REPO-SPLIT.md) + README repo map; REPO-01/REPO-03 satisfied, zero source moves, tsc green. 25-05: validation gate — build exit 0, lint exit 0, full suite 942 pass / 0 fail (zero regressions), config-surface cruft grep clean, site untouched; REPO-02 satisfied. Evidence: 25-VALIDATION.md)

Phase 26 (Docs & Benchmarks) IN PROGRESS — 3 plans, 1 wave (all docs independent, disjoint files → fully parallel):

- 26-01 (DOCS-01): BENCHMARKS page — DONE (commits fab1843, cff009d). Added a "Which number is the headline" block reconciling the honest pure-Node 72.30% SciFact NDCG@10 (Wave 2: nomic-embed-text-v1.5 dense + BM25 FTS5 hybrid, RRF k=60) against the optional Rust bge-base sidecar's 75.22% = the site's 0.7522 LED (same dataset, same hybrid fusion). Added a "Federation web-fallback (simulator)" block labeling the FolkloreBench-F 17%→1% web_fallback_rate as illustrative simulator output (whitepaper §7.2: demonstration, not validated evidence; partly true by construction under v1 boolean retrieval). Wave-3 reranker (−1.92) / Wave-4 routing (+0.34 null) kept as failures; repro command behind every claim, all gated behind `npm run build`. Numbers cross-checked against bench/README.md, RETRIEVAL-MODULES.md §5, and the whitepaper — nothing invented. tsc green; only docs/product/BENCHMARKS.md touched. SUMMARY: .planning/phases/26-docs-and-benchmarks/26-01-SUMMARY.md.
- 26-02 (DOCS-02): DONE (commits 9f7e2a1, 74340da). Authored docs/rfc/0002-deny-on-confidence.md (PreToolUse network-before-web gate, three-condition AND-gate: satisfaction ≥ 0.85 / ≥ 2 hits / decision-layer use_memory, off by default) following the README template; every default grounded against live .claude/hooks/folklore-smart-hook.cjs. Used the real FOLKLORE_DENY_*/FOLKLORE_PREFETCH_PEERS env names — the plan facts listed stale AKASHIK_* knobs, corrected per the verify-env-names guardrail (zero AKASHIK_* in .claude/ or src/). Refreshed docs/rfc/README.md index (0001 + 0002, no orphans); committed previously-untracked RFC-0001 + README so the index links no untracked files. tsc green; changes confined to docs/rfc/. DOCS-02 satisfied. SUMMARY: .planning/phases/26-docs-and-benchmarks/26-02-SUMMARY.md.
- 26-03 (DOCS-03) COMPLETE: staged usefolklore org-profile README at .github/profile/README.md (folk-pop, product-first, alone-then-compounds bet, BENCHMARKS-backed numbers only — 72.30% pure-Node / 75.22% Rust sidecar same SciFact dataset, 11ms p50, 137M params; 17%→1% federation figure omitted as simulator-only; closes on "never research twice") + reconciled docs/REPO-SPLIT.md to point the usefolklore/.github row + migration note at the staged profile (resolved the stale "do not create" instruction, kept blocked-on-org caveat). tsc green; commits e4bc56c, 2dd7678; SUMMARY 26-03. DOCS-03 satisfied.

Doc-only phase: no source changes, `npx tsc --noEmit` stays 0. Conventional commits, no AI co-authors.

Phase 28 (Merch & Meme-Agent) IN PROGRESS:

- 28-02 (AGENT-01): Twitter meme-agent scaffold — DONE (commits 4b00ed1, 94ceb74, 30978ab). Built a standalone DRY-RUN-by-default agent under src/agents/meme-agent/ (4 TS files) running the full generate → (gated) post → append pipeline. types.ts defines MemeEntry/MemeAgentConfig as the single source of truth for the site/assets/memes.json record shape (the data contract Plan 28-03 renders). generate.ts: default no-credit SVG meme composited from existing assets/gen/*.png art under a palette-matched folk-pop caption band (Ok(MemeEntry source:'svg')); optional higgsfield nano_banana_2 path gated behind --higgsfield, wrapped so a missing CLI/exhausted credit returns Err (never throws) and the pipeline falls back to SVG. pipeline.ts: runMemeAgent reuses src/infrastructure/x-client.ts postTweet (no second OAuth impl) — the post step is reached ONLY when !dryRun AND X_CLIENT_ID, otherwise it logs "[dry-run] would post:" and makes ZERO network calls; append is atomic-ish (tmp+rename), missing memes.json starts a fresh array, a failed post never drops the meme. run.ts: CLI (--live/--higgsfield/--text), default full dry-run, exits non-zero on Err. Seeded site/assets/memes.json with 2 entries referencing existing folk art (relative paths, source:"seed"); docs/agents/meme-agent.md documents env vars (X_CLIENT_ID/X_CLIENT_SECRET, reused from publish), run command, dry-run guarantee, cron lines, and the memes.json schema. Landing dir is src/agents/ NOT a top-level tools/ because tsconfig rootDir:./src + include:[src/**/*] + lint:"eslint src tests" — outside src/ it would not compile or lint. End-to-end dry-run verified (no creds → exit 0, "[dry-run] would post", zero network, memes.json grew 2→3 with source:'svg' + no postedUrl); the test append + its SVG were reverted so committed memes.json is the clean 2-entry seed. Zero new deps (twitter-api-v2 + node:fs already present); tsc 0; full suite 947 pass / 0 fail / 9 skipped (was 942 pass, +5 new tests, zero regressions); lint clean. AGENT-01 satisfied. SUMMARY: .planning/phases/28-merch-and-meme-agent/28-02-SUMMARY.md.

Next action: Phase 28 (Merch & Meme-Agent) COMPLETE — all 3 plans done (28-01 MERCH-01, 28-02 AGENT-01, 28-03 AGENT-02). All 20/20 v3.0 requirements satisfied. Remaining work is blocked-on-user only (merch fulfilment URLs + $LORE launch + X API creds + Cloudflare auth/domain + usefolklore org). No further executable plans in the v3.0 milestone.

### v3.0 Phase Map

| Phase | Name | Requirements | Depends on |
|-------|------|-------------|------------|
| 25 | Cleanup & Repo Restructure | CLEAN-01..06, REPO-01..03 (9) | Phase 24 |
| 26 | Docs & Benchmarks | DOCS-01..03 (3) | Phase 25 |
| 27 | Site Build-Out | SITE-01..05 (5) | Phase 26 |
| 28 | Merch & Meme-Agent | MERCH-01, AGENT-01..02 (3) | Phase 27 |

**Coverage:** 20/20 v3.0 requirements mapped ✓

### Blocked on user (NOT in execution scope)

- higgsfield credit top-up (art animation)
- GitHub org `usefolklore` creation
- Cloudflare auth + usefolklore.com domain purchase
- $LORE token launch on bags.fm
- X API credentials (meme-agent scaffold runs once these exist)

## Session Continuity

- v2.0 closed at Phase 24 (Delete Rooms — V5 wire-protocol break). All prior milestone history preserved in ROADMAP.md and MILESTONES.md.
- Next action: `/gsd:plan-phase 25` to decompose Cleanup & Repo Restructure into wave-friendly plans.
- v3.0 phases are wave-decomposed in ROADMAP.md "Phase Details" — each phase's waves are explicitly marked independent vs. ordered for parallel execution.

## Accumulated Context

### Brand (v3.0)

- Folklore · org usefolklore · palette cream #f4ecd8 / ink #1d1813 / pink #ff4f6d / blue #2b3a8c / yellow #f5b921 / teal #1fae8b
- Fraunces + Geist Mono · hard sticker shadows, misregistered headers
- Deploy: Cloudflare Pages only (`wrangler.toml`, output `site/`)

### Retrieval / benchmarks (carried from v2.0 — feeds DOCS-01 / CLEAN-05)

- **Retrieval SOTA (measured, full BEIR):** Wave 2 hybrid nomic-embed-text-v1.5 + BM25 RRF = 72.30% NDCG@10 on SciFact; 34.11% on NFCorpus. Within ~2 points of bge-base-en-v1.5 at our 137M param budget.
- Wave 3 reranker (bge-reranker-base) regresses quality on scientific text (−1.92) due to MS-MARCO domain mismatch — avoid.
- Wave 4 room-aware routing gate failed: oracle routing on CQADupStack gives only +0.34 NDCG@10 — rooms are UX/permissions/discovery, not a retrieval signal.
- The retired 96.8% NDCG figure (v1.1) came from a 15-passage × 10-query mini-harness; current honest ceiling is 72.30% on full BEIR SciFact (Wave 2). BENCHMARKS page (DOCS-01) must use the honest numbers.
- Benchmark sources live in BENCH-v2.md + BENCH-COMPETITORS.md; CLEAN-05 consolidates them under `bench/` with repro commands.

### Architecture invariants (carried forward)

- Functional DDD: no classes in domain/app, neverthrow Results. AppError union covers all bounded contexts.
- sequenceLazy thunks for sequential ResultAsync (never eager map — races on shared state).
- PreToolUse hook is the key differentiator for agent integration (the "network-before-web" deny-on-confidence gate).
- Max 3 new deps per phase; hand-roll when a dep is heavier than the function. Verify library picks on ossinsight/gh API, not generic WebSearch.
- At least 3 items in acceptance tests (catches eager-sequence races).
- Git: no claude/anthropic co-authors on commits.

### Post-v2.0 codebase state (restructure operates on this)

- v2.0 deleted the `room` abstraction entirely (Phase 24): `workspace?: string` (read-side) + `private: boolean` (sharing gate), federation wire protocol V5, 13 MCP tools (down from 16). Cleanup/restructure in Phase 25 must not reintroduce room concepts.
- 313/313 tests passed at v2.0 close; REPO-02 holds this zero-regression bar through the restructure.

## Decisions (v3.0)

- AGENT-02 (28-03): wired the #memes grid to site/assets/memes.json with STATIC-FIRST PROGRESSIVE ENHANCEMENT rather than client-only rendering — the six seeded cards are the resilient server-rendered baseline, and JS replaces them (replaceChildren + removeAttribute data-static) ONLY on a successful non-empty MemeEntry[] fetch. This is the deliberate choice that satisfies "never blank at file://": every failure path (file:// fetch rejection, 404, bad JSON, empty/non-array, no renderable entries) returns early or is swallowed by try/catch + .catch(()=>{}), leaving the static cards untouched with no console-error spam. Render reuses ONLY the 28-02 MemeEntry contract (image/alt, caption→img title) and reproduces the exact inline card styling (cycling pink/blue/teal/yellow hard shadows) so there is zero schema/style drift between the agent (writer) and site (reader). Kept fully self-contained: no localStorage/sessionStorage (banned, grep-clean), no new dep, no new external host, no build step, relative image refs; Store/$LORE (28-01) + hero/commons/graph videos untouched. Verification accounted for two headless-Chrome quirks reused from Phase 27: the .rv IntersectionObserver never fires in a static capture and the DOM is clamped to ~500px min — so the data-driven/fallback PROOF is via `--dump-dom` of the real unmodified index.html (http: 2 cards, data-static gone, seed alts; file://: 6 cards, data-static kept), and the visual is via a throwaway forced-reveal clone served over http (so relative fetch resolves). Round-trip proven: dry-run agent appended entry 3 → unchanged site rendered it (2→3) → seed restored, test SVG removed (git clean). 390px PNG width == 390 (single-column, no overflow). tsc 0; only site/index.html (+32/-1).
- MERCH-01 (28-01): made the Store merch "real" by CSS-framing the existing gen art into product mockups rather than generating new product photos — honors the blocked-on-user higgsfield credit constraint (zero credits, zero new image files, no new external host) while still reading as a tee chest-print, a die-cut sticker sheet, and a round enamel pin. Mockup classes are scoped (`.shot.tee`/`.shot.sheet`/`.shot.pin`) so the generic `.prod`/`.shot` rules stay reusable; `position:relative` went on the base `.prod .shot` rule (harmless for the other cards — no abs-positioned children there) so the tee collar `::before` anchors inside the box. Spec lines use the plan's exact concrete material strings so they are grep-checkable product truth, not marketing fluff. Tasks 1+2 shipped in one atomic commit (c655cc1) because the mockup CSS, the spec CSS, and the per-card markup are one coherent Store-card upgrade gated by the same 1512/390px screenshot pass — splitting would have left spec markup with no spec CSS mid-stream. Inert `data-buy` CTAs + TODO comments + `$LORE` block preserved by construction; only site/index.html touched (+30/-4); tsc 0.
- AGENT-01 (28-02): the meme-agent lands under src/agents/meme-agent/ (NOT a top-level tools/) — the only spot that compiles under tsconfig rootDir:./src + include:[src/**/*] and is linted by `eslint src tests`, honoring the "tsc stays 0" + "compiles" constraints. DRY-RUN is the default and the only behavior without creds: the live X post is reached ONLY when --live AND X_CLIENT_ID are both present, otherwise it logs "[dry-run] would post:" and makes zero network calls — no account creation, no spend this milestone. Default generation is a no-credit SVG composited from existing assets/gen/ art (zero new deps); the higgsfield nano_banana_2 path (~1 credit) is opt-in behind --higgsfield and falls back to SVG on any failure (missing CLI / no credit → Err, never throws). The post step REUSES src/infrastructure/x-client.ts postTweet — no second OAuth implementation — and a failed post never drops the locally-generated meme (orElse keeps the bare entry; memes.json still appended). MemeEntry (types.ts) is the single source of truth for the memes.json record shape that Plan 28-03 renders; committed memes.json is a clean 2-entry seed (the round-trip test append + its generated SVG were reverted after verification). Zero new deps, tsc 0, full suite 947 pass / 0 fail (+5 new tests, zero regressions).
- SITE-01 (27-04): final composition/mobile sweep was audit-first then surgical — a full per-section screenshot pass at 1512px + 390px found the desktop composition already balanced (zero desktop edits) and only two real mobile issues, so the fix is 17 lines (+15/-2), not a redesign. Fixed the #join inscription by WRAPPING not inner-scrolling: `overflow-wrap:anywhere` on `.insc pre` + `.install` lets the libp2p multiaddr and the daemon/hooks comment break inside the dark box (preferred over a horizontal scroll inside the code block — worse on touch), with a `@media(max-width:480px)` drop to 12px mono + tighter padding. Freed the proof bars at ≤480px by trimming the `.bar` grid label column 120px→96px (track ~170px→~284px; the labels Folklore/Pinecone base/LangChain RAG still fit). Kept the breakpoint at the real-device-correct 480px even though headless Chrome clamps its DOM to a 500px minimum (so a 390px headless screenshot can't prove the rule directly) — verified the rule fires via a separate 520-breakpoint clone + getComputedStyle probe (preFont=12px, barCols=96px 284px 50px, DOC overflow=0); 390px phones satisfy 390 ≤ 480. Brand lock held by construction: the diff adds only overflow-wrap + one media block, so type scale, palette, folk-pop skin, scroll-snap, and the hero/commons/graph poster `<video>`s are all untouched (grep-confirmed). Only site/index.html touched; guidebook/culture/store all intact.
- SITE-04 (27-03): chose plan option (a) — folded the standalone `#coin` $LORE block into `#store` as a `.lore` sub-block and deleted `#coin`, so one coherent Store holds both merch and the coin and reads like a real shop (nav never linked `#coin`, grep-verified, so no anchor cleanup). Built the shop on a fresh minimal `.shop`/`.prod` card system (mirroring the `#memes` `repeat(auto-fit,minmax(240px,1fr))` grid) rather than extending the old 2-panel `.merch` grid — the 2-up layout didn't cleanly become a 3-up card layout, and auto-fit gives single-column mobile stacking with no extra media query for the cards. E-commerce placeholders are deliberately INERT-but-wired, never fabricated: each buy CTA is `aria-disabled="true"` + `data-buy="<id>"` + `href="#store"` (in-page, no fake shop URL) with a `<!-- TODO: real product URL at fulfilment launch -->` comment; each price is a visible `$—` slot (`data-price=""`), not a fake price — so the real fulfilment URLs + $LORE launch (both blocked-on-user) drop in mechanically. Product art: Tee→tee.png, Sticker Pack→fishflower.png + sun.png (paired in one card), Pin→char1.png (the round "teller" character reads as an enamel pin). The bags.fm CTA + the verbatim "not financial advice" disclaimer + the round/bordered/hard-shadow coin treatment were all preserved from the old `#coin` block. Screenshot acceptance reused the 27-01/27-02 harness (clone to /tmp, force `.rv` visible + `min-height:100svh→auto`); the full page is ~11000px tall at 1512w so the store was captured with a tall window and cropped; 390px no-overflow confirmed by full-page mobile PNG width == 390. Only `site/index.html` touched (+78/-30); Guidebook/Culture/#memes/videos untouched.
- SITE-03 (27-02): the Platform Culture section was built on the existing `.illum`/`.il` 3-card grid (the same component Mechanism + the 27-01 Guidebook use) rather than stacked `.beat` rows — the per-child pink/blue/yellow shadows + clamped type come for free, so the folk-pop brand lock holds by construction with zero new CSS and zero new deps. Placed between #together and #mechanism so the page reads problem → alone → together → culture (why it matters / the commons) → mechanism (how): Culture bridges the emotional beat into the technical one. Card `.num` is a folk dot (`·`) rather than roman numerals, to visually separate the cultural beats from the numbered how-to steps in Mechanism/Guidebook. The thesis ("the lore is the graph; the folk are the peers") is restated as a culture beat here (echoing the Name flourish — this section earns it). Copy is strictly docs/brand/README.md voice (hearth/stone-soup/commons; no "revolutionary"/"paradigm"/protocol-speak), bet stated in order (alone first, then compounds). Screenshot acceptance reused the 27-01 harness: clone site to /tmp, force `.rv` visible AND neutralize `min-height:100svh→auto` so the page flows to content height for a clean full-page Chrome `--headless=new` capture; 390px no-overflow confirmed by full-page mobile PNG width == 390. Only site/index.html touched (+28/-1); hero/commons/graph videos and the 27-01 Guidebook untouched.
- SITE-05 (27-05): treated as verify-only — wrangler.toml (`pages_build_output_dir = "site"`) and site/_headers were already correct, so zero files were touched (the plan's "fix only if needed" branch never fired). Did NOT assert the CF-Pages `_headers` security headers over the local python static serve, because a plain static server does not apply `_headers` — header presence is asserted at the file/config level instead, exactly per the plan note. Did NOT run any deploy/login/publish; the actual Cloudflare Pages deploy stays blocked-on-user (Cloudflare auth + usefolklore.com domain). Local CF-Pages-equivalent proof: static serve of site/ returns 200 at `/` (Folklore title) and 200 for the relative asset /assets/gen/hero.png, so once the user completes auth+domain the deploy is a one-step `wrangler pages deploy site`.
- SITE-02 (27-01): the Guidebook was built on the existing `.illum`/`.il` 4-card grid (the Mechanism section's layout) rather than a new component, so the per-child colored shadows + clamped type come for free and the folk-pop brand lock holds by construction. Card i reuses the hero `.install` chip (size-trimmed) to make the install command copyable through the existing `.copy` handler — no new JS, no browser storage, no new CSS framework, no new deps. All copy quotes the real CLI surface verbatim (folklore onboard/ask/peer add … --peers) with accurate deny-on-confidence wording; nothing invented. Screenshot acceptance for `.rv`-animated sections required isolating the section into a styles-inlined doc with `.rv` forced visible (the IntersectionObserver never fires in a static headless capture); 390px no-overflow verified by confirming the full-page mobile PNG width equals 390px.
- DOCS-01 (26-01): the single honest BENCHMARKS headline is 72.30% SciFact NDCG@10 (Wave 2 pure-Node hybrid) because it reproduces from a fresh clone with zero extra build steps and is the canonical figure in bench/README.md. The 75.22%/0.7522 figure (= the site's LED) is reconciled in prose as the SAME SciFact dataset + same hybrid RRF fusion on the optional Rust bge-base sidecar — not a separate claim. FolkloreBench-F's 17%→1% web-fallback decay is labeled a SIMULATOR figure mirroring whitepaper §7.2 ("demonstration, not validated evidence; partly true by construction under v1 boolean retrieval"). Wave-3/Wave-4 nulls kept as failures; existing ASCII box, leaderboard table, and 13-null-attacks table left intact. All numbers cross-checked against three in-repo sources; nothing invented; no source/site files touched; tsc green.
- v3.0 phasing: foundational Cleanup & Repo Restructure (25) precedes Docs (26) → Site (27) → Merch & Agent (28), because docs reference the clean `bench/` + module layout, the site sources docs content, and merch/agent fill the site Store.
- Meme-agent (AGENT-01) is scaffold-only this milestone: full generate → post → append-to-`/store` pipeline runs against mocked/credential-gated X access; no live X post until user supplies X API creds.
- Store section (SITE-04) is structured for live products with link points wired but inert until $LORE launch + merch fulfilment exist (both blocked on user).
- CLEAN-04 (25-02): satisfied "documented module layout" with an authoritative map (docs/architecture/RETRIEVAL-MODULES.md) + in-tree index (src/infrastructure/README.md) rather than physically moving source files — a file reshuffle would force an import rewrite across ~80 test files, endangering the zero-regression bar for no behavioral gain.
- CLEAN-01/02/03/06 (25-01): the new CLAUDE.md does NOT re-list the generic-good behavioural rules from the deleted claude-flow block — the authoritative conventions live in PROJECT.md + STATE.md "Architecture invariants", so CLAUDE.md just points there. statusLine was collapsed to the single helper that exists on disk (ak-statusline.cjs). CLEAN-06 documented by recording the removed 3-tier router and pointing to the real hw-detect rerank hardware-tier picker in .claude/README.md.
- SECURITY follow-up (25-01): untracked-but-present .claude/settings.local.json holds a live HCLOUD_TOKEN + stale claude-flow enabledMcpjsonServers entry. Gitignored (won't ship) but token should be rotated before public launch.
- REPO-01/REPO-03 (25-04): satisfied the akashikprotocol-clean layout by documenting it (docs/architecture/REPO-LAYOUT.md) and adding the two missing surfaces (spec/README.md as a thin index into docs/rfc + V5-PROTOCOL; examples/README.md with CLI commands verified against `folklore help`) rather than moving src/ — a source reshuffle would break tsconfig rootDir + ~85 test files of relative imports for zero behavioral gain (same precedent as CLEAN-04). The org split (docs/REPO-SPLIT.md) is a written plan only: physical multi-repo extraction (usefolklore core+cli/spec/site/.github) is deferred until the org exists (blocked on user); the doc records the exact git filter-repo boundaries so the split is mechanical later.
- REPO-02 (25-05): phase-25 validation gate is green — `npm run build` exit 0, `npm run lint` exit 0, `npm test` 942 pass / 0 fail (951 total, 9 skipped) matching the planning-time baseline, so the cleanup + restructure introduced zero regressions. Cruft grep over the live config surface (CLAUDE.md/.claude/settings.json/.mcp.json) is clean — the only hits are the documented-removal sentences in .claude/README.md. Site is untouched (it is entirely untracked; no phase-25 commit touched it; index.html + assets/gen present). Evidence: 25-VALIDATION.md. Phase 25 closed.
- CLEAN-05 (25-03): 29 standalone benchmark runners moved scripts/ → bench/ (24 via git mv preserving history, 5 untracked via mv+add) with a copy-paste repro README. No import edits needed — scripts/ and bench/ are repo-root siblings so `../dist/` resolves identically (verified by smoke import + full 942-test pass). Frozen docs/research/octopus-discover/ audit captures left unedited (dated historical records, not live repro paths).
